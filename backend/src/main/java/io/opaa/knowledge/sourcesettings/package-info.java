/**
 * The S3 connector's settings records the library entity still carries: the typed configuration,
 * its JSON form and its scopes. They move to the connector once each connector owns its settings
 * (#1977).
 */
package io.opaa.knowledge.sourcesettings;
