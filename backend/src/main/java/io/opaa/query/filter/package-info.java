/**
 * The metadata filter offer the user interface presents for a search scope - which values a person
 * can filter by, cached per user and scope (docs/handbuch/suche.md, Abschnitt 5). Not part of the
 * pipeline: applying a filter is {@code io.opaa.query.retrieval.scope}'s job.
 */
package io.opaa.query.filter;
