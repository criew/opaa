/**
 * The retrieval pipeline's frame - the stage contract, the run's context and state, and the
 * explanation protocol every stage writes (docs/handbuch/suche.md, Abschnitte 1, 4 und 8.2). The
 * stages themselves live in the sub-packages {@code scope}, {@code search} and {@code ranking}.
 */
package io.opaa.query.retrieval;
