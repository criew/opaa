name: Daily Report

on:
  schedule:
    # 04:30 UTC entspricht 06:30 MESZ beziehungsweise 05:30 MEZ. Der Vortag ist
    # damit in jedem Fall abgeschlossen.
    - cron: "30 4 * * *"
  workflow_dispatch:
    inputs:
      date:
        description: "Berichtstag als JJJJ-MM-TT (leer = Vortag)"
        required: false
        type: string

permissions:
  contents: write
  issues: read
  pull-requests: read
  actions: read

concurrency:
  group: daily-report
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Skript auschecken
        uses: actions/checkout@v4
        with:
          sparse-checkout: .github/scripts
          sparse-checkout-cone-mode: false

      - name: Veröffentlichte Seite auschecken
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: site
        continue-on-error: true

      - name: Seitenverzeichnis vorbereiten
        run: mkdir -p site

      - name: Zeitzonendaten bereitstellen
        run: python -m pip install --quiet tzdata

      - name: Report erzeugen
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPAA_OPENAI_API_KEY: ${{ secrets.OPAA_OPENAI_API_KEY }}
          OPAA_REPORT_MODEL: ${{ vars.OPAA_REPORT_MODEL }}
        run: |
          python .github/scripts/daily_report.py \
            --repo "${{ github.repository }}" \
            --date "${{ inputs.date }}" \
            --output site \
            --site-url "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"

      - name: Seite veröffentlichen
        working-directory: site
        run: |
          if [ ! -d .git ]; then
            git init -b gh-pages
            git remote add origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "Keine Änderungen zu veröffentlichen."
            exit 0
          fi
          git commit -m "docs(report): Tagesreport aktualisieren"
          git push origin gh-pages
