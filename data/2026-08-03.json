{
  "repo": "criew/opaa",
  "date": "2026-08-03",
  "generated_at": "2026-08-04T09:12:06.698672+02:00",
  "closed_issues": [
    {
      "number": 302,
      "title": "docs(agents): Umgang mit Transaktionen in die Entwickler-Rollendefinition aufnehmen",
      "url": "https://github.com/criew/opaa/issues/302",
      "author": "criew",
      "labels": [
        "documentation",
        "size:S"
      ],
      "body": "## Kontext\n\nIn Stufe A von #198 hat dieselbe Konstruktion dreimal einen Fehler verursacht, jedes Mal fachlich falscher Code bei grünem CI, jedes Mal erst im Review gefunden:\n\n| PR | Konstruktion | Fehler |\n|---|---|---|\n| #280 | `REQUIRES_NEW` innerhalb einer offenen Transaktion | Eigene Connection sah die noch nicht committete `users`-Zeile nicht, `fk_spaces_owner` schlug fehl — **jede Erstanmeldung** war gebrochen |\n| #297 | `REQUIRES_NEW` für einen Statusschreiber | Committete vor der Hauptransaktion und meldete `APPLIED` für eine Anwendung, die zurückgerollt wurde |\n| #299 | `REQUIRES_NEW` neben einer haltenden äußeren Transaktion | Zwei Pool-Connections gleichzeitig je paralleler Anfrage; ab Pool-Größe Stillstand für 30 Sekunden, danach Fehler — auch für unbeteiligte Requests |\n\nDazu kam in #297 ein verwandter Fall: Eine Planberechnung unter `@Transactional(readOnly = true)` behauptete im Javadoc, „nichts wurde geschrieben\" sei strukturell garantiert — der `save()`-Aufruf stand im Code und wurde nur vom Flush-Modus geschluckt.\n\nIn allen Fällen war die Absicht richtig und die Nebenwirkung überraschend. Und in allen Fällen war die beste Lösung, die Konstruktion **ganz zu vermeiden**, statt sie zu reparieren.\n\n## Warum in die Rollendefinition und nicht in AGENTS.md\n\nEs ist Handwerkswissen des Entwicklers, keine Projektkonvention. Entscheidung des Maintainers.\n\n## Umsetzung\n\nEinen Abschnitt zu Transaktionen in `agents/roles/developer.md` aufnehmen, sinnvollerweise zwischen „Testschutz\" und „Umfang und Blocker\". Inhalt:\n\n- Vor dem Einsatz einer eigenen Transaktion (`REQUIRES_NEW`, `TransactionTemplate`) prüfen, ob sie wirklich gebraucht wird — sie ist in diesem Projekt dreimal zur Fehlerquelle geworden.\n- Wenn sie gebraucht wird, beide Fehlerrichtungen prüfen und im PR benennen: Was passiert bei Fehlschlag der inneren, was bei Fehlschlag der äußeren Transaktion.\n- Sichtbarkeit: Eine eigene Transaktion sieht nichts, was die umgebende noch nicht committet hat.\n- Commit-Reihenfolge: Eine innere Transaktion committet vor der äußeren. Was sie schreibt, überlebt deren Rollback.\n- Ressourcen: Eine innere Transaktion neben einer haltenden äußeren belegt zwei Connections gleichzeitig. Bei nebenläufigen Aufrufen skaliert das mit der Anzahl paralleler Anfragen.\n- `@Transactional(readOnly = true)` verhindert Schreibzugriffe nicht strukturell, sondern schluckt sie über den Flush-Modus. Wer „schreibt nichts\" zusichert, muss den Schreibpfad entfernen, nicht auf die Annotation vertrauen.\n- Prüfen, ob die umgebende Methode überhaupt `@Transactional` braucht — oft ist der einfachste Fix, sie wegzulassen.\n- Nach einer abgefangenen `DataIntegrityViolationException` ist die umgebende Transaktion unter PostgreSQL abgebrochen; ein Neulesen braucht einen eigenen Kontext.\n\nAuf Deutsch, im Stil der bestehenden Abschnitte: knappe Stichpunkte, keine Abhandlung. Die drei PR-Nummern als Beleg nennen, damit der Abschnitt nicht als abstrakte Vorsicht gelesen wird.\n\nPrüfen, ob die Client-Adapter (`.claude/agents/developer.md`, `.codex/agents/developer.toml`, `.opencode/agents/developer.md`) unverändert bleiben können — sie verweisen nur auf den gemeinsamen Vertrag und sollten nichts Inhaltliches enthalten.\n\n## Abnahmekriterien\n\n- [ ] `agents/roles/developer.md` enthält den Abschnitt\n- [ ] Die drei Fälle sind als Beleg genannt\n- [ ] Client-Adapter bleiben frei von Inhalt\n- [ ] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt",
      "closes": []
    },
    {
      "number": 295,
      "title": "docs(agents): Branch-Regel für Fehlerbehebungen und Hotfixes klarstellen",
      "url": "https://github.com/criew/opaa/issues/295",
      "author": "criew",
      "labels": [
        "documentation",
        "size:S"
      ],
      "body": "## Kontext\n\n`AGENTS.md` legt verbindlich fest, dass Branches immer mit `feature/` beginnen und die GitHub-Issue-ID enthalten.\n\nBeim Hotfix zu PR #287 wurde stattdessen `fix/280_personal-space-transaction` verwendet — naheliegend, weil es sich um eine Fehlerbehebung handelte, aber gegen die Regel. Im Review fiel auf, dass die Formulierung zwar eindeutig ist, aber nicht ausdrücklich sagt, dass sie auch für Fehlerbehebungen und dringende Korrekturen gilt.\n\n## Entscheidung des Maintainers\n\nKein zusätzliches `fix/`-Präfix. **Alle** Branches beginnen mit `feature/`, unabhängig von der Art der Änderung — auch Fehlerbehebungen und Hotfixes.\n\n## Umsetzung\n\nIn `AGENTS.md` unter „Branch-Regel (verbindlich)\" ergänzen, dass die Regel ausnahmslos gilt, einschließlich Fehlerbehebungen und dringender Korrekturen. Der Conventional-Commit-Typ (`fix`, `feat`, …) bleibt davon unberührt und wird weiterhin in der Commit-Nachricht und im PR-Titel geführt.\n\nPrüfen, ob `.claude/rules/workflow.md` und `CONTRIBUTING.md` dieselbe Aussage tragen und angeglichen werden müssen.\n\n## Abnahmekriterien\n\n- [ ] `AGENTS.md` stellt klar, dass die Regel auch für Fehlerbehebungen und Hotfixes gilt\n- [ ] `.claude/rules/workflow.md` und `CONTRIBUTING.md` sind konsistent\n- [ ] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt",
      "closes": []
    },
    {
      "number": 293,
      "title": "fix(auth): Race bei paralleler Erstanmeldung erzeugt 500er auf uq_users_subject_issuer",
      "url": "https://github.com/criew/opaa/issues/293",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:S",
        "auth"
      ],
      "body": "## Kontext\n\n`UserService.findOrCreateUser` prüft auf einen vorhandenen Nutzer und legt ihn andernfalls an. Zwischen Prüfung und Anlage liegt keine Behandlung der Unique-Constraint `uq_users_subject_issuer`.\n\nBei der allerersten Anmeldung eines Nutzers laufen mehrere Requests parallel: `UserProvisioningFilter` greift bei **jedem** authentifizierten Request, und die Single-Page-Anwendung setzt direkt nach dem Login mehrere Aufrufe gleichzeitig ab.\n\nBeim Review von PR #287 empirisch gemessen: Bei vier parallelen Erstanmeldungen desselben Subjects scheiterten in **15 von 15 Runden jeweils drei der vier Aufrufe** mit\n\n```\nduplicate key value violates unique constraint \"uq_users_subject_issuer\"\n```\n\nEiner gewinnt, Nutzer und persönlicher Space entstehen korrekt, ab dem nächsten Request ist alles in Ordnung. Der Nutzer sieht beim ersten Login jedoch Fehler.\n\n## Abgrenzung\n\nNicht durch #280 oder #287 eingeführt — derselbe Code steht unverändert auf `main` und stand dort schon vorher. #287 macht die Anmeldung wieder funktionsfähig; „die erste Anmeldung ist vollständig fehlerfrei\" gilt erst mit diesem Issue.\n\n## Umsetzung\n\nDasselbe Muster wie in `SpaceService.ensurePersonalSpace`: `DataIntegrityViolationException` fangen und den inzwischen von der konkurrierenden Transaktion angelegten Nutzer neu lesen.\n\nZu beachten: Der Re-Read braucht einen eigenen Transaktionskontext, weil ein fehlgeschlagener Insert unter PostgreSQL die umgebende Transaktion abbricht — siehe die Begründung in `SpaceService.ensurePersonalSpace` und PR #287.\n\n## Abnahmekriterien\n\n- [ ] Parallele Erstanmeldungen desselben Subjects führen zu genau einem Nutzer, ohne Fehler beim Aufrufer\n- [ ] Test mit mehreren echten Threads gegen Postgres mit Liquibase-Schema (`spring.liquibase.enabled=true`, `ddl-auto=none`), Muster: `UserServicePersonalSpaceIntegrationTest`\n- [ ] Der Test schlägt ohne den Fix fehl — nachweisen\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nGefunden beim Review von PR #287.",
      "closes": []
    },
    {
      "number": 288,
      "title": "test(backend): FK-abhängige Integrationstests auf echtes Liquibase-Schema umstellen",
      "url": "https://github.com/criew/opaa/issues/288",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:M"
      ],
      "body": "## Kontext\n\nDie Integrationstests laufen überwiegend mit `spring.liquibase.enabled=false` und `spring.jpa.hibernate.ddl-auto=create-drop`. Das Schema kommt damit von Hibernate, nicht von Liquibase — und die beiden unterscheiden sich an einer entscheidenden Stelle.\n\n`Space.ownerId`, `SpaceMembership.userId` und sämtliche `organizationId`-Felder sind schlichte `UUID`-Spalten ohne `@ManyToOne`. Hibernate legt dafür **keine** Fremdschlüssel an, Liquibase schon. **Jeder FK-abhängige Fall ist in diesen Suiten damit ungetestet.**\n\n## Warum das jetzt angegangen werden muss\n\nInnerhalb kurzer Zeit sind zwei Regressionen durch grünes CI gerutscht, beide aus derselben Wurzel:\n\n1. **PR #254** — Changelog 008 brach auf jedem nicht-leeren Bestand ab. Nicht bemerkt, weil die Integrationstests Liquibase gar nicht ausführen und die einzige Stelle, die es anwendet, gegen eine leere Datenbank läuft.\n2. **PR #280** — die `REQUIRES_NEW`-Transaktion in `ensurePersonalSpace` brach auf `main` jede Erstanmeldung, weil der Insert gegen `fk_spaces_owner` lief. Nicht bemerkt, weil dieser Fremdschlüssel im Testschema nicht existiert. Behoben in PR #287.\n\nBeide Male war der Code fachlich falsch und die Tests waren grün. Das ist kein Zufall, sondern ein struktureller blinder Fleck.\n\n**#201** (Wissensbibliothek als Dokumentencontainer) und **#202** (Asset-Rechte und rechtebewusste Vektorsuche) führen weitere Fremdschlüssel und die gesamte Rechtedurchsetzung ein. Ein blinder Fleck an dieser Stelle wäre deutlich teurer als die beiden bisherigen Fälle.\n\n## Umsetzung\n\nSchrittweise Umstellung FK-abhängiger Tests auf `spring.liquibase.enabled=true` und `ddl-auto=none`. Als Muster dient `UserServicePersonalSpaceIntegrationTest` aus PR #287.\n\nNicht jeder Test muss umgestellt werden — reine Service-Logik ohne Bezug auf Fremdschlüssel darf beim schnelleren Hibernate-Schema bleiben. Zu entscheiden ist, welche Suiten betroffen sind; Kandidaten sind `SpaceServiceIntegrationTest`, `SpaceRepositoryTest` und alles, was Nutzer-, Space- oder Organisationsbezüge schreibt.\n\nZusätzlich zu klären: ob die Laufzeit darunter leidet und ob sich ein gemeinsamer Testcontainer über mehrere Suiten hinweg lohnt.\n\n## Nebenpunkt: uneinheitliche Teardown-Muster\n\nIn den Migrationstests stehen inzwischen zwei Lösungen für dasselbe Problem nebeneinander — Liquibase lässt die JDBC-Verbindung nach `update()` mit `autoCommit=false` zurück:\n\n- `Migration010SpaceUniquenessTest`: `setAutoCommit(true)` nach jedem `update()`, Schema-Reset im `tearDown()`\n- `Migration009CreateGroupsTest`: `connection.rollback()` im `tearDown()`\n\nBeides ist korrekt. Vor #237 und #238 sollte eines davon als verbindliches Muster festgelegt werden, damit die Folge-Issues nicht raten müssen.\n\n## Abnahmekriterien\n\n- [ ] Entscheidung dokumentiert, welche Suiten auf Liquibase-Schema laufen und welche nicht\n- [ ] Betroffene Suiten umgestellt, alle Tests grün\n- [ ] Ein Test, der ohne Fremdschlüssel bestehen würde, schlägt nach der Umstellung ohne den zugehörigen Produktivcode fehl (Nachweis, dass die Umstellung wirkt)\n- [ ] Einheitliches Teardown-Muster für Migrationstests festgelegt und dokumentiert\n- [ ] Laufzeit der Testsuite gemessen und bewertet\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nFolge aus PR #254, #280 und #287. Gehört zu Stufe A von #198 und sollte **vor #202** abgeschlossen sein.",
      "closes": []
    },
    {
      "number": 237,
      "title": "Verzeichnissynchronisation als Rechteereignis behandeln",
      "url": "https://github.com/criew/opaa/issues/237",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "size:L",
        "security",
        "auth"
      ],
      "body": "## Kontext\r\n\r\nGruppen kommen aus dem Verzeichnisdienst. Ein Synchronisationslauf, der Mitgliedschaften entfernt,\r\nist damit ein **Massen-Rechteentzug ohne menschlichen Entscheidungspunkt** — die einzige Stelle im\r\nModell, an der sich Rechte in großer Zahl ändern, ohne dass jemand entscheidet.\r\n\r\nDie frühere Formulierung, die Synchronisation ändere „nur die Herkunft der Mitgliedschaften, nicht\r\ndas Rechtemodell\", ist irreführend: Die Herkunft **ist** das Risiko. An ihr hängen die\r\nNachfolgeregelung, der Zustand von Strikt-Spaces und die Nachweisbarkeit.\r\n\r\nDies ist keine spätere Ausbaustufe, sondern Voraussetzung für den ersten Produktivbetrieb.\r\n\r\nSiehe [`docs/features/spaces-and-assets.md`](docs/features/spaces-and-assets.md) und [ADR-0008](docs/decisions/0008-space-and-asset-model.md).\r\n\r\n## Umfang\r\n\r\n- Abgleich über die **stabile Kennung** des Verzeichnisses (`objectGUID`, SCIM-`externalId`),\r\n  niemals über den Gruppennamen\r\n- **Plausibilitätsschwelle mit Abbruch:** Übersteigt ein Lauf einen konfigurierbaren Anteil\r\n  geänderter Mitgliedschaften, wird er abgebrochen und gemeldet\r\n- **Trockenlauf mit Differenzbericht** vor dem ersten Lauf und vor jeder Konfigurationsänderung\r\n- **Nicht erreichbares Verzeichnis: `last-known-good`.** Keine Rechte werden entzogen, der Zustand\r\n  wird gemeldet\r\n- **Eine Protokollzeile je bewirkter Rechteänderung**, nicht je Lauf\r\n- **Reorganisation:** Umbenennung folgenlos (stabile Kennung); bei Zusammenlegung oder Neuschnitt\r\n  werden Gruppen als aufgelöst markiert, bestehende Grants bleiben wirksam, können aber nicht\r\n  erweitert werden; Assets aufgelöster Einheiten gehen in „Nachfolge offen\"\r\n- **Reorganisationsbericht** für den System-Admin: weggefallene Einheiten, betroffene Assets und\r\n  Grants, offene Nachfolgen\r\n\r\n## Abnahmekriterien\r\n\r\n- [ ] Eine umbenannte Gruppe behält ihre Grants und ihre Mitglieder\r\n- [ ] Ein Lauf, der mehr als den konfigurierten Anteil an Mitgliedschaften ändert, wird abgebrochen\r\n      und gemeldet, ohne etwas zu verändern\r\n- [ ] Liefert das Verzeichnis eine leere Gruppenliste, wird kein Recht entzogen\r\n- [ ] Ist das Verzeichnis nicht erreichbar, bleibt der letzte bekannte Stand in Kraft\r\n- [ ] Der Trockenlauf verändert nichts und liefert die vollständige Differenz\r\n- [ ] Jede bewirkte Rechteänderung erzeugt genau eine Protokollzeile mit Ursache\r\n- [ ] Assets einer aufgelösten Einheit sind nutzbar, aber ihre Reichweite ist eingefroren\r\n- [ ] Backend-Formatierung, Build und Tests bestehen\r\n\r\n## Hängt ab von\r\n\r\nGruppen als Rechtesubjekt",
      "closes": []
    },
    {
      "number": 228,
      "title": "ci(eval): Retrieval-Regressionsjob mit Baseline und Schwellenwerten",
      "url": "https://github.com/criew/opaa/issues/228",
      "author": "criew",
      "labels": [
        "enhancement",
        "size:M",
        "ci",
        "evaluation"
      ],
      "body": "## Zusammenfassung\n\nDer Retrieval-Harness läuft automatisiert in GitHub Actions gegen eine committete Baseline und schlägt fehl, wenn die Suchqualität abrutscht.\n\n## Kontext / Warum\n\nEin Harness, den niemand ausführt, verhindert keine Regression. Erst der automatisierte Lauf gegen eine feste Baseline macht Suchqualität zu einem Qualitätsgate.\n\nBewusst **nicht bei jedem Pull Request**: Der Lauf zieht ein Ollama-Modell (~275 MB) und bettet 1.000 Dokumente ein. Das ist zu teuer für jeden Push und würde nur dazu führen, dass der Job abgeschaltet wird.\n\nSpezifikation: `docs/features/search-quality-evaluation.md`, Abschnitte „Baseline und Fehlerkriterium\" und „Auslösung\". Entscheidung: ADR-0008.\n\n## Ziel / Ergebnis\n\nDanach fällt eine Verschlechterung des Retrievals automatisch auf, ohne dass ein Mensch danach sucht.\n\n## Umfang\n\n- Neuer Workflow-Job, ausgelöst durch: nächtlichen Zeitplan auf `main`, manuelle Auslösung (`workflow_dispatch`) und Label `evaluation` an einem Pull Request\n- Baseline als versioniertes JSON im Repository (`eval/baseline/comic-characters.json`)\n- Zweistufiges Fehlerkriterium: harte Untergrenze je Primärmetrik **und** maximale Verschlechterung gegenüber der Baseline (Vorschlag 0,03 absolut)\n- Verbesserungen schlagen nie fehl, erzeugen aber einen sichtbaren Hinweis zur Baseline-Aktualisierung\n- Report als CI-Artefakt mit Aufbewahrungsfrist\n- Bei Auslösung per PR-Label: Ergebnis als PR-Kommentar mit Delta-Tabelle gegenüber der Baseline\n\n## Außerhalb des Umfangs\n\n- Der Harness selbst (#227)\n- Gepaarte statistische Vergleichsläufe zwischen zwei Konfigurationen\n- Trendauswertung über die Zeit / Dashboard\n\n## Abnahmekriterien\n\n- [ ] Der Job läuft nächtlich auf `main`, per `workflow_dispatch` und per Label `evaluation` an einem Pull Request\n- [ ] Eine committete Baseline liegt im Repository und dokumentiert, mit welcher Konfiguration sie erzeugt wurde\n- [ ] Eine künstlich herbeigeführte Verschlechterung (z. B. `topK` auf 1 oder Ähnlichkeitsschwelle auf 0,9) lässt den Job nachweislich fehlschlagen\n- [ ] Eine Verbesserung lässt den Job bestehen und erzeugt einen Hinweis auf die veraltete Baseline\n- [ ] Die Fehlermeldung nennt Metrik, Baseline-Wert, Ist-Wert und Delta — nicht nur „failed\"\n- [ ] Der Job überschreitet 20 Minuten Laufzeit nicht\n- [ ] Der Job benötigt keine Secrets und läuft damit auch in Forks\n- [ ] Das Verfahren zur Baseline-Aktualisierung ist dokumentiert: wer darf sie ändern, mit welcher Begründung\n- [ ] Dokumentation aktualisiert (`AGENTS.md`/`CONTRIBUTING.md`: wann muss die Baseline neu gezogen werden)\n\n## Technische Hinweise\n\n- Modell-Cache in Actions prüfen, um den Ollama-Pull nicht bei jedem Lauf zu bezahlen.\n- Bei Auslösung per Label an einem PR muss klar sein, gegen welchen Korpus-Stand gemessen wird — die Manifest-Summe gehört in den Kommentar.\n\n## Betroffene Module\n\n`.github/workflows/`, `eval/`. Keine Änderung am Produktivcode.\n\n## Abhängigkeiten\n\nBlockiert durch #227.\n\n## Teil des Epics\n\n#224",
      "closes": []
    },
    {
      "number": 227,
      "title": "test(eval): Retrieval-Metrik-Harness (Hit Rate, MRR, nDCG, Recall)",
      "url": "https://github.com/criew/opaa/issues/227",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "size:L",
        "evaluation"
      ],
      "body": "## Zusammenfassung\n\nEin JUnit-Integrationstest indiziert den eingefrorenen Korpus über die produktive Pipeline, führt alle Golden-Queries gegen den Vektor-Store aus und berechnet Hit Rate@5, MRR, nDCG@10 und Recall@10.\n\n## Kontext / Warum\n\nOhne Zahl gibt es keine Aussage über Suchqualität. Der Harness ist das Werkzeug, das aus „fühlt sich besser an\" ein „Hit Rate@5 steigt von 0,81 auf 0,87\" macht.\n\nBewusst **ohne LLM**: Retrieval-Metriken sind reine Ranking-Metriken. Generationsmetriken (`RelevancyEvaluator`, `FactCheckingEvaluator`) folgen in einer späteren Phase und würden hier nur Laufzeit, Kosten und Rauschen einbringen.\n\nBewusst **nicht gegen ein laufendes System**: `QueryService` bündelt Suche und Antwortgenerierung, ein retrieval-only-Pfad existiert über die API nicht. Der Harness spricht `VectorStore.similaritySearch` direkt an und ist damit unabhängig von der Demo-Bereitstellung.\n\nSpezifikation: `docs/features/search-quality-evaluation.md`, Abschnitt „Retrieval-Harness und Regression in CI\". Metrikdefinitionen: `docs/discussions/discussion-rag-evaluation.md`, Abschnitt 2.1.\n\n## Ziel / Ergebnis\n\nDanach lässt sich die Retrieval-Qualität lokal mit einem Gradle-Aufruf messen und als Report ausgeben.\n\n## Umfang\n\n- Integrationstest im Backend, eigener Testtag/Task, damit er nicht Teil von `./gradlew build` ist\n- Testcontainers: `pgvector/pgvector:pg18` und Ollama mit `nomic-embed-text` (Modellversion festgenagelt)\n- Prüfung von `MANIFEST.sha256` vor dem Lauf; Abbruch bei Abweichung\n- Indizierung des Korpus über die produktive Pipeline (`FileProcessingService` / `ChunkingService`), nicht über einen Sonderweg\n- Abbildung der Treffer auf Entitäten über das Chunk-Metadatum `file_name`\n- Metriken: Hit Rate@5, MRR, nDCG@10, Recall@10 — gesamt und aufgeschlüsselt nach Kategorie, Schwierigkeit und Sprache\n- Report als maschinenlesbares JSON plus lesbare Zusammenfassung\n\n## Außerhalb des Umfangs\n\n- CI-Integration und Baseline-Vergleich (#228)\n- Generationsmetriken, RAGAS-Sidecar\n- Gepaarte Bootstrap-Vergleiche zwischen zwei Konfigurationen\n\n## Abnahmekriterien\n\n- [ ] Der Harness läuft lokal über einen eigenen Gradle-Task durch und erzeugt einen Report\n- [ ] `./gradlew build` wird durch den Harness **nicht** verlangsamt (er ist ausgeschlossen)\n- [ ] Ein manipuliertes Korpus-Byte führt zum Abbruch mit klarer Fehlermeldung (Manifest-Prüfung)\n- [ ] Alle vier Metriken werden gesamt und je Kategorie, Schwierigkeit und Sprache ausgewiesen\n- [ ] Der Report enthält die Konfiguration des Laufs: Embedding-Modell und -Version, `chunkSize`, `topK`, Ähnlichkeitsschwelle, Korpus-Manifest-Summe, Golden-Dataset-Version\n- [ ] Zwei aufeinanderfolgende Läufe auf demselben Rechner liefern Metriken, die um weniger als 0,01 absolut voneinander abweichen (Nachweis der Stabilität; falls nicht erreichbar, ist die HNSW-Approximation als Ursache zu dokumentieren)\n- [ ] Der Report weist die schlechtesten 10 Anfragen namentlich aus — der Harness soll Debugging ermöglichen, nicht nur eine Note vergeben\n- [ ] Dokumentation aktualisiert: wie führt man ihn aus, wie liest man den Report\n\n## Technische Hinweise\n\n- Der pgvector-Indextyp ist in `application.yml` fest auf `hnsw` gesetzt (siehe #77). Approximative Suche kann Ranglisten leicht schwanken lassen. Falls das Stabilitätskriterium nicht erreichbar ist: Indextyp für die Evaluierung konfigurierbar machen oder exakte Suche verwenden — dann bitte im Issue vermerken.\n- Chunk-Metadaten enthalten heute nur `document_id`, `chunk_index` und `file_name`. `file_name` ist deshalb der Schlüssel für die Zuordnung; die Dateinamen aus #225 sind eindeutig.\n- Das Einbettungsmodell ist gemäß ADR-0008 Ollama, nicht OpenAI: kostenlos, ohne Secret, auch in Forks lauffähig und über die Modellversion reproduzierbar.\n\n## Betroffene Module\n\n`backend/` (Testquellen, Gradle-Build), `eval/`. Keine Änderung an der OpenAPI-Spezifikation, keine Änderung am Produktivcode erwartet.\n\n## Abhängigkeiten\n\nBlockiert durch #225 und #226.\n\n## Teil des Epics\n\n#224",
      "closes": []
    },
    {
      "number": 201,
      "title": "Knowledge library as the document container, with data migration",
      "url": "https://github.com/criew/opaa/issues/201",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "size:L"
      ],
      "body": "## Context\r\n\r\nDocuments currently have **no** owning container at all: `io.opaa.indexing.Document` has no workspace\r\nreference and the Liquibase changelogs contain no such column. The permission anchor is not being\r\nmoved here, it is being introduced for the first time.\r\n\r\nSee [`docs/features/spaces-and-assets.md`](docs/features/spaces-and-assets.md) and [ADR-0008](docs/decisions/0008-space-and-asset-model.md).\r\n\r\n## Scope\r\n\r\n- `KnowledgeLibrary` as the first asset type: id, name, description, owner (user or group),\r\n  organization, `visibility`, `listed`\r\n- `Document.libraryId` and `library_id` on the chunk metadata\r\n- Personal knowledge library \"My documents\" created together with the personal space\r\n- Library CRUD API and a documents endpoint at `/api/v1/libraries/{id}/documents`\r\n- Upload targets a library, not a space\r\n- **The relational database is the leading store**; the vector store is derived. Documented here and\r\n  in the specification, because it determines how backups are set up\r\n\r\n## Acceptance criteria\r\n\r\n- [ ] Every document belongs to exactly one knowledge library; the column is non-nullable\r\n- [ ] Every chunk carries `library_id` and `organization_id`\r\n- [ ] Existing documents are migrated into a system library **readable by system admins only**\r\n- [ ] Creating a user creates personal space and personal library atomically\r\n- [ ] Re-indexing keeps the library assignment\r\n- [ ] **Dry run with volume figures** before the `library_id` backfill; migration is resumable and\r\n      the rollback order is documented\r\n- [ ] Backend format, build and test pass\r\n\r\n## Depends on\r\n\r\n#199, #200 (for group-owned libraries)\r\n\r\nPart of #198",
      "closes": []
    },
    {
      "number": 200,
      "title": "Introduce groups as permission subjects",
      "url": "https://github.com/criew/opaa/issues/200",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "size:M",
        "auth"
      ],
      "body": "## Context\r\n\r\nPermission entries reference a **user or a group**. Groups are not a convenience feature: the\r\ndistribution level \"department\" is a grant to the department group, and there is deliberately no\r\ndepartment object. Without groups that level does not exist. Groups can also **own an asset**, which\r\nis what keeps centrally maintained libraries alive across staff changes.\r\n\r\nSee [`docs/features/spaces-and-assets.md`](docs/features/spaces-and-assets.md) and [ADR-0008](docs/decisions/0008-space-and-asset-model.md).\r\n\r\n## Scope\r\n\r\n- `Group` entity scoped to an organization, with members\r\n- `Group.kind`: `ORG_UNIT` (from the directory, carries a parent unit, can have curators) and\r\n  `AD_HOC` (created in the system, no curator, no distribution level)\r\n- Group management API and admin UI (create, rename, delete, add and remove members)\r\n- A permission subject abstraction resolving to user or group\r\n- Group membership resolution with caching and invalidation on change\r\n\r\n## Acceptance criteria\r\n\r\n- [ ] A group can be created, renamed, deleted and have members added and removed\r\n- [ ] Groups never cross an organization boundary\r\n- [ ] Resolving the groups of a user is cached and invalidated immediately on membership change\r\n- [ ] Deleting a group that owns assets is blocked until ownership is transferred\r\n- [ ] Membership is **not** inherited downward: a grant to \"Amt 5\" reaches exactly the people the\r\n      directory places in that group — no inheritance is invented\r\n- [ ] Backend and frontend format, lint, build and test pass\r\n\r\n## Notes\r\n\r\nDirectory synchronisation is #237 and changes only the origin of memberships, not the permission\r\nmodel. Responsibility, unlike membership, **is** inherited upward — see #208.\r\n\r\nPart of #198",
      "closes": []
    }
  ],
  "opened_issues": [
    {
      "number": 310,
      "title": "fix(api): GlobalExceptionHandler mappt ResponseStatusException und DataIntegrityViolationException nicht",
      "url": "https://github.com/criew/opaa/issues/310",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:S"
      ],
      "body": "## Kontext\n\n`io.opaa.api.GlobalExceptionHandler` behandelt `MethodArgumentNotValidException`, `HttpMessageNotReadableException`, `IllegalArgumentException`, `TransientAiException`, `NonTransientAiException`, `AccessDeniedException` — und alles Übrige über `@ExceptionHandler(Exception.class)` als HTTP 500.\n\nZwei häufige Ausnahmen fallen damit auf den Catch-all:\n\n- **`ResponseStatusException`** — der im Projekt durchgängig verwendete Weg, einen Statuscode zu setzen (`SpaceService`, `KnowledgeLibraryService`, `GroupService`). Der gesetzte Status wird nicht übernommen.\n- **`DataIntegrityViolationException`** — jede Verletzung eines Fremdschlüssels oder einer Unique-Constraint, die die Anwendung nicht selbst abfängt.\n\n## Warum das wichtig ist\n\nBeim Review von PR #309 fiel auf: Eine Gruppe, die einen Grant auf eine fremde Bibliothek hält, ließ sich nicht löschen — statt einer verständlichen Meldung kam „Interner Serverfehler\". Derselbe Effekt trat in PR #305 zweimal auf (Bibliothek mit Dokumenten, Gruppe mit eigener Bibliothek).\n\nJedes Mal wurde es an der Aufrufstelle mit einem gezielten Guard behoben, was richtig ist. Die Lücke dahinter bleibt aber bestehen: **Jeder künftige `RESTRICT`-Fremdschlüssel erzeugt denselben 500er**, bis jemand daran denkt. Das Rechtemodell führt laufend neue solche Beziehungen ein.\n\nEin 500er verschleiert zudem die Ursache. Ein Nutzer, der eine Gruppe nicht löschen kann, weil sie noch Rechte hält, soll das erfahren — nicht „Interner Serverfehler\" lesen.\n\n## Umsetzung\n\n- `ResponseStatusException` mit ihrem gesetzten Status und ihrer Meldung durchreichen\n- `DataIntegrityViolationException` auf **409 Conflict** mit einer verständlichen deutschen Meldung abbilden. Die technische Ursache (Constraint-Name) gehört ins Log, nicht in die Antwort\n- Prüfen, ob die Abbildung zu grob ist: Nicht jede Integritätsverletzung ist ein Konflikt im Sinne des Aufrufers. Falls nötig, nach Constraint-Art unterscheiden\n\nDie gezielten Guards an den Aufrufstellen bleiben bestehen — sie liefern die bessere Meldung. Diese Änderung ist das Sicherheitsnetz darunter.\n\n## Abnahmekriterien\n\n- [ ] `ResponseStatusException` behält Status und Meldung\n- [ ] Eine unbehandelte `DataIntegrityViolationException` ergibt 409 statt 500, mit verständlicher Meldung\n- [ ] Der Constraint-Name erscheint im Log, nicht in der Antwort\n- [ ] Tests für beide Fälle\n- [ ] Bestehende Guards und ihre Meldungen bleiben unverändert wirksam\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nVorbestehend, gefunden bei den Reviews zu PR #305 und PR #309.",
      "closes": []
    },
    {
      "number": 308,
      "title": "test(backend): GroupServiceIntegrationTest auf echtes Liquibase-Schema umstellen",
      "url": "https://github.com/criew/opaa/issues/308",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:S"
      ],
      "body": "## Kontext\n\n`GroupServiceIntegrationTest` läuft weiterhin mit `spring.liquibase.enabled=false` und `ddl-auto=create-drop`. In #288 wurden nur die Space-Suiten umgestellt.\n\nFolge, gefunden beim Review von PR #305: Der neue Test `cannotDeleteAGroupThatStillOwnsALibrary` sichert den Guard in `GroupService.deleteGroup` ab — kann aber den Fall, gegen den der Guard schützt, **strukturell nicht prüfen**. Ohne den Guard schlägt er mit `AssertionError: Expecting code to raise a throwable` fehl, nicht mit der erwarteten Fremdschlüsselverletzung, weil `fk_knowledge_libraries_owner_group_organization` im Hibernate-Schema gar nicht existiert.\n\nDer Guard selbst ist korrekt. Nur ist der Test kein Beleg dafür, dass der 500er behoben ist.\n\n## Warum jetzt\n\nDies ist derselbe blinde Fleck, der in #254 und #280 je eine Regression durchgelassen hat. #202 führt die Rechteprüfung samt Grants ein und wird weitere Fremdschlüssel auf `groups` anlegen — die wären in dieser Suite ebenfalls ungetestet.\n\n## Umsetzung\n\nUmstellung auf `spring.liquibase.enabled=true` und `ddl-auto=none`, `TestcontainersConfiguration` per `@Import`. Muster: `SpaceServiceIntegrationTest` und `SpaceRepositoryTest` (aus #288), sowie `UserServicePersonalSpaceIntegrationTest`.\n\nZu beachten: Bei geteiltem Kontext und echten Fremdschlüsseln greifen pauschale `deleteAll()`-Bereinigungen nicht mehr. In #305 wurde dafür gezielt nach eigenen IDs aufgeräumt statt pauschal zu löschen — das ist die sauberere Variante und sollte hier übernommen werden.\n\n## Abnahmekriterien\n\n- [ ] `GroupServiceIntegrationTest` läuft gegen das echte Liquibase-Schema\n- [ ] `cannotDeleteAGroupThatStillOwnsALibrary` schlägt ohne den Guard mit der **Fremdschlüsselverletzung** fehl, nicht mit einer fehlenden Exception — nachweisen\n- [ ] Keine Datenreste zwischen Testklassen im geteilten Kontext\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nFolge aus #288 und PR #305. Sollte vor #202 abgeschlossen sein.",
      "closes": []
    },
    {
      "number": 307,
      "title": "fix(auth): Gleichzeitige Erstanmeldungen verschiedener Nutzer erschöpfen den Connection-Pool",
      "url": "https://github.com/criew/opaa/issues/307",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:M",
        "auth"
      ],
      "body": "## Kontext\n\nBei **12 gleichzeitigen Erstanmeldungen verschiedener Nutzer** scheitern die Requests bei der Standard-Poolgröße von 10 nach 30 Sekunden mit `Connection is not available`.\n\nGemessen beim Review von PR #305, sowohl auf dem PR-Stand als auch auf dem Basis-Commit `aab3eda` — jeweils 3 von 3 Läufen rot, gleiche Signatur. **Vorbestehend, nicht durch #305 eingeführt.**\n\nAufschlussreich ist der Zustand während des Hängers: `pg_stat_activity` zeigt **keine einzige aktive Session**. Die Datenbank ist unbeschäftigt, alle Anwendungs-Threads warten auf eine Connection aus dem Pool.\n\n## Warum das wichtig ist\n\nDer Fall ist nicht konstruiert, sondern der Regelfall bei der Einführung: Eine Behörde schaltet OPAA frei, dreißig Beschäftigte melden sich am Montagmorgen zum ersten Mal an.\n\nSchlimmer als der Fehler selbst ist die Wirkung: **Der Login meldet Erfolg, während persönlicher Space und Bibliothek fehlen.** `UserService.ensureBothPersonalAssets` protokolliert Provisionierungsfehler bewusst, statt sie zu werfen (#294, PR #305) — richtig so, sonst wäre der Nutzer ausgesperrt. Die Folge ist aber ein stiller Defekt, der erst beim nächsten unbelasteten Login heilt und bis dahin niemandem auffällt.\n\nDas in PR #305 eingeführte Striped Lock hilft hier nicht: Es serialisiert je Nutzer und entlastet nur den Fall vieler gleichzeitiger Anmeldungen **desselben** Nutzers.\n\n## Zu klären\n\nDie Ursache ist noch nicht verstanden. Die Datenbank ist idle, die Threads warten trotzdem — es geht also um die Anzahl gleichzeitig gehaltener Connections je Request, nicht um Datenbanklast.\n\n- Wie viele Connections hält ein Login-Request gleichzeitig, und über welche Dauer? Zwei `ensure*`-Aufrufe mit je eigener Transaktion, dazu die Requestverarbeitung.\n- Reicht es, die Provisionierung aus dem Login-Request zu lösen — etwa asynchron nach der Antwort, oder beim ersten Zugriff, der sie tatsächlich braucht?\n- Ist die Standard-Poolgröße von 10 für den erwarteten Rollout überhaupt angemessen? Falls sie steigen soll, gehört das mit Begründung in `application.yml`, nicht in eine Testklasse (das wurde in PR #305 bereits einmal als untauglich verworfen).\n- Sollte ein fehlgeschlagener Provisionierungsversuch sichtbar werden, statt nur protokolliert? Das ist die in #294 offen gelassene Metrik-Frage — beide Issues hängen zusammen.\n\n## Abnahmekriterien\n\n- [ ] 12 gleichzeitige Erstanmeldungen verschiedener Nutzer laufen bei der konfigurierten Produktions-Poolgröße durch\n- [ ] Nach dem Lauf hat **jeder** Nutzer Space und persönliche Bibliothek — Datenbankzustand prüfen, nicht nur den Testausgang\n- [ ] Test mit echten Threads gegen Postgres mit Liquibase-Schema, mehrere Läufe (der Fehler tritt intermittierend auf)\n- [ ] Reproduktionsnachweis: Der Test schlägt ohne den Fix fehl\n- [ ] Ursache dokumentiert, nicht nur behoben\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nGefunden beim Review von PR #305. Hängt mit #294 zusammen (Sichtbarkeit fehlgeschlagener Provisionierung).",
      "closes": []
    },
    {
      "number": 306,
      "title": "eval(baseline): Fallzahlbasierte Regressionsprüfung für Paare mit Toleranz < 1/n",
      "url": "https://github.com/criew/opaa/issues/306",
      "author": "criew",
      "labels": [
        "size:M",
        "ci",
        "evaluation"
      ],
      "body": "## Kontext / Warum\n\nAus der zweiten Review-Runde zu PR #301 (Issue #228, ADR-0013, Abschnitt „Offen\"): Die Mittelwert-Toleranz (`BaselineComparator.toleranceFor`) schützt sechs Metrik/Gruppen-Paare nicht zuverlässig gegen einen einzelnen kippenden Fall, weil deren Toleranz enger ist als die Verschiebung, die ein einzelner Fall erzeugt (`1/n`):\n\n| Paar | Toleranz | 1/n | Verhältnis |\n|---|---|---|---|\n| `numeric_range` / `recallAt10` | 0,0150 | 0,0625 | 0,24 |\n| `numeric_range` / `ndcgAt10` | 0,0158 | 0,0625 | 0,25 |\n| `numeric_range` / `mrr` | 0,0253 | 0,0625 | 0,40 |\n| `multi_attribute_filter` / `ndcgAt10` | 0,0343 | 0,0476 | 0,72 |\n| `numeric_range` / `hitRateAt5` | 0,0470 | 0,0625 | 0,75 |\n| `multi_attribute_filter` / `recallAt10` | 0,0398 | 0,0476 | 0,83 |\n\nFür `numeric_range`s nDCG@10 genügt es bereits, dass eine einzige der 16 Anfragen von Rang 1 auf Rang 3 rutscht — kein verlorener Treffer nötig —, um die Toleranz mehr als doppelt zu reißen.\n\nDer Report führt in `allQueryResults` bereits jeden Einzelfall mit seinen vier Metriken. Eine fallzahlbasierte Prüfung („die Zahl der Fälle mit z. B. `ndcgAt10 > 0` darf um höchstens 1 sinken\") wäre exakt, bräuchte keine neue Kalibrierungsevidenz und wäre strenger als die bestehende 25-%-Deckelung — billiger als zunächst angenommen.\n\n## Ziel / Ergebnis\n\nDie sechs betroffenen Metrik/Gruppen-Paare sind gegen einen einzelnen falsch-positiven Fehlschlag ebenso robust wie die übrigen, ohne die Mittelwert-Toleranz für alle anderen Paare zu verändern.\n\n## Abnahmekriterien\n\n- [ ] Entschieden und in ADR-0013 (oder einem Nachtrag) begründet: fallzahlbasierte Prüfung ausschließlich für Paare mit `Toleranz < 1/n`, oder ein anderer, ebenso begründeter Ansatz\n- [ ] `BaselineComparator` (oder eine neue, dedizierte Klasse) implementiert die gewählte Prüfung unter Verwendung von `allQueryResults`\n- [ ] Unit-Test reproduziert das Szenario „ein Fall kippt in `numeric_range`\" und zeigt, dass es nicht mehr fälschlich fehlschlägt, während eine echte Verschlechterung (mehrere Fälle) weiterhin erkannt wird\n- [ ] `eval/baseline/README.md` und `BaselineComparator`-Javadoc aktualisiert\n\n## Umfang\n\n`backend/src/evalTest/java/io/opaa/eval/` (`BaselineComparator`, ggf. neue Klasse), `docs/decisions/0013-fehlerkriterium-retrieval-regression.md`, `eval/baseline/README.md`\n\n## Außerhalb des Umfangs\n\nDie Mittelwert-Toleranzformel für alle anderen (nicht betroffenen) Metrik/Gruppen-Paare bleibt unverändert.\n\n## Abhängigkeiten\n\nBaut auf #228 (Retrieval-Regressionsjob, gemergt) und ADR-0013 auf.",
      "closes": []
    },
    {
      "number": 304,
      "title": "eval(golden): category:crosslingual und language:de sind identische Fallmengen",
      "url": "https://github.com/criew/opaa/issues/304",
      "author": "criew",
      "labels": [
        "size:S",
        "evaluation"
      ],
      "body": "## Kontext / Warum\n\nAus dem Review zu PR #301 (Issue #228): Jeder \\`crosslingual\\`-Fall im Golden Dataset (\\`eval/golden/comic-characters.json\\`) ist konstruktionsbedingt eine deutsche Frage, und alle \\`crosslingual\\`-Fälle sind gleichzeitig alle \\`de\\`-Fälle. \\`category:crosslingual\\` und \\`language:de\\` sind damit exakt dieselbe Teilmenge (34 Fälle, 33 unterschiedliche Erwartungsmengen) — im Retrieval-Regressionsjob (#228) erzeugt das acht statt vier Prüfungen über identische Daten und suggeriert eine breitere Abdeckung, als tatsächlich besteht.\n\nHerkunft: \\`eval/generator/generate_golden_dataset.py\\` (Issue #226), nicht #228 selbst — siehe ADR-0013, Abschnitt „Offen\".\n\n## Ziel / Ergebnis\n\nEntschieden und dokumentiert, ob \\`category:crosslingual\\` und \\`language:de\\` als separate Baseline-Gruppen bestehen bleiben, obwohl sie identisch sind — oder ob der Generator künftig auch nicht-deutsche \\`crosslingual\\`-Fälle erzeugt (sodass die beiden Gruppen sich tatsächlich unterscheiden), oder ob eine der beiden Gruppen aus der Baseline entfernt wird.\n\n## Abnahmekriterien\n\n- [ ] Entscheidung dokumentiert (ADR-0013 oder \\`eval/golden/README.md\\`): getrennte Gruppen trotz Identität, Generator-Erweiterung, oder Konsolidierung\n- [ ] Falls Generator-Erweiterung: \\`eval/generator/generate_golden_dataset.py\\` erzeugt \\`crosslingual\\`-Fälle auch für mindestens eine weitere Sprache oder Domäne, \\`eval/baseline/comic-characters.json\\` neu gezogen\n- [ ] Falls Konsolidierung: eine der beiden Baseline-Gruppen entfernt, \\`BaselineComparator\\`/\\`eval/baseline/README.md\\` entsprechend angepasst\n\n## Umfang\n\n\\`eval/generator/\\`, \\`eval/golden/\\`, ggf. \\`eval/baseline/comic-characters.json\\`, \\`docs/decisions/0013-fehlerkriterium-retrieval-regression.md\\`\n\n## Außerhalb des Umfangs\n\nDer Retrieval-Regressionsjob selbst (#228) und der Harness (#227) — beide funktionieren unabhängig davon, ob diese beiden Gruppen identisch sind.\n\n## Abhängigkeiten\n\nKeine blockierenden Abhängigkeiten. Bezieht sich auf #226 (Golden-Dataset-Generator) und #228 (Regressionsjob, wo die Redundanz zuerst auffiel).",
      "closes": []
    },
    {
      "number": 302,
      "title": "docs(agents): Umgang mit Transaktionen in die Entwickler-Rollendefinition aufnehmen",
      "url": "https://github.com/criew/opaa/issues/302",
      "author": "criew",
      "labels": [
        "documentation",
        "size:S"
      ],
      "body": "## Kontext\n\nIn Stufe A von #198 hat dieselbe Konstruktion dreimal einen Fehler verursacht, jedes Mal fachlich falscher Code bei grünem CI, jedes Mal erst im Review gefunden:\n\n| PR | Konstruktion | Fehler |\n|---|---|---|\n| #280 | `REQUIRES_NEW` innerhalb einer offenen Transaktion | Eigene Connection sah die noch nicht committete `users`-Zeile nicht, `fk_spaces_owner` schlug fehl — **jede Erstanmeldung** war gebrochen |\n| #297 | `REQUIRES_NEW` für einen Statusschreiber | Committete vor der Hauptransaktion und meldete `APPLIED` für eine Anwendung, die zurückgerollt wurde |\n| #299 | `REQUIRES_NEW` neben einer haltenden äußeren Transaktion | Zwei Pool-Connections gleichzeitig je paralleler Anfrage; ab Pool-Größe Stillstand für 30 Sekunden, danach Fehler — auch für unbeteiligte Requests |\n\nDazu kam in #297 ein verwandter Fall: Eine Planberechnung unter `@Transactional(readOnly = true)` behauptete im Javadoc, „nichts wurde geschrieben\" sei strukturell garantiert — der `save()`-Aufruf stand im Code und wurde nur vom Flush-Modus geschluckt.\n\nIn allen Fällen war die Absicht richtig und die Nebenwirkung überraschend. Und in allen Fällen war die beste Lösung, die Konstruktion **ganz zu vermeiden**, statt sie zu reparieren.\n\n## Warum in die Rollendefinition und nicht in AGENTS.md\n\nEs ist Handwerkswissen des Entwicklers, keine Projektkonvention. Entscheidung des Maintainers.\n\n## Umsetzung\n\nEinen Abschnitt zu Transaktionen in `agents/roles/developer.md` aufnehmen, sinnvollerweise zwischen „Testschutz\" und „Umfang und Blocker\". Inhalt:\n\n- Vor dem Einsatz einer eigenen Transaktion (`REQUIRES_NEW`, `TransactionTemplate`) prüfen, ob sie wirklich gebraucht wird — sie ist in diesem Projekt dreimal zur Fehlerquelle geworden.\n- Wenn sie gebraucht wird, beide Fehlerrichtungen prüfen und im PR benennen: Was passiert bei Fehlschlag der inneren, was bei Fehlschlag der äußeren Transaktion.\n- Sichtbarkeit: Eine eigene Transaktion sieht nichts, was die umgebende noch nicht committet hat.\n- Commit-Reihenfolge: Eine innere Transaktion committet vor der äußeren. Was sie schreibt, überlebt deren Rollback.\n- Ressourcen: Eine innere Transaktion neben einer haltenden äußeren belegt zwei Connections gleichzeitig. Bei nebenläufigen Aufrufen skaliert das mit der Anzahl paralleler Anfragen.\n- `@Transactional(readOnly = true)` verhindert Schreibzugriffe nicht strukturell, sondern schluckt sie über den Flush-Modus. Wer „schreibt nichts\" zusichert, muss den Schreibpfad entfernen, nicht auf die Annotation vertrauen.\n- Prüfen, ob die umgebende Methode überhaupt `@Transactional` braucht — oft ist der einfachste Fix, sie wegzulassen.\n- Nach einer abgefangenen `DataIntegrityViolationException` ist die umgebende Transaktion unter PostgreSQL abgebrochen; ein Neulesen braucht einen eigenen Kontext.\n\nAuf Deutsch, im Stil der bestehenden Abschnitte: knappe Stichpunkte, keine Abhandlung. Die drei PR-Nummern als Beleg nennen, damit der Abschnitt nicht als abstrakte Vorsicht gelesen wird.\n\nPrüfen, ob die Client-Adapter (`.claude/agents/developer.md`, `.codex/agents/developer.toml`, `.opencode/agents/developer.md`) unverändert bleiben können — sie verweisen nur auf den gemeinsamen Vertrag und sollten nichts Inhaltliches enthalten.\n\n## Abnahmekriterien\n\n- [ ] `agents/roles/developer.md` enthält den Abschnitt\n- [ ] Die drei Fälle sind als Beleg genannt\n- [ ] Client-Adapter bleiben frei von Inhalt\n- [ ] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt",
      "closes": []
    },
    {
      "number": 300,
      "title": "fix(group): DirectorySyncStatusRecorder behandelt Race auf uk_directory_sync_status_organization nicht",
      "url": "https://github.com/criew/opaa/issues/300",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:S"
      ],
      "body": "## Kontext\n\n`DirectorySyncStatusRecorder.record` (`backend/src/main/java/io/opaa/group/sync/DirectorySyncStatusRecorder.java:39`) folgt dem Muster\n\n```java\nfindByOrganizationId(...).orElseGet(() -> new DirectorySyncStatus(organizationId))\n```\n\ngegen die Unique-Constraint `uk_directory_sync_status_organization`, ohne eine `DataIntegrityViolationException` zu behandeln. Zwischen Prüfung und Anlage liegt keine Absicherung.\n\nGemeldet beim Bearbeiten von #293, wo dasselbe Muster in `UserService.findOrCreateUser` behoben wurde.\n\n## Risiko\n\nGeringer als bei #293: Synchronisationsläufe werden je Organisation von einem Administrator angestoßen und laufen typischerweise nicht parallel. Der Fall tritt nur beim **allerersten** Lauf einer Organisation ein, und nur wenn zwei Läufe gleichzeitig starten.\n\nDie Nachprüfung zu PR #297 hat allerdings festgestellt, dass zwei gleichzeitige Läufe derzeit **nicht serialisiert** sind — es hindert nichts zwei Administratoren daran, `run` parallel auszulösen. Damit ist der Fall konstruierbar, wenn auch selten.\n\n## Umsetzung\n\nDasselbe Muster wie in `UserService.findOrCreateUser` (#293) und `SpaceService.ensurePersonalSpace` (#265): `DataIntegrityViolationException` fangen und neu lesen.\n\nZu beachten: `record` läuft in einem eigenen Transaktionskontext. Prüfe vor der Umsetzung die Historie von `REQUIRES_NEW` in diesem Projekt — die Konstruktion hat in #280 und #297 jeweils einen Fehler verursacht, beide Male durch CI gerutscht.\n\nAlternativ oder ergänzend zu erwägen: die in der Nachprüfung zu #297 vorgeschlagene Serialisierung gleichzeitiger Läufe je Organisation (Advisory-Lock). Sie würde diesen Fall mit erledigen und zugleich das dort dokumentierte Fenster zwischen Verzeichnisabruf und Anwendung schließen. Bewerte, was verhältnismäßig ist.\n\n## Abnahmekriterien\n\n- [ ] Zwei gleichzeitige Erstläufe derselben Organisation führen zu genau einer Statuszeile, ohne Fehler beim Aufrufer\n- [ ] Test mit echten Threads gegen Postgres mit Liquibase-Schema (`spring.liquibase.enabled=true`, `ddl-auto=none`)\n- [ ] Reproduktionsnachweis: Der Test schlägt ohne den Fix fehl, mit konkreter Fehlermeldung in der PR-Beschreibung\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nGefunden bei der Umsetzung von #293. Betrifft Code aus #237 / PR #297.",
      "closes": []
    }
  ],
  "merged_pull_requests": [
    {
      "number": 305,
      "title": "feat(library): Wissensbibliothek als Dokumentencontainer mit Migration",
      "url": "https://github.com/criew/opaa/pull/305",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "size:L"
      ],
      "body": "## Zusammenfassung\n\nFuehrt `KnowledgeLibrary` als ersten Asset-Typ ein (#201, Teil des Epics #198): eine Wissensbibliothek hat einen eigenen Eigentuemer (Nutzer oder Gruppe), gehoert zu genau einer Organisation, traegt Sichtbarkeitsstufe (`PRIVATE`/`SHARED`/`ORGANIZATION`) und Auffindbarkeits-Flag - unabhaengig vom Space. Jedes Dokument gehoert ab jetzt zu genau einer Bibliothek (`library_id`, nicht mehr nullbar); jeder Chunk traegt zusaetzlich `organization_id`, die Filterachse, die #202s rechtebewusste Vektorsuche direkt braucht.\n\n**Migration 012** (`db.changelog-master.yaml`): Tabelle `knowledge_libraries`, Seed der einen System-Bibliothek (nur fuer System-Admins lesbar, fail-closed), Backfill der bestehenden Dokumente als ein einzelnes `UPDATE` (nicht batched - siehe Begleitdokument fuer die Begruendung, das eine fruehere Batch-Fassung als fehlerhaft korrigiert), danach `NOT NULL` + zusammengesetzter Fremdschluessel gegen `knowledge_libraries(id, organization_id)`. Begleitdokument mit Trockenlauf-Anleitung, Mengengeruest und Rollback-Reihenfolge: `docs/migrations/012-knowledge-library.md`.\n\n**Owner-Modell:** Zwei separate Fremdschluessel-Spalten (`owner_user_id`, `owner_group_id`) statt einer polymorphen `owner_id` - beide tragen dadurch einen echten, von der Datenbank durchgesetzten Fremdschluessel.\n\n**Nutzeranlage:** Ein neuer Nutzer bekommt automatisch einen persoenlichen Space *und* eine persoenliche Bibliothek \"Meine Dokumente\". Beide Provisionierungsaufrufe fuer denselben Nutzer werden ueber ein prozesslokales, gestriptes Lock serialisiert (reine Performance-Massnahme gegen Datenbank-Contention bei vielen gleichzeitigen Erstanmeldungen desselben Nutzers), laufen jeweils als einzelnes `INSERT ... ON CONFLICT ... DO NOTHING` (ein Roundtrip statt bis zu zwei), und ein Fehlschlag wird protokolliert statt den Login-Request scheitern zu lassen - damit ein Provisionierungsfehler nicht zur Aussperrung wird. Die Korrektheit traegt weiterhin ausschliesslich der jeweilige partielle Unique-Index in der Datenbank.\n\n**Loeschschutz:** Eine Bibliothek mit Dokumenten und eine Gruppe mit Bibliotheken lassen sich nicht loeschen (409 statt eines unbehandelten 500).\n\n## Zugehoerige Issues\n\nCloses #201\n\n## Art der Aenderung\n\n- [x] Neues Feature\n\n## Checkliste\n\n- [x] Tests bestehen lokal (Backend: 317 Tests, 0 fehlgeschlagen, 1 vorbestehender unabhaengiger Skip; Frontend: 140 Tests, 0 fehlgeschlagen; `UserServiceCreationRaceIntegrationTest` zusaetzlich 9x in Folge gruen bei unveraendertem Produktions-Standardpool von 10)\n- [ ] Bei Bugfix: Reproduktionsnachweis erbracht - entfaellt, kein Bugfix\n- [x] Dokumentation aktualisiert (`docs/migrations/012-knowledge-library.md`, `docs/features/spaces-and-assets.md`)\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet - wird im ersten PR-Kommentar nachgereicht\n\n## KI-Agenten-Offenlegung\n\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude, Entwickler-Subagent)\n\n## Annahmen\n\n- **\"Atomisch\"** bei der gemeinsamen Anlage von persoenlichem Space und persoenlicher Bibliothek ist nicht als eine gemeinsame Datenbanktransaktion umgesetzt, sondern als \"immer gemeinsam versucht, unabhaengige Fehlergrenzen, Fehlschlag wird protokolliert statt den Login zu blockieren\" - jede der beiden Operationen behaelt ihren eigenen Race-Schutz ueber ihren jeweiligen partiellen Unique-Index. In `UserService#ensureBothPersonalAssets` begruendet.\n- **Bewusster Zwischenzustand (mit dem Maintainer abgestimmt):** Sowohl Datei-/URL-Crawl als auch ein spaeterer manueller Upload-Pfad weisen Dokumente ausschliesslich der System-Bibliothek zu - fuer normale Nutzer nicht erreichbar, nur fuer System-Admins. Die Zielauswahl kommt mit #202 (manueller Upload) bzw. #207 (Konnektor-Quellen).\n\n## Review-Historie dieses PRs\n\nEin erster Review-Durchgang fand, dass ein testseitiger Connection-Pool-Override eine echte Regression kaschierte statt sie zu beheben (gemessen: 5 von 9 Laeufen schlugen bei Produktions-Poolgroesse fehl, und im Fehlerfall meldete der Login trotzdem Erfolg, obwohl die persoenliche Bibliothek fehlte). Der Override wurde entfernt; die tatsaechliche Ursache (Datenbank-Contention bei gleichzeitigen Inserts gegen denselben partiellen Unique-Index, verdoppelt durch die zweite Provisionierungsstufe) ist behoben durch: (1) ein einzelnes `INSERT ... ON CONFLICT ... DO NOTHING` statt des bisherigen insert-dann-Exception-fangen-dann-erneut-lesen-Musters fuer sowohl den persoenlichen Space als auch die persoenliche Bibliothek, und (2) ein prozesslokales Lock je Nutzer, das die Datenbank-Contention bei vielen gleichzeitigen Erstanmeldungen desselben Nutzers auf einen einzigen tatsaechlichen Datenbankzugriff reduziert. Nachweis: 9 aufeinanderfolgende gruene Laeufe bei unveraendertem Produktions-Standardpool von 10.",
      "closes": [
        201
      ],
      "additions": 3776,
      "deletions": 112,
      "changed_files": 33
    },
    {
      "number": 303,
      "title": "docs(agents): Umgang mit Transaktionen in die Entwicklerrolle aufnehmen",
      "url": "https://github.com/criew/opaa/pull/303",
      "author": "criew",
      "labels": [
        "documentation",
        "size:S"
      ],
      "body": "## Zusammenfassung\n\nErgänzt `agents/roles/developer.md` um einen Abschnitt zu Transaktionen.\n\nAnlass sind drei Fehler aus Stufe A von #198, die dieselbe Wurzel haben — jedes Mal fachlich falscher Code bei grünem CI, jedes Mal erst im Review gefunden:\n\n| PR | Fehler |\n|---|---|\n| #280 | `REQUIRES_NEW` innerhalb einer offenen Transaktion sah die noch nicht committete `users`-Zeile nicht — **jede Erstanmeldung** war gebrochen |\n| #297 | Ein Statusschreiber committete vor der Hauptransaktion und meldete `APPLIED` für eine Anwendung, die zurückgerollt wurde |\n| #299 | Zwei Pool-Connections gleichzeitig je paralleler Anfrage; ab Pool-Größe Stillstand für 30 Sekunden — auch für unbeteiligte Requests |\n\nDazu ein verwandter Fall aus #297: Eine Planberechnung unter `@Transactional(readOnly = true)` sicherte im Javadoc zu, „nichts wurde geschrieben\" sei strukturell garantiert — der `save()`-Aufruf stand im Code und wurde nur vom Flush-Modus geschluckt.\n\nIn allen Fällen war die Absicht richtig und die Nebenwirkung überraschend. Und in allen Fällen war die beste Lösung, die Konstruktion ganz zu vermeiden statt sie zu reparieren — deshalb steht die Frage, ob die umgebende Methode überhaupt `@Transactional` braucht, jetzt an erster Stelle.\n\nDie Client-Adapter (`.claude/`, `.codex/`, `.opencode/`) bleiben unverändert: Sie verweisen ausschließlich auf den gemeinsamen Vertrag und enthalten nichts Inhaltliches — geprüft.\n\n## Zugehörige Issues\n\nCloses #302\n\n## Art der Änderung\n\n- [x] Dokumentation\n\n## Checkliste\n\n- [x] Abschnitt in `agents/roles/developer.md`, im Stil der bestehenden Abschnitte\n- [x] Die drei Fälle als Beleg genannt, damit der Abschnitt nicht als abstrakte Vorsicht gelesen wird\n- [x] Client-Adapter geprüft, bleiben frei von Inhalt\n- [x] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt\n\n## KI-Agenten-Offenlegung\n\nErstellt von Claude Opus 5 als Orchestrator, auf Weisung des Maintainers.",
      "closes": [
        302
      ],
      "additions": 13,
      "deletions": 0,
      "changed_files": 1
    },
    {
      "number": 301,
      "title": "ci(eval): Retrieval-Regressionsjob mit Baseline und Schwellenwerten",
      "url": "https://github.com/criew/opaa/pull/301",
      "author": "criew",
      "labels": [
        "enhancement",
        "ci",
        "evaluation"
      ],
      "body": "## Zusammenfassung\n\nFührt den in #227 gebauten Retrieval-Harness als automatisierten CI-Job gegen eine committete Baseline (`eval/baseline/comic-characters.json`). Der Job vergleicht jeden Lauf über `io.opaa.eval.BaselineComparator` mit der Baseline und trennt dabei zwei Fragen bewusst voneinander:\n\n1. **Ist die Baseline überhaupt noch gültig?** Fixpunkte (Korpus-Manifest, Golden-Dataset-Hash, Embedding-Modell-Digest, Chunk-Größe, Messvertrag-Version) werden exakt verglichen. Weicht auch nur einer ab, meldet der Job **\"Baseline ungültig, Messgrundlage geändert\"** und vergleicht keine einzige Metrik — das ist ausdrücklich keine Aussage über eine Retrieval-Regression.\n2. **Nur wenn die Baseline gültig ist:** Jede Gruppe (gesamt, je Kategorie/Schwierigkeit/Sprache) wird gegen eine gruppenspezifische Toleranz geprüft, die vier Gesamtmetriken zusätzlich gegen eine baseline-unabhängige harte Untergrenze.\n\nDie Ein-Chunk-Invariante (ADR-0010) bleibt ein harter, toleranzfreier Fehlschlag.\n\n### Toleranzformel je Gruppe und Metrik\n\n```\ntoleranz = min( max(0.12 · Baselinewert, 1 / n, 0.02), 0.05 )\n```\n\nBegründung (ausführlich in `eval/baseline/README.md`):\n\n- Eine einzelne absolute Toleranz taugt nicht: 0,05 ist bei `attribute_lookup` (nDCG@10 0,942) Rauschen, bei `numeric_range` (nDCG@10 0,063) fast der gesamte Messwert.\n- Eine einzelne relative Toleranz taugt ebenfalls nicht: Bei Werten nahe null kollabiert sie gegen null.\n- Der `1/n`-Term sorgt dafür, dass **kleine** Gruppen (`numeric_range`, n=16) eine **größere**, nicht kleinere Toleranz bekommen — ein einzelner kippender Fall verschiebt eine 16er-Gruppe um bis zu 0,0625, eine 121er-Gruppe nur um 0,008.\n- Absolute Unter- (0,02) und Obergrenze (0,05) verhindern Extreme an beiden Enden.\n- So eng gewählt, **weil** drei unabhängige Läufe (zwei des Autors, einer des Reviewers auf fremder Hardware) byte-identische Metriken lieferten — die in ADR-0011 genannte HNSW-Approximationsschwankung tritt bei dieser Korpusgröße nicht auf. Eine weitere Toleranz würde bei nachgewiesener Stabilität keinen zusätzlichen Schutz kaufen, sondern nur echte Regressionen durchlassen.\n\nZusätzlich eine von der Baseline unabhängige harte Untergrenze für die vier **Gesamt**-Metriken (Hit@5 ≥ 0,30, MRR ≥ 0,25, nDCG@10 ≥ 0,25, Recall@10 ≥ 0,25) als zweites Fangnetz gegen katastrophales Versagen (z. B. leerer Vektor-Store), unabhängig von der Baseline-Datei selbst.\n\n### Baseline\n\n`eval/baseline/comic-characters.json`, gemessen am 2026-08-03 mit dem gepinnten Modell `nomic-embed-text:v1.5` (Digest `0a109f42…`), Laufzeit 1004 s, Gesamt-nDCG@10 0,445. Ein früherer Bericht nannte 0,463 — der stammte vom ungepinnten `:latest` und ist hinfällig; die Verschiebung durch das Pinning ist der empirische Beleg, warum das Pinning nötig war (siehe `eval/baseline/README.md`).\n\n### Betrieb\n\n- **Auslöser:** nächtlich auf `main` (Cron), `workflow_dispatch`, und Label `evaluation` an einem PR — bewusst **nicht** bei jedem PR (ADR-0011, Entscheidung 5).\n- **Modell-Cache:** `actions/cache` rundet den Docker-Volume-Inhalt (`opaa-eval-ollama-models`) als Tar-Archiv über ephemere Runner hinweg — der Cache-Schlüssel ist an den gepinnten Modell-Digest gebunden, ein bewusster Re-Pin invalidiert ihn automatisch. Ohne das würde jeder nächtliche Lauf ~275 MB neu von `ollama.com` ziehen und gegen das netzfreie Betriebsversprechen aus ADR-0011 verstoßen.\n- **Fehlschlag-Sichtbarkeit:** Bei Label-Auslösung wird die Delta-Tabelle als PR-Kommentar gepostet. Beim nächtlichen/manuellen Lauf legt ein Fehlschlag ein GitHub-Issue (Label `evaluation`, `bug`) an bzw. aktualisiert ein bestehendes; ein erneutes Bestehen schließt es automatisch mit Kommentar.\n- **Baseline-Aktualisierung:** dokumentiert in `eval/baseline/README.md` — wer (wer auch immer die verursachende Änderung einreicht, im selben PR), womit begründet (ein tatsächlicher `evaluateRetrieval`-Lauf, Verlinkung/Anhang des Reports, explizite Begründung bei Verschlechterung).\n\n### Verifikation ohne Docker\n\nDa der reale Lauf Docker/Testcontainers braucht (~17 Min), wurde die Vergleichslogik lokal gegen synthetische Reports verifiziert (`checkRetrievalBaseline` mit temporär entferntem `dependsOn(evaluateRetrieval)`):\n\n- Report == Baseline → **PASSED**, Delta-Tabelle zeigt `+0.000` überall.\n- Künstliche Verschlechterung (`overall.hitRateAt5=0.30`, `ndcgAt10=0.20`) → **FAILED** mit `overall hitRateAt5 Baseline=0.521 Ist=0.300 Delta=-0.221 Toleranz=0.050 [TOLERANZ VERLETZT]` und zusätzlich `[UNTERGRENZE VERLETZT]` für nDCG.\n- Manipulierter `corpusManifestSha256` → **FAILED** mit separater \"Baseline ungültig\"-Meldung samt Feld-Diff-Tabelle, keine Metrik verglichen.\n- Künstliche Verbesserung → **PASSED** mit Hinweis, die Baseline-Aktualisierung zu prüfen.\n\n## Zugehörige Issues\n\nCloses #228\n\n## Art der Änderung\n\n- [ ] Bugfix\n- [x] Neues Feature\n- [x] Dokumentation\n- [ ] Refactoring\n- [x] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal (`./gradlew spotlessCheck build`, `evalUnitTest` inkl. neuer `BaselineComparatorTest`; Frontend unverändert, `format:check`/`lint`/`test`/`build` zur Sicherheit trotzdem grün)\n- [ ] Bei Bugfix: Reproduktionsnachweis erbracht\n- [x] Dokumentation aktualisiert (`eval/README.md`, `eval/baseline/README.md`, `CONTRIBUTING.md`)\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [x] CLA unterzeichnet (bereits vom Maintainer/Betreiber unterzeichnet)\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code / QA Engineer, Sonnet 5)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
      "closes": [
        228
      ],
      "additions": 1994,
      "deletions": 14,
      "changed_files": 16
    },
    {
      "number": 299,
      "title": "fix(auth): Race bei paralleler Erstanmeldung auf uq_users_subject_issuer behoben",
      "url": "https://github.com/criew/opaa/pull/299",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "auth"
      ],
      "body": "## Zusammenfassung\n\n`UserService.findOrCreateUser` prüfte auf einen vorhandenen Nutzer und legte ihn andernfalls an, ohne die Unique-Constraint `uq_users_subject_issuer` zu behandeln. Bei der allerersten Anmeldung eines Nutzers laufen mehrere Requests parallel (`UserProvisioningFilter` bei jedem authentifizierten Request, mehrere Aufrufe der SPA direkt nach dem Login) — mehrere davon scheiterten mit einer 500er-Antwort auf `duplicate key value violates unique constraint \"uq_users_subject_issuer\"`.\n\n**Ansatz:** Dasselbe Muster wie `SpaceService.ensurePersonalSpace` (#265/#280/#287): Der Insert des neuen Nutzers läuft jetzt in einer eigenen `REQUIRES_NEW`-Transaktion, auf einer eigenen Connection. `findOrCreateUser` ist selbst `@Transactional` — auf Postgres bricht ein fehlgeschlagenes Statement die gesamte umgebende Transaktion ab, ein Catch innerhalb derselben Transaktion/Connection hätte also auch das anschließende Neulesen des Gewinner-Datensatzes unmöglich gemacht. Schlägt die `REQUIRES_NEW`-Transaktion mit `DataIntegrityViolationException` fehl, bleibt die aufrufende Transaktion unberührt, und der Verlierer liest den inzwischen committeten Nutzer einfach neu ein, statt den Fehler durchzureichen.\n\n**Warum kein `TransactionSynchronization#afterCommit` nötig ist** (anders als bei `ensurePersonalSpace`, siehe #280/#287): Der `REQUIRES_NEW`-Insert hier hängt von nichts ab, das die aufrufende Transaktion zuvor geschrieben hat — er benötigt nur, dass sein eigener Datensatz eindeutig ist, was Postgres unabhängig vom Zustand anderer Connections durchsetzt. Es gibt also kein Sichtbarkeitsproblem wie bei `fk_spaces_owner`, das eine Verzögerung auf den Commit-Zeitpunkt erfordern würde.\n\n**Zu `REQUIRES_NEW`, da dieses Muster im Projekt schon zweimal Ärger gemacht hat:**\n- Scheitert die innere `REQUIRES_NEW`-Transaktion (Konstellationsverlierer): Die äußere Transaktion (`findOrCreateUser`) ist unberührt, da sie nie selbst das fehlschlagende Statement ausgeführt hat — sie liest anschließend einfach den committeten Gewinner-Datensatz.\n- Scheitert die äußere Transaktion später (z. B. beim Registrieren des `afterCommit`-Hooks für `ensurePersonalSpace` oder danach): Der bereits committete Nutzer-Datensatz aus der `REQUIRES_NEW`-Transaktion bleibt bestehen — das ist beabsichtigt und identisch zum bereits etablierten Verhalten von `ensurePersonalSpace`, nicht neu eingeführt durch diesen PR.\n\n## Reproduktionsnachweis\n\nVor dem Fix (nur `createOrFetchUser` temporär auf direkten Insert ohne Race-Behandlung zurückgesetzt, `UserServiceCreationRaceIntegrationTest` isoliert ausgeführt):\n\n```\nUserServiceCreationRaceIntegrationTest > concurrentFirstLoginsOfTheSameSubjectCreateExactlyOneUser() FAILED\n    java.util.concurrent.ExecutionException\n        Caused by: org.springframework.dao.DataIntegrityViolationException\n            ... could not execute statement [ERROR: duplicate key value violates unique constraint \"uq_users_subject_issuer\"\n  Detail: Key (subject, issuer)=(e348b76b-c514-4255-9a05-638b3a879adc, test-issuer) already exists.]\n```\n\nMit dem Fix wiederhergestellt: `concurrentFirstLoginsOfTheSameSubjectCreateExactlyOneUser()` bestanden (4 parallele echte Threads, echtes Postgres mit Liquibase-Schema, `spring.liquibase.enabled=true`, `ddl-auto=none`).\n\n## Änderungen\n\n- `UserService.findOrCreateUser` teilt sich jetzt in `updateExistingUser` (unverändert) und `createOrFetchUser` (neu, mit Race-Behandlung via `REQUIRES_NEW` + Catch + Neu-Lesen)\n- `UserServiceCreationRaceIntegrationTest` (neu): 4 parallele echte Threads gegen echtes Postgres/Liquibase, Muster `UserServicePersonalSpaceIntegrationTest`\n- `UserServiceTest`: umgestellt von `@InjectMocks`/`MockitoExtension` auf manuelle Konstruktion (analog `SpaceServiceTest`), da der Konstruktor jetzt einen `PlatformTransactionManager` erwartet; zwei neue Fälle für Race-Gewinn-Lesen und unabhängige Constraint-Verletzungen\n\n## Weitere Fundstellen desselben Musters (nicht behoben, zur Information)\n\n- `DirectorySyncStatusRecorder.record` (`io.opaa.group.sync`): `statusRepository.findByOrganizationId(...).orElseGet(() -> new DirectorySyncStatus(organizationId))` gegen die Unique-Constraint `uk_directory_sync_status_organization`, ohne Behandlung einer `DataIntegrityViolationException`. Vermutlich geringes Risiko, da Sync-Läufe pro Organisation nicht typischerweise parallel angestoßen werden — aber dasselbe Check-then-Create-Muster wie hier.\n\n## Zugehörige Issues\n\nCloses #293\n\n## Art der Änderung\n\n- [x] Bugfix\n- [ ] Neues Feature\n- [ ] Dokumentation\n- [ ] Refactoring\n- [ ] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal\n- [x] Bei Bugfix: Reproduktionsnachweis erbracht — der Test schlägt auf dem fehlerhaften Stand fehl und besteht mit dem Fix (rote Fehlermeldung oben eingefügt)\n- [ ] Dokumentation aktualisiert (falls zutreffend) — nicht erforderlich\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet) — bereits unterzeichnet\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code, Developer-Rolle)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
      "closes": [
        293
      ],
      "additions": 282,
      "deletions": 50,
      "changed_files": 3
    },
    {
      "number": 298,
      "title": "test(backend): FK-abhängige Space-Tests auf echtes Liquibase-Schema umstellen",
      "url": "https://github.com/criew/opaa/pull/298",
      "author": "criew",
      "labels": [
        "bug",
        "backend",
        "size:M"
      ],
      "body": "## Zusammenfassung\n\nStellt die Fremdschlüssel-abhängigen Space-Integrationstests (`SpaceServiceIntegrationTest`, `SpaceRepositoryTest`) von Hibernate-generiertem Schema (`spring.liquibase.enabled=false`, `ddl-auto=create-drop`) auf das echte, versionierte Liquibase-Schema um (`spring.liquibase.enabled=true`, `ddl-auto=none`), nach dem in `UserServicePersonalSpaceIntegrationTest` (#287) etablierten Muster. `Space.ownerId`, `SpaceMembership.userId` und alle `organizationId`-Felder sind schlichte `UUID`-Spalten ohne `@ManyToOne` — Hibernate legt dafür keine Fremdschlüssel an, Liquibase schon (`fk_spaces_owner`, `fk_space_memberships_user`, `fk_spaces_organization`, `fk_space_memberships_organization`). Jeder FK-abhängige Fall war in diesen Suiten bisher ungetestet und genau das war die Wurzel der Regressionen in PR #254 und #280.\n\n**Update nach Review:** Beide Blocker und alle vier Nits aus dem Review sind behoben, siehe Kommentar unten für die Details je Befund.\n\n### Entscheidung: welche Suiten umgestellt wurden und welche nicht\n\n| Suite | Umgestellt? | Begründung |\n|---|---|---|\n| `SpaceServiceIntegrationTest` | Ja | Erzeugt Spaces/Memberships mit echten Owner-/Member-/Organisations-Bezügen über `SpaceService` — genau der FK-abhängige Fall |\n| `SpaceRepositoryTest` | Ja | Gleicher Grund, auf Repository-Ebene |\n| `UserServicePersonalSpaceIntegrationTest` (#287) | War bereits umgestellt | Als Muster übernommen; Container-Definition jetzt über die gemeinsame `TestcontainersConfiguration` statt einer eigenen Kopie |\n| `SpaceServiceTest` | Bewusst **nicht** umgestellt | Reine Unit-Logik gegen gemockte Repositories (`@Mock`), keine reale Datenbankverbindung, kein Fremdschlüsselbezug — hier bringt Liquibase keinen Mehrwert, nur Laufzeit |\n| `QueryIntegrationTest`, `DocumentIndexingIntegrationTest`, `ProviderConfigurationTest`, `MixedProviderConfigurationTest`, `OpenAiIntegrationTest` | **Bereits auf dem Liquibase-Schema, kein Handlungsbedarf** | Keine dieser Klassen setzt `spring.liquibase.enabled=false` — sie nutzen die Anwendungsdefaults (`application.yml`: `ddl-auto: none`, `liquibase.enabled: true`) und liefen von Anfang an gegen das volle Liquibase-Schema. Es gab hier nichts umzustellen. |\n| `Migration008RenameWorkspaceToSpaceTest`, `Migration010SpaceUniquenessTest` | Unverändert (liefen bereits gegen echtes Liquibase per Definition) | Wenden Changelogs direkt per JDBC an, kein Hibernate im Spiel |\n\n`TestcontainersConfiguration` (`io.opaa`) ist jetzt `public` statt package-private und wird von allen drei betroffenen Klassen per `@Import` geteilt, statt den Container-Bean ein drittes Mal zu duplizieren (Hinweis aus dem Review von PR #287).\n\n### Nachweis der Wirksamkeit (Abnahmekriterium)\n\n**Ergänzt nach Review:** Der ursprüngliche Nachweis (Revert des #280-Fixes) reproduzierte #287, nicht #288 — `UserServicePersonalSpaceIntegrationTest` lief schon vor diesem PR gegen das Liquibase-Schema und wird hier nicht umgestellt. Der eigentliche Nachweis für die tatsächlich umgestellten Klassen steht jetzt in `SpaceRepositoryTest` selbst, als zwei permanente Regressionswächter:\n\n- `savingASpaceWithANonExistentOwnerFailsInsteadOfSilentlyPersisting()`\n- `savingASpaceWithANonExistentOrganizationFailsInsteadOfSilentlyPersisting()`\n\nBeide rufen `spaceRepository.saveAndFlush(space)` auf — Produktivcode (`SpaceRepository` wird von `SpaceService` und jedem zukünftigen Aufrufer verwendet) — mit einer `ownerId` bzw. `organizationId`, die nicht in der Datenbank existiert.\n\n**Rot** (`SpaceRepositoryTest` testweise mit den alten Properties `spring.liquibase.enabled=false`, `ddl-auto=create-drop`, nur die beiden neuen Tests):\n```\nSpaceRepositoryTest > savingASpaceWithANonExistentOwnerFailsInsteadOfSilentlyPersisting() FAILED\n    java.lang.AssertionError: Expecting code to raise a throwable.\n\nSpaceRepositoryTest > savingASpaceWithANonExistentOrganizationFailsInsteadOfSilentlyPersisting() FAILED\n    java.lang.AssertionError: Expecting code to raise a throwable.\n\n2 tests completed, 2 failed\n```\nDer Insert eines Space mit nicht existierendem Owner/Organisation gelang unter dem alten Schema lautlos — exakt die Lücke, die #288 schließt.\n\n**Grün** (mit dem in diesem PR eingeführten Liquibase-Schema, den Properties aus `main`):\n```\nBUILD SUCCESSFUL in 27s\n```\nBeide Tests werfen jetzt `DataIntegrityViolationException` mit `fk_spaces_owner` bzw. `fk_spaces_organization` in der Meldung.\n\nDer ursprüngliche #280-Nachweis über `UserServicePersonalSpaceIntegrationTest` bleibt als Beleg dafür im PR, dass das etablierte Muster (#287) grundsätzlich wirkt — er ist nur nicht der Wirksamkeitsnachweis *dieses* PRs.\n\nZusätzlich beim Umstellen selbst ein reales Cross-Test-Problem gefunden und behoben: Der erste Entwurf von `SpaceServiceIntegrationTest#cleanUp()` rief `organizationRepository.deleteAll()` auf und löschte damit versehentlich die von Liquibase einmalig geseedete Standard-Organisation (`Organization.DEFAULT_ID`), von der `UserServicePersonalSpaceIntegrationTest` abhängt, sobald beide Klassen denselben Spring-Kontext teilen. Der volle Testlauf (`./gradlew test`) deckte das sofort auf (3 Fehlschläge mit `fk_users_organization`-Verletzung); behoben, indem jeder Test nur seine eigenen, frisch angelegten Organisationen anlegt statt alle zu löschen.\n\n### Aufräumen der selbst angelegten Organisationen (Nit 4)\n\n`SpaceServiceIntegrationTest` und `SpaceRepositoryTest` löschen ihre je Testmethode angelegten Organisationen jetzt in `@AfterEach` per ID (nach den davon abhängigen Users/Spaces/Memberships) — `Organization.DEFAULT_ID` bleibt unangetastet. Vorher sammelten sich pro vollem Lauf ~50 Restzeilen in der geteilten Datenbank an; das ist behoben.\n\n### Teardown-Muster für Migrationstests vereinheitlicht (Nit 5)\n\n`backend/src/test/java/io/opaa/migration/package-info.java` (neu) macht `connection.setAutoCommit(true)` nach jedem `Liquibase.update(...)` **unbedingt** verbindlich für dieses Package (nicht nur „falls danach noch etwas gebraucht wird\") und dokumentiert die Begründung — das im Review von #283 empfohlene Muster, weil es die Ursache (Liquibase lässt `autoCommit=false` zurück) behebt statt nur das Symptom in `tearDown()` zu kaschieren. `Migration008RenameWorkspaceToSpaceTest` ist als dokumentierte Altlast (vor #283 entstanden) benannt, nicht als zweite gültige Alternative. Adressiert an #201, #202 und #238.\n\n### Laufzeit\n\n- Beide konvertierten Klassen isoliert: **27–32s** (neu, gemeinsamer Kontext) vs. **62s** (alt, zwei separate `@DynamicPropertySource`-Container ohne `@ServiceConnection`-Caching) — die Umstellung ist hier sogar schneller, weil der Kontext jetzt mit `UserServicePersonalSpaceIntegrationTest` geteilt wird.\n- Voller Backend-Build (`./gradlew build`, alle 231+2 Tests): **~2m 30s**, wiederholt stabil bei 2m–2m15s (231–233 bestanden, 1 übersprungen ohne Docker).\n- **Korrektur (Nit 6):** `OpaaApplicationTests` teilt den Kontext entgegen der ursprünglichen PR-Beschreibung *nicht* — ihr fehlen `@ActiveProfiles`/`@TestPropertySource`, sie hat damit einen eigenen Cache-Key und startet einen eigenen Container. Der geteilte Kontext umfasst `SpaceServiceIntegrationTest`, `SpaceRepositoryTest` und `UserServicePersonalSpaceIntegrationTest`. Am Laufzeit-Fazit ändert das nichts.\n- Eine isolierte Baseline-Messung auf dem alten Stand (`git stash`) lieferte in dieser gemeinsam genutzten Umgebung (mehrere parallele Agent-Worktrees, gleicher Docker-Daemon) stark schwankende Werte (9m45s mit 14 infrastrukturbedingten Fehlschlägen durch Verbindungsabbrüche) — kein belastbarer Vergleichswert unter Last.\n- Bewertung: Kein spürbarer Laufzeit-Nachteil. Ein über mehrere Suiten geteilter Testcontainer lohnt sich nicht zusätzlich — er existiert durch `@ServiceConnection` + `@Import(TestcontainersConfiguration.class)` bereits: Spring cached identische Kontexte automatisch.\n\n## Was #201, #202 und #238 wissen müssen\n\n- **Muster für FK-abhängige Service-/Repository-Tests:** `@SpringBootTest(webEnvironment = RANDOM_PORT)` + `@Import(TestcontainersConfiguration.class)` + `@ActiveProfiles({\"local\", \"basic\"})` + `@TestPropertySource(properties = \"OPAA_AUTH_BASIC_SECRET=...\")` + `@Testcontainers(disabledWithoutDocker = true)`. Kein `@TestPropertySource` mehr für Liquibase/ddl-auto nötig — das sind bereits die Anwendungsdefaults (`application.yml`). `WebEnvironment.NONE` funktioniert **nicht**, weil `BasicSecurityConfig` einen Web-Anwendungskontext braucht (`HttpSecurity`-Bean fehlt sonst).\n- **Jede Organisation/jeder Nutzer muss real persistiert sein**, bevor er als `organizationId`/`ownerId`/`userId` verwendet wird — sonst schlägt der Insert mit der jeweiligen `fk_*`-Verletzung fehl. Bare `UUID.randomUUID()` als Platzhalter für Owner/Member/Organisation funktioniert unter diesem Schema nicht mehr.\n- **`organizationRepository.deleteAll()` in `@BeforeEach` niemals verwenden**, solange der Kontext geteilt wird — das löscht die von Liquibase geseedete `Organization.DEFAULT_ID` und reißt jeden Test um, der sich (wie `UserService.findOrCreateUser`) implizit auf ihre Existenz verlässt. Stattdessen pro Test eigene, frische Organisationen mit zufälliger ID anlegen und in `@AfterEach` gezielt per ID wieder entfernen.\n- **Teardown-Muster für Migrationstests:** `connection.setAutoCommit(true)` **unbedingt** nach jedem `Liquibase.update(...)`, dokumentiert in `io.opaa.migration.package-info`. `Migration008RenameWorkspaceToSpaceTest` folgt dem nicht — das ist eine dokumentierte Altlast, kein Muster zum Nachahmen.\n- **Wirksamkeitsnachweis muss zur eigenen Suite gehören:** Ein Nachweis über eine bereits vorher FK-fähige Klasse belegt nicht die Wirksamkeit der eigenen Umstellung — siehe die Korrektur oben.\n\n## Zugehörige Issues\n\nCloses #288\n\n## Art der Änderung\n\n- [ ] Bugfix\n- [ ] Neues Feature\n- [ ] Dokumentation\n- [x] Refactoring\n- [ ] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal\n- [x] Bei Bugfix: Reproduktionsnachweis erbracht — siehe Abschnitt „Nachweis der Wirksamkeit\" oben (kein Bugfix im engeren Sinn, aber derselbe Nachweis erbracht)\n- [x] Dokumentation aktualisiert (falls zutreffend) — Javadoc in den umgestellten Testklassen und neues `io.opaa.migration.package-info`\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet)\n\n## Annahmen\n\n- Frontend ist von dieser Änderung nicht betroffen; die Frontend-Schritte der Pre-Push-Checkliste (`npm run format/lint/test/build`) wurden deshalb nicht ausgeführt (kein `node_modules` in diesem frischen Worktree, keine Frontend-Datei geändert).\n- `SpaceServiceTest` (reine Unit-Tests mit gemockten Repositories) bewusst nicht umgestellt — dort existiert keine reale Datenbankverbindung und damit kein Fremdschlüsselbezug.\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Opus 5 / Claude Code, Developer-Rolle)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
      "closes": [
        288
      ],
      "additions": 229,
      "deletions": 82,
      "changed_files": 6
    },
    {
      "number": 297,
      "title": "feat(group): Verzeichnissynchronisation als Rechteereignis behandeln",
      "url": "https://github.com/criew/opaa/pull/297",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "size:L",
        "security",
        "auth"
      ],
      "body": "## Zusammenfassung\n\nFuehrt die Verzeichnissynchronisation fuer `ORG_UNIT`-Gruppen (aus #200) als eigenes Rechteereignis ein: `io.opaa.group.sync.DirectorySyncService` gleicht Gruppen und Mitgliedschaften ausschliesslich ueber die stabile Verzeichnis-`externalId` ab, nie ueber den Namen, und trennt strikt Diff-Berechnung (`buildPlan`, liest nur) von Anwendung (`applyPlan`, schreibt nur bei plausiblem, angefordertem Lauf).\n\n**Verbindliche Anforderungen aus dem Issue, umgesetzt:**\n\n- **Stabile Kennung statt Name** — Abgleich ueber `Group.externalId`; eine Umbenennung im Verzeichnis aendert nur den Namen, Grants und Mitgliedschaften bleiben unberuehrt.\n- **Plausibilitaetsschwelle mit Abbruch** — `opaa.directory-sync.change-threshold-fraction` (Default 30 %), gemessen an entfernten Mitgliedschaften relativ zum Vorher-Bestand. Bewusst nur auf Entzuege bezogen, nicht auf Zuwaechse: ein Lauf, der nur Mitgliedschaften hinzufuegt, kann kein Recht entziehen und braucht daher keine Bremse.\n- **Leere Gruppenliste** — liefert das Verzeichnis eine leere Liste, obwohl bereits `ORG_UNIT`-Gruppen existieren, wird der Lauf unbedingt abgebrochen (`ABORTED_EMPTY_RESULT`), unabhaengig von der konfigurierten Schwelle.\n- **Nicht erreichbares Verzeichnis: last-known-good** — `DirectoryUnavailableException` fuehrt zu `UNREACHABLE`, nichts wird geschrieben, der Zustand wird aber dauerhaft ueber `directory_sync_status` (eine Zeile je Organisation) gemeldet, nicht nur in der Antwort des ausloesenden Requests.\n- **Trockenlauf mit Differenzbericht** — `POST /api/v1/admin/directory-sync/dry-run` durchlaeuft denselben Plan-Code wie ein echter Lauf, verlaesst ihn aber vor jeder Schreiboperation.\n- **Eine Protokollzeile je bewirkter Rechteaenderung** — je hinzugefuegter/entfernter Mitgliedschaft, je erstellter/umbenannter/aufgeloester/reaktivierter Gruppe eine eigene `log.info`/`log.warn`-Zeile mit Ursache, nicht eine Sammelzeile je Lauf.\n- **Reorganisation** — verschwindet eine Einheit aus dem Verzeichnis, wird ihre Gruppe als `dissolved` markiert; bestehende Mitgliedschaften bleiben unangetastet (eingefroren), koennen aber nicht mehr wachsen, solange die Gruppe aufgeloest ist. Taucht sie spaeter wieder auf, wird sie automatisch reaktiviert und wie jede andere bestehende Gruppe weitersynchronisiert.\n\n## Architekturentscheidungen\n\n- **`DirectoryClient` als einzige Nahtstelle.** Die eigentliche Verbindung zu einem realen Verzeichnisdienst (LDAP/SCIM, Netzwerk, Credentials) ist eine deployment-spezifische Frage ausserhalb des mit vertretbarem Aufwand testbaren Teils dieses Issues. `DirectorySyncService` ist gegen ein Test-Double von `DirectoryClient` vollstaendig durchgetestet; `NoOpDirectoryClient` ist die produktive Default-Implementierung und meldet konsequent \"nicht erreichbar\" statt einer falschen leeren Gruppenliste, solange kein echter Client konfiguriert ist. Ein echter Verzeichnis-Client kann spaeter als eigene `DirectoryClient`-Bean (per `@ConditionalOnMissingBean` bevorzugt) nachgezogen werden, ohne diese Klasse anzufassen.\n- **Verschachtelte Gruppen: bewusst nicht aufgeloest.** Die Feature-Spezifikation nennt dies einen offenen Punkt. Entscheidung: `DirectoryGroup.parentExternalId` wird auf `Group.parentGroupId` abgebildet und ausschliesslich fuer die Curator-Eskalation aus #208 vorgehalten (so schon in `GroupKind`s Javadoc aus #200 angelegt). Die Mitgliedschaft einer Gruppe ist und bleibt genau das, was das Verzeichnis als direkte Mitglieder dieser Gruppe meldet — ein Mitglied einer Kind-Einheit wird nicht implizit Mitglied der Eltern-Einheit. Diese Grenze ist explizit in `DirectorySyncService`s und `DirectoryGroup`s Javadoc dokumentiert, nicht stillschweigend offengelassen.\n- **`Organization.createdAt`-Falle aus #199 geprüft:** Die Entity traegt bereits ein `@PrePersist`, das `createdAt` nur setzt, wenn es noch `null` ist (aus #200 behoben) — die im Auftrag genannte Falle existiert im aktuellen `main` nicht mehr.\n\n## Zugehörige Issues\n\nCloses #237\nTeil von #198\n\n## Betroffene Module\n\n- `backend` — `io.opaa.group.sync` (neu), `io.opaa.group.Group`/`GroupRepository`, `io.opaa.auth.UserRepository`, Liquibase-Changelog 011, OpenAPI-Spezifikation\n\n## Art der Änderung\n\n- [x] Neues Feature\n\n## Checkliste\n\n- [x] Tests bestehen lokal (siehe Pre-Push-Nachweise unten)\n- [ ] Bei Bugfix: Reproduktionsnachweis erbracht — entfällt, kein Bugfix\n- [x] Dokumentation aktualisiert (falls zutreffend) — Feature-Spezifikation (`docs/features/spaces-and-assets.md`) enthielt die Anforderungen bereits vollständig; keine Änderung nötig\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet) — bereits unterzeichnet (früherer PR)\n\n## Pre-Push-Nachweise\n\n```\n$ ./gradlew spotlessCheck\nBUILD SUCCESSFUL\n\n$ ./gradlew build -x test\nBUILD SUCCESSFUL\n\n$ ./gradlew test\nBUILD SUCCESSFUL in 3m 27s\n(alle Tests grün, inkl. neuer DirectorySyncServiceIntegrationTest (9 Szenarien, echte\nPostgres/Liquibase-Schema-FKs) und Migration011DirectorySyncTest;\n1 vorbestehender, unveränderter Skip: OpenAiIntegrationTest, benötigt echten API-Key)\n\n$ npm run format && npm run lint\nalles unverändert / keine Findings\n\n$ npm run test -- --run\nTest Files  28 passed (28)\nTests  140 passed (140)\n\n$ npm run build\nBUILD SUCCESSFUL\n```\n\nFrontend ist von diesem PR nicht inhaltlich betroffen (kein `frontend`-Label, keine UI); alle Frontend-Checks wurden dennoch vollständig ausgeführt.\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code, Developer-Rolle)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
      "closes": [
        237
      ],
      "additions": 2634,
      "deletions": 1,
      "changed_files": 27
    },
    {
      "number": 296,
      "title": "docs(agents): Branch-Regel klarstellen und Reproduktionsnachweis verbindlich machen",
      "url": "https://github.com/criew/opaa/pull/296",
      "author": "criew",
      "labels": [
        "documentation",
        "size:S"
      ],
      "body": "## Zusammenfassung\n\nZwei Klarstellungen an den Agenten- und Workflow-Regeln, beide ausgelöst durch Befunde aus den Reviews zu Stufe A von #198.\n\n### 1. Branch-Regel gilt ausnahmslos\n\nBeim Hotfix in PR #287 wurde der Branch `fix/280_personal-space-transaction` verwendet. Die Regel in `AGENTS.md` war eindeutig, sagte aber nicht ausdrücklich, dass sie auch für Fehlerbehebungen gilt.\n\nEntscheidung des Maintainers: **Alle** Branches beginnen mit `feature/`. Kein `fix/`-, `hotfix/`- oder `docs/`-Präfix. Die Art der Änderung wird über den Conventional-Commit-Typ ausgedrückt. In `CONTRIBUTING.md` stand mit `feature/15_fix-null-pointer` bereits ein passendes Beispiel — die Absicht war immer so gemeint, nur nicht ausgesprochen.\n\n### 2. Reproduktionsnachweis bei Fehlerbehebungen\n\n**Dreimal in Folge war der Produktivcode fehlerhaft und der zugehörige Test trotzdem grün:**\n\n| PR | Fehler | Warum der Test ihn nicht fing |\n|---|---|---|\n| #254 | Migration brach auf nicht-leerem Bestand ab | Changelog wurde in den Tests nie ausgeführt |\n| #280 | `REQUIRES_NEW` brach jede Erstanmeldung | Testschema kannte den Fremdschlüssel nicht |\n| #283 | Cache-Invalidierung vor dem Commit | Test prüft eine Bedingung, die vor und nach dem Fix gilt |\n\nGefunden hat es jedes Mal erst der Code Reviewer — indem er den Fix zurückdrehte und schaute, ob der Test dann fehlschlägt. `AGENTS.md` verlangte bisher den reproduzierenden Test, aber nicht den Beleg, dass er den Fehler auch fängt.\n\nNeu ist ein Abschnitt „Reproduktionsnachweis\" mit den vier bisher beobachteten Ursachen für wirkungslose Tests, eine Regel in `.claude/rules/workflow.md` und ein Checklistenpunkt im PR-Template.\n\n## Zugehörige Issues\n\nCloses #295\n\n## Art der Änderung\n\n- [x] Dokumentation\n\n## Checkliste\n\n- [x] `AGENTS.md`, `.claude/rules/workflow.md`, `CONTRIBUTING.md` und das PR-Template tragen konsistente Aussagen\n- [x] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt\n\n## KI-Agenten-Offenlegung\n\nErstellt von Claude Opus 5 als Orchestrator, auf Weisung des Maintainers. Auslöser waren Befunde aus den Reviews zu PR #254, #280, #283 und #287.",
      "closes": [
        295
      ],
      "additions": 22,
      "deletions": 2,
      "changed_files": 4
    },
    {
      "number": 292,
      "title": "test(eval): Retrieval-Metrik-Harness (Hit Rate, MRR, nDCG, Recall)",
      "url": "https://github.com/criew/opaa/pull/292",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "evaluation"
      ],
      "body": "## Zusammenfassung\n\nEin JUnit-Integrationstest indiziert den eingefrorenen Korpus (`eval/corpus/comic-characters/`) über die produktive Indizierungs-Pipeline (`FileProcessingService`/`ChunkingService`) und führt jede Anfrage aus dem Golden Dataset (`eval/golden/comic-characters.json`) direkt gegen `VectorStore.similaritySearch(topK=10)` aus — ohne LLM, per ADR-0011 Entscheidung 3. Testcontainers: `pgvector/pgvector:pg18` und `ollama/ollama:0.6.5` mit `nomic-embed-text` (768 Dimensionen).\n\nDer Harness:\n\n- prüft `MANIFEST.sha256` gegen den Korpus vor jedem Lauf und bricht bei jeder Abweichung mit einer benannten Fehlermeldung ab (ADR-0011)\n- verifiziert nach dem Indizieren die **Ein-Chunk-Invariante** aus ADR-0010 — jedes Korpusdokument muss über den echten, produktiv konfigurierten `TokenTextSplitter` genau einen Chunk ergeben — und benennt Verletzungen namentlich\n- berechnet Hit Rate@5, MRR, nDCG@10 und Recall@10, gesamt sowie je Kategorie, Schwierigkeit und Sprache\n- schreibt einen maschinenlesbaren JSON-Report (`backend/build/eval-reports/retrieval-metrics.json`) mit Lauf-Konfiguration (Embedding-Modell/-Version, `chunkSize`, `topK`, Korpus-Manifest-Hash, Golden-Dataset-Hash) sowie eine lesbare Konsolen-Zusammenfassung inklusive der zehn schlechtesten Anfragen\n\n## Auslösemechanismus\n\nEigenes Gradle-Source-Set (`backend/src/evalTest/`) mit eigenem Task (`./gradlew evaluateRetrieval`), bewusst **nicht** an `build`/`check`/`test` angehängt (per `git grep`/`--dry-run` verifiziert). Begründung: Der Lauf braucht Docker, zieht beim ersten Mal ein ~275 MB Embedding-Modell und indiziert danach ~1.450 Dokumente über echte Ollama-Embeddings — das würde jeden normalen `./gradlew build` unbeabsichtigt verlangsamen und in Umgebungen ohne Docker/Netz hart brechen lassen. Ein eigenes Source-Set (statt z. B. eines JUnit-Tags mit `excludeTags`) hält die Trennung auch auf Klassenpfad-Ebene sauber und ist das etablierte Gradle-Muster für solche \"zweiten Testsuiten\".\n\n## Gemessene Ergebnisse (1.448 Dokumente, 121 Golden-Fälle)\n\nVoller Lauf: **~18 Minuten** (Indizierung + Auswertung), Ein-Chunk-Invariante hält für **alle 1.448 Dokumente ohne Ausnahme**.\n\n| Kategorie | n | Hit Rate@5 | MRR | nDCG@10 | Recall@10 |\n|---|---|---|---|---|---|\n| Gesamt | 121 | 0.537 | 0.474 | 0.463 | 0.500 |\n| `attribute_lookup` | 30 | 1.000 | 0.967 | 0.975 | 1.000 |\n| `entity_description` | 20 | 0.650 | 0.502 | 0.550 | 0.700 |\n| `crosslingual` | 34 | 0.412 | 0.364 | 0.348 | 0.358 |\n| `multi_attribute_filter` | 21 | 0.238 | 0.206 | 0.137 | 0.159 |\n| `numeric_range` | 16 | 0.188 | 0.101 | 0.063 | 0.060 |\n\nJe Sprache: Englisch (n=87) nDCG@10=0.507, Deutsch (n=34, alles `crosslingual`-Zwillinge) nDCG@10=0.348. Je Schwierigkeit: `easy` nDCG@10=0.982, `medium` 0.252, `hard` 0.140.\n\nDas entspricht genau dem in ADR-0011/der Feature-Spezifikation erwarteten Muster: reines Attribut-Lookup (eine Entität, ein exaktes Faktenfeld) funktioniert nahezu perfekt, während Mengen-Fragen (`multi_attribute_filter`, `numeric_range`) — der Fall, an dem reine Vektorsuche bricht — deutlich schlechter abschneiden. Die Baseline und Schwellenwerte für eine CI-Regression sind bewusst nicht Teil dieses PRs, siehe #228.\n\n## Kalibrierungshinweise (im Report abgebildet)\n\n- Der Korpus ist absichtlich uniform (Jaccard-Median 0,51 über ganze Dokumente laut Review zu #225/#226) — das staucht die Score-Verteilung und macht Hit Rate/MRR unempfindlicher gegenüber echten Regressionen. Künftige Schwellenwerte (#228) müssen gegen diese tatsächliche Verteilung kalibriert werden.\n- Die Fallzahl überschätzt die Zahl unabhängiger Beobachtungen: 121 Fälle teilen sich nur 75 unterschiedliche Erwartungsmengen, u. a. weil jeder `crosslingual`-Fall konstruktionsbedingt der deutsche Zwilling eines englischen Falls ist. Der Report weist das explizit über `datasetNotes` aus.\n\n## Zugehörige Issues\n\nCloses #227\n\n## Art der Änderung\n\n- [ ] Bugfix\n- [x] Neues Feature\n- [ ] Dokumentation\n- [ ] Refactoring\n- [ ] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal (`./gradlew evaluateRetrieval`, zweimal ausgeführt; `./gradlew build` unverändert grün und unbeeinflusst)\n- [x] Dokumentation aktualisiert (`eval/README.md`: wie der Harness ausgeführt wird, wie der Report zu lesen ist)\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet)\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code, Rolle QA Engineer)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
      "closes": [
        227
      ],
      "additions": 1661,
      "deletions": 6,
      "changed_files": 15
    },
    {
      "number": 283,
      "title": "Gruppen als Rechtesubjekt einfuehren",
      "url": "https://github.com/criew/opaa/pull/283",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "frontend",
        "size:M",
        "auth"
      ],
      "body": "## Zusammenfassung\n\nFuehrt Gruppen als zweiten Rechtesubjekt-Typ neben Nutzern ein, bevor der erste Grant existiert (#202) - ein nachtraeglich eingefuehrter Subjekttyp wuerde sonst jede Rechteabfrage, jeden Grant und jeden Cache beruehren.\n\n- `io.opaa.group` mit `Group`/`GroupMembership`: `Group.kind` unterscheidet `ORG_UNIT` (aus dem Verzeichnis, stabile `externalId` = objectGUID/SCIM-externalId statt Name, Elterneinheit) von `AD_HOC` (im System angelegt, ohne Kurator/Verteilungsstufe). Gruppen sind per zusammengesetztem Fremdschluessel strikt an eine Organisation gebunden (analog `space_memberships`).\n- `PermissionSubject` (User oder Group, mit `organizationId`) als gemeinsame Abstraktion fuer kuenftige Grants; `GroupMembershipResolver` loest die Gruppen eines Nutzers sowie die Nutzer eines Subjekts auf und cacht das Ergebnis (Caffeine, wie `RateLimitService`), mit Invalidierung nach Transaktions-Commit bei jeder Mitgliedschaftsaenderung. Beide Zweige von `resolveUserIds` (USER und GROUP) sind organisationsscharf.\n- `GroupService`/`GroupController` unter `/api/v1/admin/groups` (nur Systemadministratoren): Gruppen anlegen, umbenennen, loeschen, Mitglieder hinzufuegen/entfernen - beschraenkt auf `AD_HOC`-Gruppen; `ORG_UNIT`-Gruppen bleiben schreibgeschuetzt, bis die Verzeichnissynchronisation sie befuellt (#237).\n- Liquibase-Changelog `009` legt `groups` und `group_memberships` an; `Migration009CreateGroupsTest` belegt am realen Schema (nicht am Hibernate-generierten Test-Schema), dass die Organisationsgrenze durch die zusammengesetzte Fremdschluesselbeziehung tatsaechlich durchgesetzt wird.\n- Frontend: neue Seite „Gruppen\" unter `/admin/groups`, nur fuer Systemadministratoren im Menue sichtbar - Gruppen anlegen/umbenennen/loeschen, Mitglieder pflegen; `ORG_UNIT`-Gruppen werden als schreibgeschuetzt dargestellt.\n\n## Review-Runden\n\n**Runde 1** (Merge-Konflikt mit #280, Cache-Invalidierung vor statt nach Commit, `PermissionSubject` ohne Organisationsgrenze, diverse Nits) - behoben, siehe Kommentare unten.\n\n**Runde 2** (Nachpruefung) - zwei verbleibende Punkte behoben:\n- Der Integrationstest fuer die Cache-Invalidierung pruefte bislang nur „irgendwann vor der Assertion\", nicht „nach dem Commit\". Neuer Test `removingAMemberDoesNotLeaveAStaleCacheEntryFromAReaderDuringTheOpenTransaction` in `GroupServiceIntegrationTest` stellt die Race explizit nach: offene Transaktion via `PlatformTransactionManager`/`TransactionTemplate`, ein Leser-Thread liest waehrend die Transaktion offen ist, Assertion erst nach dem Commit.\n  **Reproduktionsnachweis** (gefordert seit der zwischenzeitlich gemergten AGENTS.md-Ergaenzung, siehe #295): Fix in `GroupService#removeMember` vorruebergehend auf die urspruengliche Inline-Invalidierung zurueckgesetzt (`membershipResolver.invalidateUser(memberUserId)` statt `invalidateAfterCommit(...)`), Test lief:\n  ```\n  GroupServiceIntegrationTest > removingAMemberDoesNotLeaveAStaleCacheEntryFromAReaderDuringTheOpenTransaction() FAILED\n  java.lang.AssertionError:\n  Expecting empty but was: [2ec42d3e-5d9c-444d-9fc8-3fdc887779c7]\n  ```\n  Fix wiederhergestellt, Test lief wieder gruen (siehe Pre-Push-Belege unten).\n- `GroupMembershipResolver#resolveUserIds` pruefte im `USER`-Zweig die Organisationsgrenze nicht, obwohl der Javadoc genau das versprach. Behoben: `USER`-Subjekte werden jetzt gegen `UserRepository` auf Zugehoerigkeit zur genannten Organisation geprueft (ein zusaetzlicher indizierter Zugriff auf dem heissen Pfad, bewusst in Kauf genommen, damit die Klasse ihr eigenes Versprechen haelt statt es an kuenftige Aufrufer in #202 zu delegieren).\n- Nit: `GroupMembershipRepository#findByUserIdAndGroupId` entfernt (unbenutzt).\n- Nit (asymmetrische DB-Grenze `users(id, organization_id)`): bewusst nicht in diesem PR behoben, siehe #289.\n\n## Zugehörige Issues\n\nCloses #200\nTeil von #198 (Stage A)\nFolge-Issue fuer eine DB-seitige Haertung, die dieser PR bewusst nicht mitnimmt: #289\n\n## Art der Änderung\n\n- [x] Neues Feature\n- [ ] Bugfix\n- [ ] Dokumentation\n- [ ] Refactoring\n- [ ] CI/Build\n\n## Annahmen\n\n- **Gruppenerstellung ist auf `AD_HOC` beschraenkt.** `ORG_UNIT`-Gruppen entstehen laut Spezifikation ausschliesslich durch die Verzeichnissynchronisation (#237); die Admin-API/-UI dieses PRs erstellt daher nur `AD_HOC`-Gruppen. `ORG_UNIT` ist im Datenmodell bereits vollstaendig angelegt (Kind, `externalId`, `parentGroupId`).\n- **„Loeschen einer Gruppe mit Asset-Eigentum wird blockiert\" ist noch nicht implementierbar.** Es gibt noch kein Asset-Modell (`io.opaa.indexing.Document` kennt keinen Eigentuemer) - #201/#202 fuehren Assets und Asset-Eigentuemerschaft erst ein. `GroupService#deleteGroup` traegt dafuer ein `TODO(#202)`.\n- **Gruppenmanagement ist system-adminweit, nicht curator-basiert.** Kuratoren an Organisationseinheiten sind #208; dieses PR haelt lediglich `parentGroupId` fuer die spaetere Eskalation vor.\n\n## Checkliste\n\n- [x] Tests bestehen lokal\n- [x] Dokumentation aktualisiert (falls zutreffend) - die Spezifikation (`docs/features/spaces-and-assets.md`, ADR-0008) beschreibt das Modell bereits vollstaendig; keine weiteren Aenderungen noetig\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet)\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Sonnet 5, Developer-Agent)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
      "closes": [
        200
      ],
      "additions": 3127,
      "deletions": 7,
      "changed_files": 30
    }
  ],
  "open_pull_requests": [
    {
      "number": 309,
      "title": "feat(library): Asset-Rechte und rechtebewusste Vektorsuche",
      "url": "https://github.com/criew/opaa/pull/309",
      "author": "criew",
      "labels": [
        "enhancement",
        "backend",
        "security"
      ],
      "body": "",
      "closes": []
    },
    {
      "number": 161,
      "title": "docs: plugin architecture deep dive for all three variants",
      "url": "https://github.com/criew/opaa/pull/161",
      "author": "bigpuritz",
      "labels": [
        "documentation",
        "enhancement",
        "backend"
      ],
      "body": "",
      "closes": []
    }
  ],
  "ci": {
    "conclusion": "success",
    "url": "https://github.com/criew/opaa/actions/runs/30826947865",
    "title": "Merge pull request #305 from criew/feature/201_wissensbibliothek",
    "finished_at": "2026-08-03T15:22:25Z"
  },
  "summary": "Epic #198 „Space and asset model — replace the workspace model\" machte heute Fortschritte bei der Umsetzung: drei Tickets wurden abgeschlossen und drei gemergt, womit vier von 25 Tickets erledigt sind. Die gemergte #305 führt KnowledgeLibrary als ersten Asset-Typ ein und migriert Dokumente in diesen Container. #297 behandelt Verzeichnissynchronisation als Rechteereignis mit stabiler externer ID und strikter Trennung von Diff-Berechnung und Anwendung, während #283 Gruppen als zweiten Rechtesubjekt-Typ neben Nutzern einführt.\n\nEpic #224 „Suchqualität messbar machen — Demo-Korpus und Retrieval-Regression\" machte ebenfalls Fortschritte: zwei Tickets wurden abgeschlossen und zwei gemergt, womit sechs von 14 Tickets erledigt sind. Die gemergte #301 führt den Retrieval-Harness als automatisierten CI-Job gegen eine committete Baseline aus und vergleicht dabei Fixpunkte wie Korpus-Manifest und Embedding-Modell-Digest. #292 indiziert den eingefrorenen Korpus über die produktive Pipeline und berechnet Hit Rate, MRR, nDCG und Recall gegen den Vektor-Store.\n\nOhne Epic-Bezug wurden sieben Tickets neu angelegt und vier abgeschlossen. Die gemergte #299 behebt einen Race Condition bei paralleler Erstanmeldung auf der Unique-Constraint uq_users_subject_issuer. #298 stellt FK-abhängige Space-Tests auf echtes Liquibase-Schema um. #303 und #296 dokumentieren Transaktionsbehandlung und Branch-Regeln in den Entwickler-Richtlinien.",
  "epics": [
    {
      "number": 198,
      "title": "Epic: Space and asset model — replace the workspace model",
      "tickets_total": 25,
      "tickets_closed": 4,
      "opened": [],
      "closed": [
        {
          "number": 237,
          "title": "Verzeichnissynchronisation als Rechteereignis behandeln",
          "url": "https://github.com/criew/opaa/issues/237",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "size:L",
            "security",
            "auth"
          ],
          "body": "## Kontext\r\n\r\nGruppen kommen aus dem Verzeichnisdienst. Ein Synchronisationslauf, der Mitgliedschaften entfernt,\r\nist damit ein **Massen-Rechteentzug ohne menschlichen Entscheidungspunkt** — die einzige Stelle im\r\nModell, an der sich Rechte in großer Zahl ändern, ohne dass jemand entscheidet.\r\n\r\nDie frühere Formulierung, die Synchronisation ändere „nur die Herkunft der Mitgliedschaften, nicht\r\ndas Rechtemodell\", ist irreführend: Die Herkunft **ist** das Risiko. An ihr hängen die\r\nNachfolgeregelung, der Zustand von Strikt-Spaces und die Nachweisbarkeit.\r\n\r\nDies ist keine spätere Ausbaustufe, sondern Voraussetzung für den ersten Produktivbetrieb.\r\n\r\nSiehe [`docs/features/spaces-and-assets.md`](docs/features/spaces-and-assets.md) und [ADR-0008](docs/decisions/0008-space-and-asset-model.md).\r\n\r\n## Umfang\r\n\r\n- Abgleich über die **stabile Kennung** des Verzeichnisses (`objectGUID`, SCIM-`externalId`),\r\n  niemals über den Gruppennamen\r\n- **Plausibilitätsschwelle mit Abbruch:** Übersteigt ein Lauf einen konfigurierbaren Anteil\r\n  geänderter Mitgliedschaften, wird er abgebrochen und gemeldet\r\n- **Trockenlauf mit Differenzbericht** vor dem ersten Lauf und vor jeder Konfigurationsänderung\r\n- **Nicht erreichbares Verzeichnis: `last-known-good`.** Keine Rechte werden entzogen, der Zustand\r\n  wird gemeldet\r\n- **Eine Protokollzeile je bewirkter Rechteänderung**, nicht je Lauf\r\n- **Reorganisation:** Umbenennung folgenlos (stabile Kennung); bei Zusammenlegung oder Neuschnitt\r\n  werden Gruppen als aufgelöst markiert, bestehende Grants bleiben wirksam, können aber nicht\r\n  erweitert werden; Assets aufgelöster Einheiten gehen in „Nachfolge offen\"\r\n- **Reorganisationsbericht** für den System-Admin: weggefallene Einheiten, betroffene Assets und\r\n  Grants, offene Nachfolgen\r\n\r\n## Abnahmekriterien\r\n\r\n- [ ] Eine umbenannte Gruppe behält ihre Grants und ihre Mitglieder\r\n- [ ] Ein Lauf, der mehr als den konfigurierten Anteil an Mitgliedschaften ändert, wird abgebrochen\r\n      und gemeldet, ohne etwas zu verändern\r\n- [ ] Liefert das Verzeichnis eine leere Gruppenliste, wird kein Recht entzogen\r\n- [ ] Ist das Verzeichnis nicht erreichbar, bleibt der letzte bekannte Stand in Kraft\r\n- [ ] Der Trockenlauf verändert nichts und liefert die vollständige Differenz\r\n- [ ] Jede bewirkte Rechteänderung erzeugt genau eine Protokollzeile mit Ursache\r\n- [ ] Assets einer aufgelösten Einheit sind nutzbar, aber ihre Reichweite ist eingefroren\r\n- [ ] Backend-Formatierung, Build und Tests bestehen\r\n\r\n## Hängt ab von\r\n\r\nGruppen als Rechtesubjekt",
          "closes": []
        },
        {
          "number": 201,
          "title": "Knowledge library as the document container, with data migration",
          "url": "https://github.com/criew/opaa/issues/201",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "size:L"
          ],
          "body": "## Context\r\n\r\nDocuments currently have **no** owning container at all: `io.opaa.indexing.Document` has no workspace\r\nreference and the Liquibase changelogs contain no such column. The permission anchor is not being\r\nmoved here, it is being introduced for the first time.\r\n\r\nSee [`docs/features/spaces-and-assets.md`](docs/features/spaces-and-assets.md) and [ADR-0008](docs/decisions/0008-space-and-asset-model.md).\r\n\r\n## Scope\r\n\r\n- `KnowledgeLibrary` as the first asset type: id, name, description, owner (user or group),\r\n  organization, `visibility`, `listed`\r\n- `Document.libraryId` and `library_id` on the chunk metadata\r\n- Personal knowledge library \"My documents\" created together with the personal space\r\n- Library CRUD API and a documents endpoint at `/api/v1/libraries/{id}/documents`\r\n- Upload targets a library, not a space\r\n- **The relational database is the leading store**; the vector store is derived. Documented here and\r\n  in the specification, because it determines how backups are set up\r\n\r\n## Acceptance criteria\r\n\r\n- [ ] Every document belongs to exactly one knowledge library; the column is non-nullable\r\n- [ ] Every chunk carries `library_id` and `organization_id`\r\n- [ ] Existing documents are migrated into a system library **readable by system admins only**\r\n- [ ] Creating a user creates personal space and personal library atomically\r\n- [ ] Re-indexing keeps the library assignment\r\n- [ ] **Dry run with volume figures** before the `library_id` backfill; migration is resumable and\r\n      the rollback order is documented\r\n- [ ] Backend format, build and test pass\r\n\r\n## Depends on\r\n\r\n#199, #200 (for group-owned libraries)\r\n\r\nPart of #198",
          "closes": []
        },
        {
          "number": 200,
          "title": "Introduce groups as permission subjects",
          "url": "https://github.com/criew/opaa/issues/200",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "size:M",
            "auth"
          ],
          "body": "## Context\r\n\r\nPermission entries reference a **user or a group**. Groups are not a convenience feature: the\r\ndistribution level \"department\" is a grant to the department group, and there is deliberately no\r\ndepartment object. Without groups that level does not exist. Groups can also **own an asset**, which\r\nis what keeps centrally maintained libraries alive across staff changes.\r\n\r\nSee [`docs/features/spaces-and-assets.md`](docs/features/spaces-and-assets.md) and [ADR-0008](docs/decisions/0008-space-and-asset-model.md).\r\n\r\n## Scope\r\n\r\n- `Group` entity scoped to an organization, with members\r\n- `Group.kind`: `ORG_UNIT` (from the directory, carries a parent unit, can have curators) and\r\n  `AD_HOC` (created in the system, no curator, no distribution level)\r\n- Group management API and admin UI (create, rename, delete, add and remove members)\r\n- A permission subject abstraction resolving to user or group\r\n- Group membership resolution with caching and invalidation on change\r\n\r\n## Acceptance criteria\r\n\r\n- [ ] A group can be created, renamed, deleted and have members added and removed\r\n- [ ] Groups never cross an organization boundary\r\n- [ ] Resolving the groups of a user is cached and invalidated immediately on membership change\r\n- [ ] Deleting a group that owns assets is blocked until ownership is transferred\r\n- [ ] Membership is **not** inherited downward: a grant to \"Amt 5\" reaches exactly the people the\r\n      directory places in that group — no inheritance is invented\r\n- [ ] Backend and frontend format, lint, build and test pass\r\n\r\n## Notes\r\n\r\nDirectory synchronisation is #237 and changes only the origin of memberships, not the permission\r\nmodel. Responsibility, unlike membership, **is** inherited upward — see #208.\r\n\r\nPart of #198",
          "closes": []
        }
      ],
      "merged": [
        {
          "number": 305,
          "title": "feat(library): Wissensbibliothek als Dokumentencontainer mit Migration",
          "url": "https://github.com/criew/opaa/pull/305",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "size:L"
          ],
          "body": "## Zusammenfassung\n\nFuehrt `KnowledgeLibrary` als ersten Asset-Typ ein (#201, Teil des Epics #198): eine Wissensbibliothek hat einen eigenen Eigentuemer (Nutzer oder Gruppe), gehoert zu genau einer Organisation, traegt Sichtbarkeitsstufe (`PRIVATE`/`SHARED`/`ORGANIZATION`) und Auffindbarkeits-Flag - unabhaengig vom Space. Jedes Dokument gehoert ab jetzt zu genau einer Bibliothek (`library_id`, nicht mehr nullbar); jeder Chunk traegt zusaetzlich `organization_id`, die Filterachse, die #202s rechtebewusste Vektorsuche direkt braucht.\n\n**Migration 012** (`db.changelog-master.yaml`): Tabelle `knowledge_libraries`, Seed der einen System-Bibliothek (nur fuer System-Admins lesbar, fail-closed), Backfill der bestehenden Dokumente als ein einzelnes `UPDATE` (nicht batched - siehe Begleitdokument fuer die Begruendung, das eine fruehere Batch-Fassung als fehlerhaft korrigiert), danach `NOT NULL` + zusammengesetzter Fremdschluessel gegen `knowledge_libraries(id, organization_id)`. Begleitdokument mit Trockenlauf-Anleitung, Mengengeruest und Rollback-Reihenfolge: `docs/migrations/012-knowledge-library.md`.\n\n**Owner-Modell:** Zwei separate Fremdschluessel-Spalten (`owner_user_id`, `owner_group_id`) statt einer polymorphen `owner_id` - beide tragen dadurch einen echten, von der Datenbank durchgesetzten Fremdschluessel.\n\n**Nutzeranlage:** Ein neuer Nutzer bekommt automatisch einen persoenlichen Space *und* eine persoenliche Bibliothek \"Meine Dokumente\". Beide Provisionierungsaufrufe fuer denselben Nutzer werden ueber ein prozesslokales, gestriptes Lock serialisiert (reine Performance-Massnahme gegen Datenbank-Contention bei vielen gleichzeitigen Erstanmeldungen desselben Nutzers), laufen jeweils als einzelnes `INSERT ... ON CONFLICT ... DO NOTHING` (ein Roundtrip statt bis zu zwei), und ein Fehlschlag wird protokolliert statt den Login-Request scheitern zu lassen - damit ein Provisionierungsfehler nicht zur Aussperrung wird. Die Korrektheit traegt weiterhin ausschliesslich der jeweilige partielle Unique-Index in der Datenbank.\n\n**Loeschschutz:** Eine Bibliothek mit Dokumenten und eine Gruppe mit Bibliotheken lassen sich nicht loeschen (409 statt eines unbehandelten 500).\n\n## Zugehoerige Issues\n\nCloses #201\n\n## Art der Aenderung\n\n- [x] Neues Feature\n\n## Checkliste\n\n- [x] Tests bestehen lokal (Backend: 317 Tests, 0 fehlgeschlagen, 1 vorbestehender unabhaengiger Skip; Frontend: 140 Tests, 0 fehlgeschlagen; `UserServiceCreationRaceIntegrationTest` zusaetzlich 9x in Folge gruen bei unveraendertem Produktions-Standardpool von 10)\n- [ ] Bei Bugfix: Reproduktionsnachweis erbracht - entfaellt, kein Bugfix\n- [x] Dokumentation aktualisiert (`docs/migrations/012-knowledge-library.md`, `docs/features/spaces-and-assets.md`)\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet - wird im ersten PR-Kommentar nachgereicht\n\n## KI-Agenten-Offenlegung\n\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude, Entwickler-Subagent)\n\n## Annahmen\n\n- **\"Atomisch\"** bei der gemeinsamen Anlage von persoenlichem Space und persoenlicher Bibliothek ist nicht als eine gemeinsame Datenbanktransaktion umgesetzt, sondern als \"immer gemeinsam versucht, unabhaengige Fehlergrenzen, Fehlschlag wird protokolliert statt den Login zu blockieren\" - jede der beiden Operationen behaelt ihren eigenen Race-Schutz ueber ihren jeweiligen partiellen Unique-Index. In `UserService#ensureBothPersonalAssets` begruendet.\n- **Bewusster Zwischenzustand (mit dem Maintainer abgestimmt):** Sowohl Datei-/URL-Crawl als auch ein spaeterer manueller Upload-Pfad weisen Dokumente ausschliesslich der System-Bibliothek zu - fuer normale Nutzer nicht erreichbar, nur fuer System-Admins. Die Zielauswahl kommt mit #202 (manueller Upload) bzw. #207 (Konnektor-Quellen).\n\n## Review-Historie dieses PRs\n\nEin erster Review-Durchgang fand, dass ein testseitiger Connection-Pool-Override eine echte Regression kaschierte statt sie zu beheben (gemessen: 5 von 9 Laeufen schlugen bei Produktions-Poolgroesse fehl, und im Fehlerfall meldete der Login trotzdem Erfolg, obwohl die persoenliche Bibliothek fehlte). Der Override wurde entfernt; die tatsaechliche Ursache (Datenbank-Contention bei gleichzeitigen Inserts gegen denselben partiellen Unique-Index, verdoppelt durch die zweite Provisionierungsstufe) ist behoben durch: (1) ein einzelnes `INSERT ... ON CONFLICT ... DO NOTHING` statt des bisherigen insert-dann-Exception-fangen-dann-erneut-lesen-Musters fuer sowohl den persoenlichen Space als auch die persoenliche Bibliothek, und (2) ein prozesslokales Lock je Nutzer, das die Datenbank-Contention bei vielen gleichzeitigen Erstanmeldungen desselben Nutzers auf einen einzigen tatsaechlichen Datenbankzugriff reduziert. Nachweis: 9 aufeinanderfolgende gruene Laeufe bei unveraendertem Produktions-Standardpool von 10.",
          "closes": [
            201
          ],
          "additions": 3776,
          "deletions": 112,
          "changed_files": 33
        },
        {
          "number": 297,
          "title": "feat(group): Verzeichnissynchronisation als Rechteereignis behandeln",
          "url": "https://github.com/criew/opaa/pull/297",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "size:L",
            "security",
            "auth"
          ],
          "body": "## Zusammenfassung\n\nFuehrt die Verzeichnissynchronisation fuer `ORG_UNIT`-Gruppen (aus #200) als eigenes Rechteereignis ein: `io.opaa.group.sync.DirectorySyncService` gleicht Gruppen und Mitgliedschaften ausschliesslich ueber die stabile Verzeichnis-`externalId` ab, nie ueber den Namen, und trennt strikt Diff-Berechnung (`buildPlan`, liest nur) von Anwendung (`applyPlan`, schreibt nur bei plausiblem, angefordertem Lauf).\n\n**Verbindliche Anforderungen aus dem Issue, umgesetzt:**\n\n- **Stabile Kennung statt Name** — Abgleich ueber `Group.externalId`; eine Umbenennung im Verzeichnis aendert nur den Namen, Grants und Mitgliedschaften bleiben unberuehrt.\n- **Plausibilitaetsschwelle mit Abbruch** — `opaa.directory-sync.change-threshold-fraction` (Default 30 %), gemessen an entfernten Mitgliedschaften relativ zum Vorher-Bestand. Bewusst nur auf Entzuege bezogen, nicht auf Zuwaechse: ein Lauf, der nur Mitgliedschaften hinzufuegt, kann kein Recht entziehen und braucht daher keine Bremse.\n- **Leere Gruppenliste** — liefert das Verzeichnis eine leere Liste, obwohl bereits `ORG_UNIT`-Gruppen existieren, wird der Lauf unbedingt abgebrochen (`ABORTED_EMPTY_RESULT`), unabhaengig von der konfigurierten Schwelle.\n- **Nicht erreichbares Verzeichnis: last-known-good** — `DirectoryUnavailableException` fuehrt zu `UNREACHABLE`, nichts wird geschrieben, der Zustand wird aber dauerhaft ueber `directory_sync_status` (eine Zeile je Organisation) gemeldet, nicht nur in der Antwort des ausloesenden Requests.\n- **Trockenlauf mit Differenzbericht** — `POST /api/v1/admin/directory-sync/dry-run` durchlaeuft denselben Plan-Code wie ein echter Lauf, verlaesst ihn aber vor jeder Schreiboperation.\n- **Eine Protokollzeile je bewirkter Rechteaenderung** — je hinzugefuegter/entfernter Mitgliedschaft, je erstellter/umbenannter/aufgeloester/reaktivierter Gruppe eine eigene `log.info`/`log.warn`-Zeile mit Ursache, nicht eine Sammelzeile je Lauf.\n- **Reorganisation** — verschwindet eine Einheit aus dem Verzeichnis, wird ihre Gruppe als `dissolved` markiert; bestehende Mitgliedschaften bleiben unangetastet (eingefroren), koennen aber nicht mehr wachsen, solange die Gruppe aufgeloest ist. Taucht sie spaeter wieder auf, wird sie automatisch reaktiviert und wie jede andere bestehende Gruppe weitersynchronisiert.\n\n## Architekturentscheidungen\n\n- **`DirectoryClient` als einzige Nahtstelle.** Die eigentliche Verbindung zu einem realen Verzeichnisdienst (LDAP/SCIM, Netzwerk, Credentials) ist eine deployment-spezifische Frage ausserhalb des mit vertretbarem Aufwand testbaren Teils dieses Issues. `DirectorySyncService` ist gegen ein Test-Double von `DirectoryClient` vollstaendig durchgetestet; `NoOpDirectoryClient` ist die produktive Default-Implementierung und meldet konsequent \"nicht erreichbar\" statt einer falschen leeren Gruppenliste, solange kein echter Client konfiguriert ist. Ein echter Verzeichnis-Client kann spaeter als eigene `DirectoryClient`-Bean (per `@ConditionalOnMissingBean` bevorzugt) nachgezogen werden, ohne diese Klasse anzufassen.\n- **Verschachtelte Gruppen: bewusst nicht aufgeloest.** Die Feature-Spezifikation nennt dies einen offenen Punkt. Entscheidung: `DirectoryGroup.parentExternalId` wird auf `Group.parentGroupId` abgebildet und ausschliesslich fuer die Curator-Eskalation aus #208 vorgehalten (so schon in `GroupKind`s Javadoc aus #200 angelegt). Die Mitgliedschaft einer Gruppe ist und bleibt genau das, was das Verzeichnis als direkte Mitglieder dieser Gruppe meldet — ein Mitglied einer Kind-Einheit wird nicht implizit Mitglied der Eltern-Einheit. Diese Grenze ist explizit in `DirectorySyncService`s und `DirectoryGroup`s Javadoc dokumentiert, nicht stillschweigend offengelassen.\n- **`Organization.createdAt`-Falle aus #199 geprüft:** Die Entity traegt bereits ein `@PrePersist`, das `createdAt` nur setzt, wenn es noch `null` ist (aus #200 behoben) — die im Auftrag genannte Falle existiert im aktuellen `main` nicht mehr.\n\n## Zugehörige Issues\n\nCloses #237\nTeil von #198\n\n## Betroffene Module\n\n- `backend` — `io.opaa.group.sync` (neu), `io.opaa.group.Group`/`GroupRepository`, `io.opaa.auth.UserRepository`, Liquibase-Changelog 011, OpenAPI-Spezifikation\n\n## Art der Änderung\n\n- [x] Neues Feature\n\n## Checkliste\n\n- [x] Tests bestehen lokal (siehe Pre-Push-Nachweise unten)\n- [ ] Bei Bugfix: Reproduktionsnachweis erbracht — entfällt, kein Bugfix\n- [x] Dokumentation aktualisiert (falls zutreffend) — Feature-Spezifikation (`docs/features/spaces-and-assets.md`) enthielt die Anforderungen bereits vollständig; keine Änderung nötig\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet) — bereits unterzeichnet (früherer PR)\n\n## Pre-Push-Nachweise\n\n```\n$ ./gradlew spotlessCheck\nBUILD SUCCESSFUL\n\n$ ./gradlew build -x test\nBUILD SUCCESSFUL\n\n$ ./gradlew test\nBUILD SUCCESSFUL in 3m 27s\n(alle Tests grün, inkl. neuer DirectorySyncServiceIntegrationTest (9 Szenarien, echte\nPostgres/Liquibase-Schema-FKs) und Migration011DirectorySyncTest;\n1 vorbestehender, unveränderter Skip: OpenAiIntegrationTest, benötigt echten API-Key)\n\n$ npm run format && npm run lint\nalles unverändert / keine Findings\n\n$ npm run test -- --run\nTest Files  28 passed (28)\nTests  140 passed (140)\n\n$ npm run build\nBUILD SUCCESSFUL\n```\n\nFrontend ist von diesem PR nicht inhaltlich betroffen (kein `frontend`-Label, keine UI); alle Frontend-Checks wurden dennoch vollständig ausgeführt.\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code, Developer-Rolle)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
          "closes": [
            237
          ],
          "additions": 2634,
          "deletions": 1,
          "changed_files": 27
        },
        {
          "number": 283,
          "title": "Gruppen als Rechtesubjekt einfuehren",
          "url": "https://github.com/criew/opaa/pull/283",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "frontend",
            "size:M",
            "auth"
          ],
          "body": "## Zusammenfassung\n\nFuehrt Gruppen als zweiten Rechtesubjekt-Typ neben Nutzern ein, bevor der erste Grant existiert (#202) - ein nachtraeglich eingefuehrter Subjekttyp wuerde sonst jede Rechteabfrage, jeden Grant und jeden Cache beruehren.\n\n- `io.opaa.group` mit `Group`/`GroupMembership`: `Group.kind` unterscheidet `ORG_UNIT` (aus dem Verzeichnis, stabile `externalId` = objectGUID/SCIM-externalId statt Name, Elterneinheit) von `AD_HOC` (im System angelegt, ohne Kurator/Verteilungsstufe). Gruppen sind per zusammengesetztem Fremdschluessel strikt an eine Organisation gebunden (analog `space_memberships`).\n- `PermissionSubject` (User oder Group, mit `organizationId`) als gemeinsame Abstraktion fuer kuenftige Grants; `GroupMembershipResolver` loest die Gruppen eines Nutzers sowie die Nutzer eines Subjekts auf und cacht das Ergebnis (Caffeine, wie `RateLimitService`), mit Invalidierung nach Transaktions-Commit bei jeder Mitgliedschaftsaenderung. Beide Zweige von `resolveUserIds` (USER und GROUP) sind organisationsscharf.\n- `GroupService`/`GroupController` unter `/api/v1/admin/groups` (nur Systemadministratoren): Gruppen anlegen, umbenennen, loeschen, Mitglieder hinzufuegen/entfernen - beschraenkt auf `AD_HOC`-Gruppen; `ORG_UNIT`-Gruppen bleiben schreibgeschuetzt, bis die Verzeichnissynchronisation sie befuellt (#237).\n- Liquibase-Changelog `009` legt `groups` und `group_memberships` an; `Migration009CreateGroupsTest` belegt am realen Schema (nicht am Hibernate-generierten Test-Schema), dass die Organisationsgrenze durch die zusammengesetzte Fremdschluesselbeziehung tatsaechlich durchgesetzt wird.\n- Frontend: neue Seite „Gruppen\" unter `/admin/groups`, nur fuer Systemadministratoren im Menue sichtbar - Gruppen anlegen/umbenennen/loeschen, Mitglieder pflegen; `ORG_UNIT`-Gruppen werden als schreibgeschuetzt dargestellt.\n\n## Review-Runden\n\n**Runde 1** (Merge-Konflikt mit #280, Cache-Invalidierung vor statt nach Commit, `PermissionSubject` ohne Organisationsgrenze, diverse Nits) - behoben, siehe Kommentare unten.\n\n**Runde 2** (Nachpruefung) - zwei verbleibende Punkte behoben:\n- Der Integrationstest fuer die Cache-Invalidierung pruefte bislang nur „irgendwann vor der Assertion\", nicht „nach dem Commit\". Neuer Test `removingAMemberDoesNotLeaveAStaleCacheEntryFromAReaderDuringTheOpenTransaction` in `GroupServiceIntegrationTest` stellt die Race explizit nach: offene Transaktion via `PlatformTransactionManager`/`TransactionTemplate`, ein Leser-Thread liest waehrend die Transaktion offen ist, Assertion erst nach dem Commit.\n  **Reproduktionsnachweis** (gefordert seit der zwischenzeitlich gemergten AGENTS.md-Ergaenzung, siehe #295): Fix in `GroupService#removeMember` vorruebergehend auf die urspruengliche Inline-Invalidierung zurueckgesetzt (`membershipResolver.invalidateUser(memberUserId)` statt `invalidateAfterCommit(...)`), Test lief:\n  ```\n  GroupServiceIntegrationTest > removingAMemberDoesNotLeaveAStaleCacheEntryFromAReaderDuringTheOpenTransaction() FAILED\n  java.lang.AssertionError:\n  Expecting empty but was: [2ec42d3e-5d9c-444d-9fc8-3fdc887779c7]\n  ```\n  Fix wiederhergestellt, Test lief wieder gruen (siehe Pre-Push-Belege unten).\n- `GroupMembershipResolver#resolveUserIds` pruefte im `USER`-Zweig die Organisationsgrenze nicht, obwohl der Javadoc genau das versprach. Behoben: `USER`-Subjekte werden jetzt gegen `UserRepository` auf Zugehoerigkeit zur genannten Organisation geprueft (ein zusaetzlicher indizierter Zugriff auf dem heissen Pfad, bewusst in Kauf genommen, damit die Klasse ihr eigenes Versprechen haelt statt es an kuenftige Aufrufer in #202 zu delegieren).\n- Nit: `GroupMembershipRepository#findByUserIdAndGroupId` entfernt (unbenutzt).\n- Nit (asymmetrische DB-Grenze `users(id, organization_id)`): bewusst nicht in diesem PR behoben, siehe #289.\n\n## Zugehörige Issues\n\nCloses #200\nTeil von #198 (Stage A)\nFolge-Issue fuer eine DB-seitige Haertung, die dieser PR bewusst nicht mitnimmt: #289\n\n## Art der Änderung\n\n- [x] Neues Feature\n- [ ] Bugfix\n- [ ] Dokumentation\n- [ ] Refactoring\n- [ ] CI/Build\n\n## Annahmen\n\n- **Gruppenerstellung ist auf `AD_HOC` beschraenkt.** `ORG_UNIT`-Gruppen entstehen laut Spezifikation ausschliesslich durch die Verzeichnissynchronisation (#237); die Admin-API/-UI dieses PRs erstellt daher nur `AD_HOC`-Gruppen. `ORG_UNIT` ist im Datenmodell bereits vollstaendig angelegt (Kind, `externalId`, `parentGroupId`).\n- **„Loeschen einer Gruppe mit Asset-Eigentum wird blockiert\" ist noch nicht implementierbar.** Es gibt noch kein Asset-Modell (`io.opaa.indexing.Document` kennt keinen Eigentuemer) - #201/#202 fuehren Assets und Asset-Eigentuemerschaft erst ein. `GroupService#deleteGroup` traegt dafuer ein `TODO(#202)`.\n- **Gruppenmanagement ist system-adminweit, nicht curator-basiert.** Kuratoren an Organisationseinheiten sind #208; dieses PR haelt lediglich `parentGroupId` fuer die spaetere Eskalation vor.\n\n## Checkliste\n\n- [x] Tests bestehen lokal\n- [x] Dokumentation aktualisiert (falls zutreffend) - die Spezifikation (`docs/features/spaces-and-assets.md`, ADR-0008) beschreibt das Modell bereits vollstaendig; keine weiteren Aenderungen noetig\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet)\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Sonnet 5, Developer-Agent)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
          "closes": [
            200
          ],
          "additions": 3127,
          "deletions": 7,
          "changed_files": 30
        }
      ]
    },
    {
      "number": 224,
      "title": "Epic: Suchqualität messbar machen — Demo-Korpus und Retrieval-Regression",
      "tickets_total": 14,
      "tickets_closed": 6,
      "opened": [],
      "closed": [
        {
          "number": 228,
          "title": "ci(eval): Retrieval-Regressionsjob mit Baseline und Schwellenwerten",
          "url": "https://github.com/criew/opaa/issues/228",
          "author": "criew",
          "labels": [
            "enhancement",
            "size:M",
            "ci",
            "evaluation"
          ],
          "body": "## Zusammenfassung\n\nDer Retrieval-Harness läuft automatisiert in GitHub Actions gegen eine committete Baseline und schlägt fehl, wenn die Suchqualität abrutscht.\n\n## Kontext / Warum\n\nEin Harness, den niemand ausführt, verhindert keine Regression. Erst der automatisierte Lauf gegen eine feste Baseline macht Suchqualität zu einem Qualitätsgate.\n\nBewusst **nicht bei jedem Pull Request**: Der Lauf zieht ein Ollama-Modell (~275 MB) und bettet 1.000 Dokumente ein. Das ist zu teuer für jeden Push und würde nur dazu führen, dass der Job abgeschaltet wird.\n\nSpezifikation: `docs/features/search-quality-evaluation.md`, Abschnitte „Baseline und Fehlerkriterium\" und „Auslösung\". Entscheidung: ADR-0008.\n\n## Ziel / Ergebnis\n\nDanach fällt eine Verschlechterung des Retrievals automatisch auf, ohne dass ein Mensch danach sucht.\n\n## Umfang\n\n- Neuer Workflow-Job, ausgelöst durch: nächtlichen Zeitplan auf `main`, manuelle Auslösung (`workflow_dispatch`) und Label `evaluation` an einem Pull Request\n- Baseline als versioniertes JSON im Repository (`eval/baseline/comic-characters.json`)\n- Zweistufiges Fehlerkriterium: harte Untergrenze je Primärmetrik **und** maximale Verschlechterung gegenüber der Baseline (Vorschlag 0,03 absolut)\n- Verbesserungen schlagen nie fehl, erzeugen aber einen sichtbaren Hinweis zur Baseline-Aktualisierung\n- Report als CI-Artefakt mit Aufbewahrungsfrist\n- Bei Auslösung per PR-Label: Ergebnis als PR-Kommentar mit Delta-Tabelle gegenüber der Baseline\n\n## Außerhalb des Umfangs\n\n- Der Harness selbst (#227)\n- Gepaarte statistische Vergleichsläufe zwischen zwei Konfigurationen\n- Trendauswertung über die Zeit / Dashboard\n\n## Abnahmekriterien\n\n- [ ] Der Job läuft nächtlich auf `main`, per `workflow_dispatch` und per Label `evaluation` an einem Pull Request\n- [ ] Eine committete Baseline liegt im Repository und dokumentiert, mit welcher Konfiguration sie erzeugt wurde\n- [ ] Eine künstlich herbeigeführte Verschlechterung (z. B. `topK` auf 1 oder Ähnlichkeitsschwelle auf 0,9) lässt den Job nachweislich fehlschlagen\n- [ ] Eine Verbesserung lässt den Job bestehen und erzeugt einen Hinweis auf die veraltete Baseline\n- [ ] Die Fehlermeldung nennt Metrik, Baseline-Wert, Ist-Wert und Delta — nicht nur „failed\"\n- [ ] Der Job überschreitet 20 Minuten Laufzeit nicht\n- [ ] Der Job benötigt keine Secrets und läuft damit auch in Forks\n- [ ] Das Verfahren zur Baseline-Aktualisierung ist dokumentiert: wer darf sie ändern, mit welcher Begründung\n- [ ] Dokumentation aktualisiert (`AGENTS.md`/`CONTRIBUTING.md`: wann muss die Baseline neu gezogen werden)\n\n## Technische Hinweise\n\n- Modell-Cache in Actions prüfen, um den Ollama-Pull nicht bei jedem Lauf zu bezahlen.\n- Bei Auslösung per Label an einem PR muss klar sein, gegen welchen Korpus-Stand gemessen wird — die Manifest-Summe gehört in den Kommentar.\n\n## Betroffene Module\n\n`.github/workflows/`, `eval/`. Keine Änderung am Produktivcode.\n\n## Abhängigkeiten\n\nBlockiert durch #227.\n\n## Teil des Epics\n\n#224",
          "closes": []
        },
        {
          "number": 227,
          "title": "test(eval): Retrieval-Metrik-Harness (Hit Rate, MRR, nDCG, Recall)",
          "url": "https://github.com/criew/opaa/issues/227",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "size:L",
            "evaluation"
          ],
          "body": "## Zusammenfassung\n\nEin JUnit-Integrationstest indiziert den eingefrorenen Korpus über die produktive Pipeline, führt alle Golden-Queries gegen den Vektor-Store aus und berechnet Hit Rate@5, MRR, nDCG@10 und Recall@10.\n\n## Kontext / Warum\n\nOhne Zahl gibt es keine Aussage über Suchqualität. Der Harness ist das Werkzeug, das aus „fühlt sich besser an\" ein „Hit Rate@5 steigt von 0,81 auf 0,87\" macht.\n\nBewusst **ohne LLM**: Retrieval-Metriken sind reine Ranking-Metriken. Generationsmetriken (`RelevancyEvaluator`, `FactCheckingEvaluator`) folgen in einer späteren Phase und würden hier nur Laufzeit, Kosten und Rauschen einbringen.\n\nBewusst **nicht gegen ein laufendes System**: `QueryService` bündelt Suche und Antwortgenerierung, ein retrieval-only-Pfad existiert über die API nicht. Der Harness spricht `VectorStore.similaritySearch` direkt an und ist damit unabhängig von der Demo-Bereitstellung.\n\nSpezifikation: `docs/features/search-quality-evaluation.md`, Abschnitt „Retrieval-Harness und Regression in CI\". Metrikdefinitionen: `docs/discussions/discussion-rag-evaluation.md`, Abschnitt 2.1.\n\n## Ziel / Ergebnis\n\nDanach lässt sich die Retrieval-Qualität lokal mit einem Gradle-Aufruf messen und als Report ausgeben.\n\n## Umfang\n\n- Integrationstest im Backend, eigener Testtag/Task, damit er nicht Teil von `./gradlew build` ist\n- Testcontainers: `pgvector/pgvector:pg18` und Ollama mit `nomic-embed-text` (Modellversion festgenagelt)\n- Prüfung von `MANIFEST.sha256` vor dem Lauf; Abbruch bei Abweichung\n- Indizierung des Korpus über die produktive Pipeline (`FileProcessingService` / `ChunkingService`), nicht über einen Sonderweg\n- Abbildung der Treffer auf Entitäten über das Chunk-Metadatum `file_name`\n- Metriken: Hit Rate@5, MRR, nDCG@10, Recall@10 — gesamt und aufgeschlüsselt nach Kategorie, Schwierigkeit und Sprache\n- Report als maschinenlesbares JSON plus lesbare Zusammenfassung\n\n## Außerhalb des Umfangs\n\n- CI-Integration und Baseline-Vergleich (#228)\n- Generationsmetriken, RAGAS-Sidecar\n- Gepaarte Bootstrap-Vergleiche zwischen zwei Konfigurationen\n\n## Abnahmekriterien\n\n- [ ] Der Harness läuft lokal über einen eigenen Gradle-Task durch und erzeugt einen Report\n- [ ] `./gradlew build` wird durch den Harness **nicht** verlangsamt (er ist ausgeschlossen)\n- [ ] Ein manipuliertes Korpus-Byte führt zum Abbruch mit klarer Fehlermeldung (Manifest-Prüfung)\n- [ ] Alle vier Metriken werden gesamt und je Kategorie, Schwierigkeit und Sprache ausgewiesen\n- [ ] Der Report enthält die Konfiguration des Laufs: Embedding-Modell und -Version, `chunkSize`, `topK`, Ähnlichkeitsschwelle, Korpus-Manifest-Summe, Golden-Dataset-Version\n- [ ] Zwei aufeinanderfolgende Läufe auf demselben Rechner liefern Metriken, die um weniger als 0,01 absolut voneinander abweichen (Nachweis der Stabilität; falls nicht erreichbar, ist die HNSW-Approximation als Ursache zu dokumentieren)\n- [ ] Der Report weist die schlechtesten 10 Anfragen namentlich aus — der Harness soll Debugging ermöglichen, nicht nur eine Note vergeben\n- [ ] Dokumentation aktualisiert: wie führt man ihn aus, wie liest man den Report\n\n## Technische Hinweise\n\n- Der pgvector-Indextyp ist in `application.yml` fest auf `hnsw` gesetzt (siehe #77). Approximative Suche kann Ranglisten leicht schwanken lassen. Falls das Stabilitätskriterium nicht erreichbar ist: Indextyp für die Evaluierung konfigurierbar machen oder exakte Suche verwenden — dann bitte im Issue vermerken.\n- Chunk-Metadaten enthalten heute nur `document_id`, `chunk_index` und `file_name`. `file_name` ist deshalb der Schlüssel für die Zuordnung; die Dateinamen aus #225 sind eindeutig.\n- Das Einbettungsmodell ist gemäß ADR-0008 Ollama, nicht OpenAI: kostenlos, ohne Secret, auch in Forks lauffähig und über die Modellversion reproduzierbar.\n\n## Betroffene Module\n\n`backend/` (Testquellen, Gradle-Build), `eval/`. Keine Änderung an der OpenAPI-Spezifikation, keine Änderung am Produktivcode erwartet.\n\n## Abhängigkeiten\n\nBlockiert durch #225 und #226.\n\n## Teil des Epics\n\n#224",
          "closes": []
        }
      ],
      "merged": [
        {
          "number": 301,
          "title": "ci(eval): Retrieval-Regressionsjob mit Baseline und Schwellenwerten",
          "url": "https://github.com/criew/opaa/pull/301",
          "author": "criew",
          "labels": [
            "enhancement",
            "ci",
            "evaluation"
          ],
          "body": "## Zusammenfassung\n\nFührt den in #227 gebauten Retrieval-Harness als automatisierten CI-Job gegen eine committete Baseline (`eval/baseline/comic-characters.json`). Der Job vergleicht jeden Lauf über `io.opaa.eval.BaselineComparator` mit der Baseline und trennt dabei zwei Fragen bewusst voneinander:\n\n1. **Ist die Baseline überhaupt noch gültig?** Fixpunkte (Korpus-Manifest, Golden-Dataset-Hash, Embedding-Modell-Digest, Chunk-Größe, Messvertrag-Version) werden exakt verglichen. Weicht auch nur einer ab, meldet der Job **\"Baseline ungültig, Messgrundlage geändert\"** und vergleicht keine einzige Metrik — das ist ausdrücklich keine Aussage über eine Retrieval-Regression.\n2. **Nur wenn die Baseline gültig ist:** Jede Gruppe (gesamt, je Kategorie/Schwierigkeit/Sprache) wird gegen eine gruppenspezifische Toleranz geprüft, die vier Gesamtmetriken zusätzlich gegen eine baseline-unabhängige harte Untergrenze.\n\nDie Ein-Chunk-Invariante (ADR-0010) bleibt ein harter, toleranzfreier Fehlschlag.\n\n### Toleranzformel je Gruppe und Metrik\n\n```\ntoleranz = min( max(0.12 · Baselinewert, 1 / n, 0.02), 0.05 )\n```\n\nBegründung (ausführlich in `eval/baseline/README.md`):\n\n- Eine einzelne absolute Toleranz taugt nicht: 0,05 ist bei `attribute_lookup` (nDCG@10 0,942) Rauschen, bei `numeric_range` (nDCG@10 0,063) fast der gesamte Messwert.\n- Eine einzelne relative Toleranz taugt ebenfalls nicht: Bei Werten nahe null kollabiert sie gegen null.\n- Der `1/n`-Term sorgt dafür, dass **kleine** Gruppen (`numeric_range`, n=16) eine **größere**, nicht kleinere Toleranz bekommen — ein einzelner kippender Fall verschiebt eine 16er-Gruppe um bis zu 0,0625, eine 121er-Gruppe nur um 0,008.\n- Absolute Unter- (0,02) und Obergrenze (0,05) verhindern Extreme an beiden Enden.\n- So eng gewählt, **weil** drei unabhängige Läufe (zwei des Autors, einer des Reviewers auf fremder Hardware) byte-identische Metriken lieferten — die in ADR-0011 genannte HNSW-Approximationsschwankung tritt bei dieser Korpusgröße nicht auf. Eine weitere Toleranz würde bei nachgewiesener Stabilität keinen zusätzlichen Schutz kaufen, sondern nur echte Regressionen durchlassen.\n\nZusätzlich eine von der Baseline unabhängige harte Untergrenze für die vier **Gesamt**-Metriken (Hit@5 ≥ 0,30, MRR ≥ 0,25, nDCG@10 ≥ 0,25, Recall@10 ≥ 0,25) als zweites Fangnetz gegen katastrophales Versagen (z. B. leerer Vektor-Store), unabhängig von der Baseline-Datei selbst.\n\n### Baseline\n\n`eval/baseline/comic-characters.json`, gemessen am 2026-08-03 mit dem gepinnten Modell `nomic-embed-text:v1.5` (Digest `0a109f42…`), Laufzeit 1004 s, Gesamt-nDCG@10 0,445. Ein früherer Bericht nannte 0,463 — der stammte vom ungepinnten `:latest` und ist hinfällig; die Verschiebung durch das Pinning ist der empirische Beleg, warum das Pinning nötig war (siehe `eval/baseline/README.md`).\n\n### Betrieb\n\n- **Auslöser:** nächtlich auf `main` (Cron), `workflow_dispatch`, und Label `evaluation` an einem PR — bewusst **nicht** bei jedem PR (ADR-0011, Entscheidung 5).\n- **Modell-Cache:** `actions/cache` rundet den Docker-Volume-Inhalt (`opaa-eval-ollama-models`) als Tar-Archiv über ephemere Runner hinweg — der Cache-Schlüssel ist an den gepinnten Modell-Digest gebunden, ein bewusster Re-Pin invalidiert ihn automatisch. Ohne das würde jeder nächtliche Lauf ~275 MB neu von `ollama.com` ziehen und gegen das netzfreie Betriebsversprechen aus ADR-0011 verstoßen.\n- **Fehlschlag-Sichtbarkeit:** Bei Label-Auslösung wird die Delta-Tabelle als PR-Kommentar gepostet. Beim nächtlichen/manuellen Lauf legt ein Fehlschlag ein GitHub-Issue (Label `evaluation`, `bug`) an bzw. aktualisiert ein bestehendes; ein erneutes Bestehen schließt es automatisch mit Kommentar.\n- **Baseline-Aktualisierung:** dokumentiert in `eval/baseline/README.md` — wer (wer auch immer die verursachende Änderung einreicht, im selben PR), womit begründet (ein tatsächlicher `evaluateRetrieval`-Lauf, Verlinkung/Anhang des Reports, explizite Begründung bei Verschlechterung).\n\n### Verifikation ohne Docker\n\nDa der reale Lauf Docker/Testcontainers braucht (~17 Min), wurde die Vergleichslogik lokal gegen synthetische Reports verifiziert (`checkRetrievalBaseline` mit temporär entferntem `dependsOn(evaluateRetrieval)`):\n\n- Report == Baseline → **PASSED**, Delta-Tabelle zeigt `+0.000` überall.\n- Künstliche Verschlechterung (`overall.hitRateAt5=0.30`, `ndcgAt10=0.20`) → **FAILED** mit `overall hitRateAt5 Baseline=0.521 Ist=0.300 Delta=-0.221 Toleranz=0.050 [TOLERANZ VERLETZT]` und zusätzlich `[UNTERGRENZE VERLETZT]` für nDCG.\n- Manipulierter `corpusManifestSha256` → **FAILED** mit separater \"Baseline ungültig\"-Meldung samt Feld-Diff-Tabelle, keine Metrik verglichen.\n- Künstliche Verbesserung → **PASSED** mit Hinweis, die Baseline-Aktualisierung zu prüfen.\n\n## Zugehörige Issues\n\nCloses #228\n\n## Art der Änderung\n\n- [ ] Bugfix\n- [x] Neues Feature\n- [x] Dokumentation\n- [ ] Refactoring\n- [x] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal (`./gradlew spotlessCheck build`, `evalUnitTest` inkl. neuer `BaselineComparatorTest`; Frontend unverändert, `format:check`/`lint`/`test`/`build` zur Sicherheit trotzdem grün)\n- [ ] Bei Bugfix: Reproduktionsnachweis erbracht\n- [x] Dokumentation aktualisiert (`eval/README.md`, `eval/baseline/README.md`, `CONTRIBUTING.md`)\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [x] CLA unterzeichnet (bereits vom Maintainer/Betreiber unterzeichnet)\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code / QA Engineer, Sonnet 5)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
          "closes": [
            228
          ],
          "additions": 1994,
          "deletions": 14,
          "changed_files": 16
        },
        {
          "number": 292,
          "title": "test(eval): Retrieval-Metrik-Harness (Hit Rate, MRR, nDCG, Recall)",
          "url": "https://github.com/criew/opaa/pull/292",
          "author": "criew",
          "labels": [
            "enhancement",
            "backend",
            "evaluation"
          ],
          "body": "## Zusammenfassung\n\nEin JUnit-Integrationstest indiziert den eingefrorenen Korpus (`eval/corpus/comic-characters/`) über die produktive Indizierungs-Pipeline (`FileProcessingService`/`ChunkingService`) und führt jede Anfrage aus dem Golden Dataset (`eval/golden/comic-characters.json`) direkt gegen `VectorStore.similaritySearch(topK=10)` aus — ohne LLM, per ADR-0011 Entscheidung 3. Testcontainers: `pgvector/pgvector:pg18` und `ollama/ollama:0.6.5` mit `nomic-embed-text` (768 Dimensionen).\n\nDer Harness:\n\n- prüft `MANIFEST.sha256` gegen den Korpus vor jedem Lauf und bricht bei jeder Abweichung mit einer benannten Fehlermeldung ab (ADR-0011)\n- verifiziert nach dem Indizieren die **Ein-Chunk-Invariante** aus ADR-0010 — jedes Korpusdokument muss über den echten, produktiv konfigurierten `TokenTextSplitter` genau einen Chunk ergeben — und benennt Verletzungen namentlich\n- berechnet Hit Rate@5, MRR, nDCG@10 und Recall@10, gesamt sowie je Kategorie, Schwierigkeit und Sprache\n- schreibt einen maschinenlesbaren JSON-Report (`backend/build/eval-reports/retrieval-metrics.json`) mit Lauf-Konfiguration (Embedding-Modell/-Version, `chunkSize`, `topK`, Korpus-Manifest-Hash, Golden-Dataset-Hash) sowie eine lesbare Konsolen-Zusammenfassung inklusive der zehn schlechtesten Anfragen\n\n## Auslösemechanismus\n\nEigenes Gradle-Source-Set (`backend/src/evalTest/`) mit eigenem Task (`./gradlew evaluateRetrieval`), bewusst **nicht** an `build`/`check`/`test` angehängt (per `git grep`/`--dry-run` verifiziert). Begründung: Der Lauf braucht Docker, zieht beim ersten Mal ein ~275 MB Embedding-Modell und indiziert danach ~1.450 Dokumente über echte Ollama-Embeddings — das würde jeden normalen `./gradlew build` unbeabsichtigt verlangsamen und in Umgebungen ohne Docker/Netz hart brechen lassen. Ein eigenes Source-Set (statt z. B. eines JUnit-Tags mit `excludeTags`) hält die Trennung auch auf Klassenpfad-Ebene sauber und ist das etablierte Gradle-Muster für solche \"zweiten Testsuiten\".\n\n## Gemessene Ergebnisse (1.448 Dokumente, 121 Golden-Fälle)\n\nVoller Lauf: **~18 Minuten** (Indizierung + Auswertung), Ein-Chunk-Invariante hält für **alle 1.448 Dokumente ohne Ausnahme**.\n\n| Kategorie | n | Hit Rate@5 | MRR | nDCG@10 | Recall@10 |\n|---|---|---|---|---|---|\n| Gesamt | 121 | 0.537 | 0.474 | 0.463 | 0.500 |\n| `attribute_lookup` | 30 | 1.000 | 0.967 | 0.975 | 1.000 |\n| `entity_description` | 20 | 0.650 | 0.502 | 0.550 | 0.700 |\n| `crosslingual` | 34 | 0.412 | 0.364 | 0.348 | 0.358 |\n| `multi_attribute_filter` | 21 | 0.238 | 0.206 | 0.137 | 0.159 |\n| `numeric_range` | 16 | 0.188 | 0.101 | 0.063 | 0.060 |\n\nJe Sprache: Englisch (n=87) nDCG@10=0.507, Deutsch (n=34, alles `crosslingual`-Zwillinge) nDCG@10=0.348. Je Schwierigkeit: `easy` nDCG@10=0.982, `medium` 0.252, `hard` 0.140.\n\nDas entspricht genau dem in ADR-0011/der Feature-Spezifikation erwarteten Muster: reines Attribut-Lookup (eine Entität, ein exaktes Faktenfeld) funktioniert nahezu perfekt, während Mengen-Fragen (`multi_attribute_filter`, `numeric_range`) — der Fall, an dem reine Vektorsuche bricht — deutlich schlechter abschneiden. Die Baseline und Schwellenwerte für eine CI-Regression sind bewusst nicht Teil dieses PRs, siehe #228.\n\n## Kalibrierungshinweise (im Report abgebildet)\n\n- Der Korpus ist absichtlich uniform (Jaccard-Median 0,51 über ganze Dokumente laut Review zu #225/#226) — das staucht die Score-Verteilung und macht Hit Rate/MRR unempfindlicher gegenüber echten Regressionen. Künftige Schwellenwerte (#228) müssen gegen diese tatsächliche Verteilung kalibriert werden.\n- Die Fallzahl überschätzt die Zahl unabhängiger Beobachtungen: 121 Fälle teilen sich nur 75 unterschiedliche Erwartungsmengen, u. a. weil jeder `crosslingual`-Fall konstruktionsbedingt der deutsche Zwilling eines englischen Falls ist. Der Report weist das explizit über `datasetNotes` aus.\n\n## Zugehörige Issues\n\nCloses #227\n\n## Art der Änderung\n\n- [ ] Bugfix\n- [x] Neues Feature\n- [ ] Dokumentation\n- [ ] Refactoring\n- [ ] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal (`./gradlew evaluateRetrieval`, zweimal ausgeführt; `./gradlew build` unverändert grün und unbeeinflusst)\n- [x] Dokumentation aktualisiert (`eval/README.md`: wie der Harness ausgeführt wird, wie der Report zu lesen ist)\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet)\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code, Rolle QA Engineer)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
          "closes": [
            227
          ],
          "additions": 1661,
          "deletions": 6,
          "changed_files": 15
        }
      ]
    }
  ],
  "ohne_epic": {
    "opened": [
      {
        "number": 310,
        "title": "fix(api): GlobalExceptionHandler mappt ResponseStatusException und DataIntegrityViolationException nicht",
        "url": "https://github.com/criew/opaa/issues/310",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:S"
        ],
        "body": "## Kontext\n\n`io.opaa.api.GlobalExceptionHandler` behandelt `MethodArgumentNotValidException`, `HttpMessageNotReadableException`, `IllegalArgumentException`, `TransientAiException`, `NonTransientAiException`, `AccessDeniedException` — und alles Übrige über `@ExceptionHandler(Exception.class)` als HTTP 500.\n\nZwei häufige Ausnahmen fallen damit auf den Catch-all:\n\n- **`ResponseStatusException`** — der im Projekt durchgängig verwendete Weg, einen Statuscode zu setzen (`SpaceService`, `KnowledgeLibraryService`, `GroupService`). Der gesetzte Status wird nicht übernommen.\n- **`DataIntegrityViolationException`** — jede Verletzung eines Fremdschlüssels oder einer Unique-Constraint, die die Anwendung nicht selbst abfängt.\n\n## Warum das wichtig ist\n\nBeim Review von PR #309 fiel auf: Eine Gruppe, die einen Grant auf eine fremde Bibliothek hält, ließ sich nicht löschen — statt einer verständlichen Meldung kam „Interner Serverfehler\". Derselbe Effekt trat in PR #305 zweimal auf (Bibliothek mit Dokumenten, Gruppe mit eigener Bibliothek).\n\nJedes Mal wurde es an der Aufrufstelle mit einem gezielten Guard behoben, was richtig ist. Die Lücke dahinter bleibt aber bestehen: **Jeder künftige `RESTRICT`-Fremdschlüssel erzeugt denselben 500er**, bis jemand daran denkt. Das Rechtemodell führt laufend neue solche Beziehungen ein.\n\nEin 500er verschleiert zudem die Ursache. Ein Nutzer, der eine Gruppe nicht löschen kann, weil sie noch Rechte hält, soll das erfahren — nicht „Interner Serverfehler\" lesen.\n\n## Umsetzung\n\n- `ResponseStatusException` mit ihrem gesetzten Status und ihrer Meldung durchreichen\n- `DataIntegrityViolationException` auf **409 Conflict** mit einer verständlichen deutschen Meldung abbilden. Die technische Ursache (Constraint-Name) gehört ins Log, nicht in die Antwort\n- Prüfen, ob die Abbildung zu grob ist: Nicht jede Integritätsverletzung ist ein Konflikt im Sinne des Aufrufers. Falls nötig, nach Constraint-Art unterscheiden\n\nDie gezielten Guards an den Aufrufstellen bleiben bestehen — sie liefern die bessere Meldung. Diese Änderung ist das Sicherheitsnetz darunter.\n\n## Abnahmekriterien\n\n- [ ] `ResponseStatusException` behält Status und Meldung\n- [ ] Eine unbehandelte `DataIntegrityViolationException` ergibt 409 statt 500, mit verständlicher Meldung\n- [ ] Der Constraint-Name erscheint im Log, nicht in der Antwort\n- [ ] Tests für beide Fälle\n- [ ] Bestehende Guards und ihre Meldungen bleiben unverändert wirksam\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nVorbestehend, gefunden bei den Reviews zu PR #305 und PR #309.",
        "closes": []
      },
      {
        "number": 308,
        "title": "test(backend): GroupServiceIntegrationTest auf echtes Liquibase-Schema umstellen",
        "url": "https://github.com/criew/opaa/issues/308",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:S"
        ],
        "body": "## Kontext\n\n`GroupServiceIntegrationTest` läuft weiterhin mit `spring.liquibase.enabled=false` und `ddl-auto=create-drop`. In #288 wurden nur die Space-Suiten umgestellt.\n\nFolge, gefunden beim Review von PR #305: Der neue Test `cannotDeleteAGroupThatStillOwnsALibrary` sichert den Guard in `GroupService.deleteGroup` ab — kann aber den Fall, gegen den der Guard schützt, **strukturell nicht prüfen**. Ohne den Guard schlägt er mit `AssertionError: Expecting code to raise a throwable` fehl, nicht mit der erwarteten Fremdschlüsselverletzung, weil `fk_knowledge_libraries_owner_group_organization` im Hibernate-Schema gar nicht existiert.\n\nDer Guard selbst ist korrekt. Nur ist der Test kein Beleg dafür, dass der 500er behoben ist.\n\n## Warum jetzt\n\nDies ist derselbe blinde Fleck, der in #254 und #280 je eine Regression durchgelassen hat. #202 führt die Rechteprüfung samt Grants ein und wird weitere Fremdschlüssel auf `groups` anlegen — die wären in dieser Suite ebenfalls ungetestet.\n\n## Umsetzung\n\nUmstellung auf `spring.liquibase.enabled=true` und `ddl-auto=none`, `TestcontainersConfiguration` per `@Import`. Muster: `SpaceServiceIntegrationTest` und `SpaceRepositoryTest` (aus #288), sowie `UserServicePersonalSpaceIntegrationTest`.\n\nZu beachten: Bei geteiltem Kontext und echten Fremdschlüsseln greifen pauschale `deleteAll()`-Bereinigungen nicht mehr. In #305 wurde dafür gezielt nach eigenen IDs aufgeräumt statt pauschal zu löschen — das ist die sauberere Variante und sollte hier übernommen werden.\n\n## Abnahmekriterien\n\n- [ ] `GroupServiceIntegrationTest` läuft gegen das echte Liquibase-Schema\n- [ ] `cannotDeleteAGroupThatStillOwnsALibrary` schlägt ohne den Guard mit der **Fremdschlüsselverletzung** fehl, nicht mit einer fehlenden Exception — nachweisen\n- [ ] Keine Datenreste zwischen Testklassen im geteilten Kontext\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nFolge aus #288 und PR #305. Sollte vor #202 abgeschlossen sein.",
        "closes": []
      },
      {
        "number": 307,
        "title": "fix(auth): Gleichzeitige Erstanmeldungen verschiedener Nutzer erschöpfen den Connection-Pool",
        "url": "https://github.com/criew/opaa/issues/307",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:M",
          "auth"
        ],
        "body": "## Kontext\n\nBei **12 gleichzeitigen Erstanmeldungen verschiedener Nutzer** scheitern die Requests bei der Standard-Poolgröße von 10 nach 30 Sekunden mit `Connection is not available`.\n\nGemessen beim Review von PR #305, sowohl auf dem PR-Stand als auch auf dem Basis-Commit `aab3eda` — jeweils 3 von 3 Läufen rot, gleiche Signatur. **Vorbestehend, nicht durch #305 eingeführt.**\n\nAufschlussreich ist der Zustand während des Hängers: `pg_stat_activity` zeigt **keine einzige aktive Session**. Die Datenbank ist unbeschäftigt, alle Anwendungs-Threads warten auf eine Connection aus dem Pool.\n\n## Warum das wichtig ist\n\nDer Fall ist nicht konstruiert, sondern der Regelfall bei der Einführung: Eine Behörde schaltet OPAA frei, dreißig Beschäftigte melden sich am Montagmorgen zum ersten Mal an.\n\nSchlimmer als der Fehler selbst ist die Wirkung: **Der Login meldet Erfolg, während persönlicher Space und Bibliothek fehlen.** `UserService.ensureBothPersonalAssets` protokolliert Provisionierungsfehler bewusst, statt sie zu werfen (#294, PR #305) — richtig so, sonst wäre der Nutzer ausgesperrt. Die Folge ist aber ein stiller Defekt, der erst beim nächsten unbelasteten Login heilt und bis dahin niemandem auffällt.\n\nDas in PR #305 eingeführte Striped Lock hilft hier nicht: Es serialisiert je Nutzer und entlastet nur den Fall vieler gleichzeitiger Anmeldungen **desselben** Nutzers.\n\n## Zu klären\n\nDie Ursache ist noch nicht verstanden. Die Datenbank ist idle, die Threads warten trotzdem — es geht also um die Anzahl gleichzeitig gehaltener Connections je Request, nicht um Datenbanklast.\n\n- Wie viele Connections hält ein Login-Request gleichzeitig, und über welche Dauer? Zwei `ensure*`-Aufrufe mit je eigener Transaktion, dazu die Requestverarbeitung.\n- Reicht es, die Provisionierung aus dem Login-Request zu lösen — etwa asynchron nach der Antwort, oder beim ersten Zugriff, der sie tatsächlich braucht?\n- Ist die Standard-Poolgröße von 10 für den erwarteten Rollout überhaupt angemessen? Falls sie steigen soll, gehört das mit Begründung in `application.yml`, nicht in eine Testklasse (das wurde in PR #305 bereits einmal als untauglich verworfen).\n- Sollte ein fehlgeschlagener Provisionierungsversuch sichtbar werden, statt nur protokolliert? Das ist die in #294 offen gelassene Metrik-Frage — beide Issues hängen zusammen.\n\n## Abnahmekriterien\n\n- [ ] 12 gleichzeitige Erstanmeldungen verschiedener Nutzer laufen bei der konfigurierten Produktions-Poolgröße durch\n- [ ] Nach dem Lauf hat **jeder** Nutzer Space und persönliche Bibliothek — Datenbankzustand prüfen, nicht nur den Testausgang\n- [ ] Test mit echten Threads gegen Postgres mit Liquibase-Schema, mehrere Läufe (der Fehler tritt intermittierend auf)\n- [ ] Reproduktionsnachweis: Der Test schlägt ohne den Fix fehl\n- [ ] Ursache dokumentiert, nicht nur behoben\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nGefunden beim Review von PR #305. Hängt mit #294 zusammen (Sichtbarkeit fehlgeschlagener Provisionierung).",
        "closes": []
      },
      {
        "number": 306,
        "title": "eval(baseline): Fallzahlbasierte Regressionsprüfung für Paare mit Toleranz < 1/n",
        "url": "https://github.com/criew/opaa/issues/306",
        "author": "criew",
        "labels": [
          "size:M",
          "ci",
          "evaluation"
        ],
        "body": "## Kontext / Warum\n\nAus der zweiten Review-Runde zu PR #301 (Issue #228, ADR-0013, Abschnitt „Offen\"): Die Mittelwert-Toleranz (`BaselineComparator.toleranceFor`) schützt sechs Metrik/Gruppen-Paare nicht zuverlässig gegen einen einzelnen kippenden Fall, weil deren Toleranz enger ist als die Verschiebung, die ein einzelner Fall erzeugt (`1/n`):\n\n| Paar | Toleranz | 1/n | Verhältnis |\n|---|---|---|---|\n| `numeric_range` / `recallAt10` | 0,0150 | 0,0625 | 0,24 |\n| `numeric_range` / `ndcgAt10` | 0,0158 | 0,0625 | 0,25 |\n| `numeric_range` / `mrr` | 0,0253 | 0,0625 | 0,40 |\n| `multi_attribute_filter` / `ndcgAt10` | 0,0343 | 0,0476 | 0,72 |\n| `numeric_range` / `hitRateAt5` | 0,0470 | 0,0625 | 0,75 |\n| `multi_attribute_filter` / `recallAt10` | 0,0398 | 0,0476 | 0,83 |\n\nFür `numeric_range`s nDCG@10 genügt es bereits, dass eine einzige der 16 Anfragen von Rang 1 auf Rang 3 rutscht — kein verlorener Treffer nötig —, um die Toleranz mehr als doppelt zu reißen.\n\nDer Report führt in `allQueryResults` bereits jeden Einzelfall mit seinen vier Metriken. Eine fallzahlbasierte Prüfung („die Zahl der Fälle mit z. B. `ndcgAt10 > 0` darf um höchstens 1 sinken\") wäre exakt, bräuchte keine neue Kalibrierungsevidenz und wäre strenger als die bestehende 25-%-Deckelung — billiger als zunächst angenommen.\n\n## Ziel / Ergebnis\n\nDie sechs betroffenen Metrik/Gruppen-Paare sind gegen einen einzelnen falsch-positiven Fehlschlag ebenso robust wie die übrigen, ohne die Mittelwert-Toleranz für alle anderen Paare zu verändern.\n\n## Abnahmekriterien\n\n- [ ] Entschieden und in ADR-0013 (oder einem Nachtrag) begründet: fallzahlbasierte Prüfung ausschließlich für Paare mit `Toleranz < 1/n`, oder ein anderer, ebenso begründeter Ansatz\n- [ ] `BaselineComparator` (oder eine neue, dedizierte Klasse) implementiert die gewählte Prüfung unter Verwendung von `allQueryResults`\n- [ ] Unit-Test reproduziert das Szenario „ein Fall kippt in `numeric_range`\" und zeigt, dass es nicht mehr fälschlich fehlschlägt, während eine echte Verschlechterung (mehrere Fälle) weiterhin erkannt wird\n- [ ] `eval/baseline/README.md` und `BaselineComparator`-Javadoc aktualisiert\n\n## Umfang\n\n`backend/src/evalTest/java/io/opaa/eval/` (`BaselineComparator`, ggf. neue Klasse), `docs/decisions/0013-fehlerkriterium-retrieval-regression.md`, `eval/baseline/README.md`\n\n## Außerhalb des Umfangs\n\nDie Mittelwert-Toleranzformel für alle anderen (nicht betroffenen) Metrik/Gruppen-Paare bleibt unverändert.\n\n## Abhängigkeiten\n\nBaut auf #228 (Retrieval-Regressionsjob, gemergt) und ADR-0013 auf.",
        "closes": []
      },
      {
        "number": 304,
        "title": "eval(golden): category:crosslingual und language:de sind identische Fallmengen",
        "url": "https://github.com/criew/opaa/issues/304",
        "author": "criew",
        "labels": [
          "size:S",
          "evaluation"
        ],
        "body": "## Kontext / Warum\n\nAus dem Review zu PR #301 (Issue #228): Jeder \\`crosslingual\\`-Fall im Golden Dataset (\\`eval/golden/comic-characters.json\\`) ist konstruktionsbedingt eine deutsche Frage, und alle \\`crosslingual\\`-Fälle sind gleichzeitig alle \\`de\\`-Fälle. \\`category:crosslingual\\` und \\`language:de\\` sind damit exakt dieselbe Teilmenge (34 Fälle, 33 unterschiedliche Erwartungsmengen) — im Retrieval-Regressionsjob (#228) erzeugt das acht statt vier Prüfungen über identische Daten und suggeriert eine breitere Abdeckung, als tatsächlich besteht.\n\nHerkunft: \\`eval/generator/generate_golden_dataset.py\\` (Issue #226), nicht #228 selbst — siehe ADR-0013, Abschnitt „Offen\".\n\n## Ziel / Ergebnis\n\nEntschieden und dokumentiert, ob \\`category:crosslingual\\` und \\`language:de\\` als separate Baseline-Gruppen bestehen bleiben, obwohl sie identisch sind — oder ob der Generator künftig auch nicht-deutsche \\`crosslingual\\`-Fälle erzeugt (sodass die beiden Gruppen sich tatsächlich unterscheiden), oder ob eine der beiden Gruppen aus der Baseline entfernt wird.\n\n## Abnahmekriterien\n\n- [ ] Entscheidung dokumentiert (ADR-0013 oder \\`eval/golden/README.md\\`): getrennte Gruppen trotz Identität, Generator-Erweiterung, oder Konsolidierung\n- [ ] Falls Generator-Erweiterung: \\`eval/generator/generate_golden_dataset.py\\` erzeugt \\`crosslingual\\`-Fälle auch für mindestens eine weitere Sprache oder Domäne, \\`eval/baseline/comic-characters.json\\` neu gezogen\n- [ ] Falls Konsolidierung: eine der beiden Baseline-Gruppen entfernt, \\`BaselineComparator\\`/\\`eval/baseline/README.md\\` entsprechend angepasst\n\n## Umfang\n\n\\`eval/generator/\\`, \\`eval/golden/\\`, ggf. \\`eval/baseline/comic-characters.json\\`, \\`docs/decisions/0013-fehlerkriterium-retrieval-regression.md\\`\n\n## Außerhalb des Umfangs\n\nDer Retrieval-Regressionsjob selbst (#228) und der Harness (#227) — beide funktionieren unabhängig davon, ob diese beiden Gruppen identisch sind.\n\n## Abhängigkeiten\n\nKeine blockierenden Abhängigkeiten. Bezieht sich auf #226 (Golden-Dataset-Generator) und #228 (Regressionsjob, wo die Redundanz zuerst auffiel).",
        "closes": []
      },
      {
        "number": 302,
        "title": "docs(agents): Umgang mit Transaktionen in die Entwickler-Rollendefinition aufnehmen",
        "url": "https://github.com/criew/opaa/issues/302",
        "author": "criew",
        "labels": [
          "documentation",
          "size:S"
        ],
        "body": "## Kontext\n\nIn Stufe A von #198 hat dieselbe Konstruktion dreimal einen Fehler verursacht, jedes Mal fachlich falscher Code bei grünem CI, jedes Mal erst im Review gefunden:\n\n| PR | Konstruktion | Fehler |\n|---|---|---|\n| #280 | `REQUIRES_NEW` innerhalb einer offenen Transaktion | Eigene Connection sah die noch nicht committete `users`-Zeile nicht, `fk_spaces_owner` schlug fehl — **jede Erstanmeldung** war gebrochen |\n| #297 | `REQUIRES_NEW` für einen Statusschreiber | Committete vor der Hauptransaktion und meldete `APPLIED` für eine Anwendung, die zurückgerollt wurde |\n| #299 | `REQUIRES_NEW` neben einer haltenden äußeren Transaktion | Zwei Pool-Connections gleichzeitig je paralleler Anfrage; ab Pool-Größe Stillstand für 30 Sekunden, danach Fehler — auch für unbeteiligte Requests |\n\nDazu kam in #297 ein verwandter Fall: Eine Planberechnung unter `@Transactional(readOnly = true)` behauptete im Javadoc, „nichts wurde geschrieben\" sei strukturell garantiert — der `save()`-Aufruf stand im Code und wurde nur vom Flush-Modus geschluckt.\n\nIn allen Fällen war die Absicht richtig und die Nebenwirkung überraschend. Und in allen Fällen war die beste Lösung, die Konstruktion **ganz zu vermeiden**, statt sie zu reparieren.\n\n## Warum in die Rollendefinition und nicht in AGENTS.md\n\nEs ist Handwerkswissen des Entwicklers, keine Projektkonvention. Entscheidung des Maintainers.\n\n## Umsetzung\n\nEinen Abschnitt zu Transaktionen in `agents/roles/developer.md` aufnehmen, sinnvollerweise zwischen „Testschutz\" und „Umfang und Blocker\". Inhalt:\n\n- Vor dem Einsatz einer eigenen Transaktion (`REQUIRES_NEW`, `TransactionTemplate`) prüfen, ob sie wirklich gebraucht wird — sie ist in diesem Projekt dreimal zur Fehlerquelle geworden.\n- Wenn sie gebraucht wird, beide Fehlerrichtungen prüfen und im PR benennen: Was passiert bei Fehlschlag der inneren, was bei Fehlschlag der äußeren Transaktion.\n- Sichtbarkeit: Eine eigene Transaktion sieht nichts, was die umgebende noch nicht committet hat.\n- Commit-Reihenfolge: Eine innere Transaktion committet vor der äußeren. Was sie schreibt, überlebt deren Rollback.\n- Ressourcen: Eine innere Transaktion neben einer haltenden äußeren belegt zwei Connections gleichzeitig. Bei nebenläufigen Aufrufen skaliert das mit der Anzahl paralleler Anfragen.\n- `@Transactional(readOnly = true)` verhindert Schreibzugriffe nicht strukturell, sondern schluckt sie über den Flush-Modus. Wer „schreibt nichts\" zusichert, muss den Schreibpfad entfernen, nicht auf die Annotation vertrauen.\n- Prüfen, ob die umgebende Methode überhaupt `@Transactional` braucht — oft ist der einfachste Fix, sie wegzulassen.\n- Nach einer abgefangenen `DataIntegrityViolationException` ist die umgebende Transaktion unter PostgreSQL abgebrochen; ein Neulesen braucht einen eigenen Kontext.\n\nAuf Deutsch, im Stil der bestehenden Abschnitte: knappe Stichpunkte, keine Abhandlung. Die drei PR-Nummern als Beleg nennen, damit der Abschnitt nicht als abstrakte Vorsicht gelesen wird.\n\nPrüfen, ob die Client-Adapter (`.claude/agents/developer.md`, `.codex/agents/developer.toml`, `.opencode/agents/developer.md`) unverändert bleiben können — sie verweisen nur auf den gemeinsamen Vertrag und sollten nichts Inhaltliches enthalten.\n\n## Abnahmekriterien\n\n- [ ] `agents/roles/developer.md` enthält den Abschnitt\n- [ ] Die drei Fälle sind als Beleg genannt\n- [ ] Client-Adapter bleiben frei von Inhalt\n- [ ] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt",
        "closes": []
      },
      {
        "number": 300,
        "title": "fix(group): DirectorySyncStatusRecorder behandelt Race auf uk_directory_sync_status_organization nicht",
        "url": "https://github.com/criew/opaa/issues/300",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:S"
        ],
        "body": "## Kontext\n\n`DirectorySyncStatusRecorder.record` (`backend/src/main/java/io/opaa/group/sync/DirectorySyncStatusRecorder.java:39`) folgt dem Muster\n\n```java\nfindByOrganizationId(...).orElseGet(() -> new DirectorySyncStatus(organizationId))\n```\n\ngegen die Unique-Constraint `uk_directory_sync_status_organization`, ohne eine `DataIntegrityViolationException` zu behandeln. Zwischen Prüfung und Anlage liegt keine Absicherung.\n\nGemeldet beim Bearbeiten von #293, wo dasselbe Muster in `UserService.findOrCreateUser` behoben wurde.\n\n## Risiko\n\nGeringer als bei #293: Synchronisationsläufe werden je Organisation von einem Administrator angestoßen und laufen typischerweise nicht parallel. Der Fall tritt nur beim **allerersten** Lauf einer Organisation ein, und nur wenn zwei Läufe gleichzeitig starten.\n\nDie Nachprüfung zu PR #297 hat allerdings festgestellt, dass zwei gleichzeitige Läufe derzeit **nicht serialisiert** sind — es hindert nichts zwei Administratoren daran, `run` parallel auszulösen. Damit ist der Fall konstruierbar, wenn auch selten.\n\n## Umsetzung\n\nDasselbe Muster wie in `UserService.findOrCreateUser` (#293) und `SpaceService.ensurePersonalSpace` (#265): `DataIntegrityViolationException` fangen und neu lesen.\n\nZu beachten: `record` läuft in einem eigenen Transaktionskontext. Prüfe vor der Umsetzung die Historie von `REQUIRES_NEW` in diesem Projekt — die Konstruktion hat in #280 und #297 jeweils einen Fehler verursacht, beide Male durch CI gerutscht.\n\nAlternativ oder ergänzend zu erwägen: die in der Nachprüfung zu #297 vorgeschlagene Serialisierung gleichzeitiger Läufe je Organisation (Advisory-Lock). Sie würde diesen Fall mit erledigen und zugleich das dort dokumentierte Fenster zwischen Verzeichnisabruf und Anwendung schließen. Bewerte, was verhältnismäßig ist.\n\n## Abnahmekriterien\n\n- [ ] Zwei gleichzeitige Erstläufe derselben Organisation führen zu genau einer Statuszeile, ohne Fehler beim Aufrufer\n- [ ] Test mit echten Threads gegen Postgres mit Liquibase-Schema (`spring.liquibase.enabled=true`, `ddl-auto=none`)\n- [ ] Reproduktionsnachweis: Der Test schlägt ohne den Fix fehl, mit konkreter Fehlermeldung in der PR-Beschreibung\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nGefunden bei der Umsetzung von #293. Betrifft Code aus #237 / PR #297.",
        "closes": []
      }
    ],
    "closed": [
      {
        "number": 302,
        "title": "docs(agents): Umgang mit Transaktionen in die Entwickler-Rollendefinition aufnehmen",
        "url": "https://github.com/criew/opaa/issues/302",
        "author": "criew",
        "labels": [
          "documentation",
          "size:S"
        ],
        "body": "## Kontext\n\nIn Stufe A von #198 hat dieselbe Konstruktion dreimal einen Fehler verursacht, jedes Mal fachlich falscher Code bei grünem CI, jedes Mal erst im Review gefunden:\n\n| PR | Konstruktion | Fehler |\n|---|---|---|\n| #280 | `REQUIRES_NEW` innerhalb einer offenen Transaktion | Eigene Connection sah die noch nicht committete `users`-Zeile nicht, `fk_spaces_owner` schlug fehl — **jede Erstanmeldung** war gebrochen |\n| #297 | `REQUIRES_NEW` für einen Statusschreiber | Committete vor der Hauptransaktion und meldete `APPLIED` für eine Anwendung, die zurückgerollt wurde |\n| #299 | `REQUIRES_NEW` neben einer haltenden äußeren Transaktion | Zwei Pool-Connections gleichzeitig je paralleler Anfrage; ab Pool-Größe Stillstand für 30 Sekunden, danach Fehler — auch für unbeteiligte Requests |\n\nDazu kam in #297 ein verwandter Fall: Eine Planberechnung unter `@Transactional(readOnly = true)` behauptete im Javadoc, „nichts wurde geschrieben\" sei strukturell garantiert — der `save()`-Aufruf stand im Code und wurde nur vom Flush-Modus geschluckt.\n\nIn allen Fällen war die Absicht richtig und die Nebenwirkung überraschend. Und in allen Fällen war die beste Lösung, die Konstruktion **ganz zu vermeiden**, statt sie zu reparieren.\n\n## Warum in die Rollendefinition und nicht in AGENTS.md\n\nEs ist Handwerkswissen des Entwicklers, keine Projektkonvention. Entscheidung des Maintainers.\n\n## Umsetzung\n\nEinen Abschnitt zu Transaktionen in `agents/roles/developer.md` aufnehmen, sinnvollerweise zwischen „Testschutz\" und „Umfang und Blocker\". Inhalt:\n\n- Vor dem Einsatz einer eigenen Transaktion (`REQUIRES_NEW`, `TransactionTemplate`) prüfen, ob sie wirklich gebraucht wird — sie ist in diesem Projekt dreimal zur Fehlerquelle geworden.\n- Wenn sie gebraucht wird, beide Fehlerrichtungen prüfen und im PR benennen: Was passiert bei Fehlschlag der inneren, was bei Fehlschlag der äußeren Transaktion.\n- Sichtbarkeit: Eine eigene Transaktion sieht nichts, was die umgebende noch nicht committet hat.\n- Commit-Reihenfolge: Eine innere Transaktion committet vor der äußeren. Was sie schreibt, überlebt deren Rollback.\n- Ressourcen: Eine innere Transaktion neben einer haltenden äußeren belegt zwei Connections gleichzeitig. Bei nebenläufigen Aufrufen skaliert das mit der Anzahl paralleler Anfragen.\n- `@Transactional(readOnly = true)` verhindert Schreibzugriffe nicht strukturell, sondern schluckt sie über den Flush-Modus. Wer „schreibt nichts\" zusichert, muss den Schreibpfad entfernen, nicht auf die Annotation vertrauen.\n- Prüfen, ob die umgebende Methode überhaupt `@Transactional` braucht — oft ist der einfachste Fix, sie wegzulassen.\n- Nach einer abgefangenen `DataIntegrityViolationException` ist die umgebende Transaktion unter PostgreSQL abgebrochen; ein Neulesen braucht einen eigenen Kontext.\n\nAuf Deutsch, im Stil der bestehenden Abschnitte: knappe Stichpunkte, keine Abhandlung. Die drei PR-Nummern als Beleg nennen, damit der Abschnitt nicht als abstrakte Vorsicht gelesen wird.\n\nPrüfen, ob die Client-Adapter (`.claude/agents/developer.md`, `.codex/agents/developer.toml`, `.opencode/agents/developer.md`) unverändert bleiben können — sie verweisen nur auf den gemeinsamen Vertrag und sollten nichts Inhaltliches enthalten.\n\n## Abnahmekriterien\n\n- [ ] `agents/roles/developer.md` enthält den Abschnitt\n- [ ] Die drei Fälle sind als Beleg genannt\n- [ ] Client-Adapter bleiben frei von Inhalt\n- [ ] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt",
        "closes": []
      },
      {
        "number": 295,
        "title": "docs(agents): Branch-Regel für Fehlerbehebungen und Hotfixes klarstellen",
        "url": "https://github.com/criew/opaa/issues/295",
        "author": "criew",
        "labels": [
          "documentation",
          "size:S"
        ],
        "body": "## Kontext\n\n`AGENTS.md` legt verbindlich fest, dass Branches immer mit `feature/` beginnen und die GitHub-Issue-ID enthalten.\n\nBeim Hotfix zu PR #287 wurde stattdessen `fix/280_personal-space-transaction` verwendet — naheliegend, weil es sich um eine Fehlerbehebung handelte, aber gegen die Regel. Im Review fiel auf, dass die Formulierung zwar eindeutig ist, aber nicht ausdrücklich sagt, dass sie auch für Fehlerbehebungen und dringende Korrekturen gilt.\n\n## Entscheidung des Maintainers\n\nKein zusätzliches `fix/`-Präfix. **Alle** Branches beginnen mit `feature/`, unabhängig von der Art der Änderung — auch Fehlerbehebungen und Hotfixes.\n\n## Umsetzung\n\nIn `AGENTS.md` unter „Branch-Regel (verbindlich)\" ergänzen, dass die Regel ausnahmslos gilt, einschließlich Fehlerbehebungen und dringender Korrekturen. Der Conventional-Commit-Typ (`fix`, `feat`, …) bleibt davon unberührt und wird weiterhin in der Commit-Nachricht und im PR-Titel geführt.\n\nPrüfen, ob `.claude/rules/workflow.md` und `CONTRIBUTING.md` dieselbe Aussage tragen und angeglichen werden müssen.\n\n## Abnahmekriterien\n\n- [ ] `AGENTS.md` stellt klar, dass die Regel auch für Fehlerbehebungen und Hotfixes gilt\n- [ ] `.claude/rules/workflow.md` und `CONTRIBUTING.md` sind konsistent\n- [ ] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt",
        "closes": []
      },
      {
        "number": 293,
        "title": "fix(auth): Race bei paralleler Erstanmeldung erzeugt 500er auf uq_users_subject_issuer",
        "url": "https://github.com/criew/opaa/issues/293",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:S",
          "auth"
        ],
        "body": "## Kontext\n\n`UserService.findOrCreateUser` prüft auf einen vorhandenen Nutzer und legt ihn andernfalls an. Zwischen Prüfung und Anlage liegt keine Behandlung der Unique-Constraint `uq_users_subject_issuer`.\n\nBei der allerersten Anmeldung eines Nutzers laufen mehrere Requests parallel: `UserProvisioningFilter` greift bei **jedem** authentifizierten Request, und die Single-Page-Anwendung setzt direkt nach dem Login mehrere Aufrufe gleichzeitig ab.\n\nBeim Review von PR #287 empirisch gemessen: Bei vier parallelen Erstanmeldungen desselben Subjects scheiterten in **15 von 15 Runden jeweils drei der vier Aufrufe** mit\n\n```\nduplicate key value violates unique constraint \"uq_users_subject_issuer\"\n```\n\nEiner gewinnt, Nutzer und persönlicher Space entstehen korrekt, ab dem nächsten Request ist alles in Ordnung. Der Nutzer sieht beim ersten Login jedoch Fehler.\n\n## Abgrenzung\n\nNicht durch #280 oder #287 eingeführt — derselbe Code steht unverändert auf `main` und stand dort schon vorher. #287 macht die Anmeldung wieder funktionsfähig; „die erste Anmeldung ist vollständig fehlerfrei\" gilt erst mit diesem Issue.\n\n## Umsetzung\n\nDasselbe Muster wie in `SpaceService.ensurePersonalSpace`: `DataIntegrityViolationException` fangen und den inzwischen von der konkurrierenden Transaktion angelegten Nutzer neu lesen.\n\nZu beachten: Der Re-Read braucht einen eigenen Transaktionskontext, weil ein fehlgeschlagener Insert unter PostgreSQL die umgebende Transaktion abbricht — siehe die Begründung in `SpaceService.ensurePersonalSpace` und PR #287.\n\n## Abnahmekriterien\n\n- [ ] Parallele Erstanmeldungen desselben Subjects führen zu genau einem Nutzer, ohne Fehler beim Aufrufer\n- [ ] Test mit mehreren echten Threads gegen Postgres mit Liquibase-Schema (`spring.liquibase.enabled=true`, `ddl-auto=none`), Muster: `UserServicePersonalSpaceIntegrationTest`\n- [ ] Der Test schlägt ohne den Fix fehl — nachweisen\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nGefunden beim Review von PR #287.",
        "closes": []
      },
      {
        "number": 288,
        "title": "test(backend): FK-abhängige Integrationstests auf echtes Liquibase-Schema umstellen",
        "url": "https://github.com/criew/opaa/issues/288",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:M"
        ],
        "body": "## Kontext\n\nDie Integrationstests laufen überwiegend mit `spring.liquibase.enabled=false` und `spring.jpa.hibernate.ddl-auto=create-drop`. Das Schema kommt damit von Hibernate, nicht von Liquibase — und die beiden unterscheiden sich an einer entscheidenden Stelle.\n\n`Space.ownerId`, `SpaceMembership.userId` und sämtliche `organizationId`-Felder sind schlichte `UUID`-Spalten ohne `@ManyToOne`. Hibernate legt dafür **keine** Fremdschlüssel an, Liquibase schon. **Jeder FK-abhängige Fall ist in diesen Suiten damit ungetestet.**\n\n## Warum das jetzt angegangen werden muss\n\nInnerhalb kurzer Zeit sind zwei Regressionen durch grünes CI gerutscht, beide aus derselben Wurzel:\n\n1. **PR #254** — Changelog 008 brach auf jedem nicht-leeren Bestand ab. Nicht bemerkt, weil die Integrationstests Liquibase gar nicht ausführen und die einzige Stelle, die es anwendet, gegen eine leere Datenbank läuft.\n2. **PR #280** — die `REQUIRES_NEW`-Transaktion in `ensurePersonalSpace` brach auf `main` jede Erstanmeldung, weil der Insert gegen `fk_spaces_owner` lief. Nicht bemerkt, weil dieser Fremdschlüssel im Testschema nicht existiert. Behoben in PR #287.\n\nBeide Male war der Code fachlich falsch und die Tests waren grün. Das ist kein Zufall, sondern ein struktureller blinder Fleck.\n\n**#201** (Wissensbibliothek als Dokumentencontainer) und **#202** (Asset-Rechte und rechtebewusste Vektorsuche) führen weitere Fremdschlüssel und die gesamte Rechtedurchsetzung ein. Ein blinder Fleck an dieser Stelle wäre deutlich teurer als die beiden bisherigen Fälle.\n\n## Umsetzung\n\nSchrittweise Umstellung FK-abhängiger Tests auf `spring.liquibase.enabled=true` und `ddl-auto=none`. Als Muster dient `UserServicePersonalSpaceIntegrationTest` aus PR #287.\n\nNicht jeder Test muss umgestellt werden — reine Service-Logik ohne Bezug auf Fremdschlüssel darf beim schnelleren Hibernate-Schema bleiben. Zu entscheiden ist, welche Suiten betroffen sind; Kandidaten sind `SpaceServiceIntegrationTest`, `SpaceRepositoryTest` und alles, was Nutzer-, Space- oder Organisationsbezüge schreibt.\n\nZusätzlich zu klären: ob die Laufzeit darunter leidet und ob sich ein gemeinsamer Testcontainer über mehrere Suiten hinweg lohnt.\n\n## Nebenpunkt: uneinheitliche Teardown-Muster\n\nIn den Migrationstests stehen inzwischen zwei Lösungen für dasselbe Problem nebeneinander — Liquibase lässt die JDBC-Verbindung nach `update()` mit `autoCommit=false` zurück:\n\n- `Migration010SpaceUniquenessTest`: `setAutoCommit(true)` nach jedem `update()`, Schema-Reset im `tearDown()`\n- `Migration009CreateGroupsTest`: `connection.rollback()` im `tearDown()`\n\nBeides ist korrekt. Vor #237 und #238 sollte eines davon als verbindliches Muster festgelegt werden, damit die Folge-Issues nicht raten müssen.\n\n## Abnahmekriterien\n\n- [ ] Entscheidung dokumentiert, welche Suiten auf Liquibase-Schema laufen und welche nicht\n- [ ] Betroffene Suiten umgestellt, alle Tests grün\n- [ ] Ein Test, der ohne Fremdschlüssel bestehen würde, schlägt nach der Umstellung ohne den zugehörigen Produktivcode fehl (Nachweis, dass die Umstellung wirkt)\n- [ ] Einheitliches Teardown-Muster für Migrationstests festgelegt und dokumentiert\n- [ ] Laufzeit der Testsuite gemessen und bewertet\n- [ ] Backend-Formatierung, Build und Test bestehen\n\n## Bezug\n\nFolge aus PR #254, #280 und #287. Gehört zu Stufe A von #198 und sollte **vor #202** abgeschlossen sein.",
        "closes": []
      }
    ],
    "merged": [
      {
        "number": 303,
        "title": "docs(agents): Umgang mit Transaktionen in die Entwicklerrolle aufnehmen",
        "url": "https://github.com/criew/opaa/pull/303",
        "author": "criew",
        "labels": [
          "documentation",
          "size:S"
        ],
        "body": "## Zusammenfassung\n\nErgänzt `agents/roles/developer.md` um einen Abschnitt zu Transaktionen.\n\nAnlass sind drei Fehler aus Stufe A von #198, die dieselbe Wurzel haben — jedes Mal fachlich falscher Code bei grünem CI, jedes Mal erst im Review gefunden:\n\n| PR | Fehler |\n|---|---|\n| #280 | `REQUIRES_NEW` innerhalb einer offenen Transaktion sah die noch nicht committete `users`-Zeile nicht — **jede Erstanmeldung** war gebrochen |\n| #297 | Ein Statusschreiber committete vor der Hauptransaktion und meldete `APPLIED` für eine Anwendung, die zurückgerollt wurde |\n| #299 | Zwei Pool-Connections gleichzeitig je paralleler Anfrage; ab Pool-Größe Stillstand für 30 Sekunden — auch für unbeteiligte Requests |\n\nDazu ein verwandter Fall aus #297: Eine Planberechnung unter `@Transactional(readOnly = true)` sicherte im Javadoc zu, „nichts wurde geschrieben\" sei strukturell garantiert — der `save()`-Aufruf stand im Code und wurde nur vom Flush-Modus geschluckt.\n\nIn allen Fällen war die Absicht richtig und die Nebenwirkung überraschend. Und in allen Fällen war die beste Lösung, die Konstruktion ganz zu vermeiden statt sie zu reparieren — deshalb steht die Frage, ob die umgebende Methode überhaupt `@Transactional` braucht, jetzt an erster Stelle.\n\nDie Client-Adapter (`.claude/`, `.codex/`, `.opencode/`) bleiben unverändert: Sie verweisen ausschließlich auf den gemeinsamen Vertrag und enthalten nichts Inhaltliches — geprüft.\n\n## Zugehörige Issues\n\nCloses #302\n\n## Art der Änderung\n\n- [x] Dokumentation\n\n## Checkliste\n\n- [x] Abschnitt in `agents/roles/developer.md`, im Stil der bestehenden Abschnitte\n- [x] Die drei Fälle als Beleg genannt, damit der Abschnitt nicht als abstrakte Vorsicht gelesen wird\n- [x] Client-Adapter geprüft, bleiben frei von Inhalt\n- [x] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt\n\n## KI-Agenten-Offenlegung\n\nErstellt von Claude Opus 5 als Orchestrator, auf Weisung des Maintainers.",
        "closes": [
          302
        ],
        "additions": 13,
        "deletions": 0,
        "changed_files": 1
      },
      {
        "number": 299,
        "title": "fix(auth): Race bei paralleler Erstanmeldung auf uq_users_subject_issuer behoben",
        "url": "https://github.com/criew/opaa/pull/299",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "auth"
        ],
        "body": "## Zusammenfassung\n\n`UserService.findOrCreateUser` prüfte auf einen vorhandenen Nutzer und legte ihn andernfalls an, ohne die Unique-Constraint `uq_users_subject_issuer` zu behandeln. Bei der allerersten Anmeldung eines Nutzers laufen mehrere Requests parallel (`UserProvisioningFilter` bei jedem authentifizierten Request, mehrere Aufrufe der SPA direkt nach dem Login) — mehrere davon scheiterten mit einer 500er-Antwort auf `duplicate key value violates unique constraint \"uq_users_subject_issuer\"`.\n\n**Ansatz:** Dasselbe Muster wie `SpaceService.ensurePersonalSpace` (#265/#280/#287): Der Insert des neuen Nutzers läuft jetzt in einer eigenen `REQUIRES_NEW`-Transaktion, auf einer eigenen Connection. `findOrCreateUser` ist selbst `@Transactional` — auf Postgres bricht ein fehlgeschlagenes Statement die gesamte umgebende Transaktion ab, ein Catch innerhalb derselben Transaktion/Connection hätte also auch das anschließende Neulesen des Gewinner-Datensatzes unmöglich gemacht. Schlägt die `REQUIRES_NEW`-Transaktion mit `DataIntegrityViolationException` fehl, bleibt die aufrufende Transaktion unberührt, und der Verlierer liest den inzwischen committeten Nutzer einfach neu ein, statt den Fehler durchzureichen.\n\n**Warum kein `TransactionSynchronization#afterCommit` nötig ist** (anders als bei `ensurePersonalSpace`, siehe #280/#287): Der `REQUIRES_NEW`-Insert hier hängt von nichts ab, das die aufrufende Transaktion zuvor geschrieben hat — er benötigt nur, dass sein eigener Datensatz eindeutig ist, was Postgres unabhängig vom Zustand anderer Connections durchsetzt. Es gibt also kein Sichtbarkeitsproblem wie bei `fk_spaces_owner`, das eine Verzögerung auf den Commit-Zeitpunkt erfordern würde.\n\n**Zu `REQUIRES_NEW`, da dieses Muster im Projekt schon zweimal Ärger gemacht hat:**\n- Scheitert die innere `REQUIRES_NEW`-Transaktion (Konstellationsverlierer): Die äußere Transaktion (`findOrCreateUser`) ist unberührt, da sie nie selbst das fehlschlagende Statement ausgeführt hat — sie liest anschließend einfach den committeten Gewinner-Datensatz.\n- Scheitert die äußere Transaktion später (z. B. beim Registrieren des `afterCommit`-Hooks für `ensurePersonalSpace` oder danach): Der bereits committete Nutzer-Datensatz aus der `REQUIRES_NEW`-Transaktion bleibt bestehen — das ist beabsichtigt und identisch zum bereits etablierten Verhalten von `ensurePersonalSpace`, nicht neu eingeführt durch diesen PR.\n\n## Reproduktionsnachweis\n\nVor dem Fix (nur `createOrFetchUser` temporär auf direkten Insert ohne Race-Behandlung zurückgesetzt, `UserServiceCreationRaceIntegrationTest` isoliert ausgeführt):\n\n```\nUserServiceCreationRaceIntegrationTest > concurrentFirstLoginsOfTheSameSubjectCreateExactlyOneUser() FAILED\n    java.util.concurrent.ExecutionException\n        Caused by: org.springframework.dao.DataIntegrityViolationException\n            ... could not execute statement [ERROR: duplicate key value violates unique constraint \"uq_users_subject_issuer\"\n  Detail: Key (subject, issuer)=(e348b76b-c514-4255-9a05-638b3a879adc, test-issuer) already exists.]\n```\n\nMit dem Fix wiederhergestellt: `concurrentFirstLoginsOfTheSameSubjectCreateExactlyOneUser()` bestanden (4 parallele echte Threads, echtes Postgres mit Liquibase-Schema, `spring.liquibase.enabled=true`, `ddl-auto=none`).\n\n## Änderungen\n\n- `UserService.findOrCreateUser` teilt sich jetzt in `updateExistingUser` (unverändert) und `createOrFetchUser` (neu, mit Race-Behandlung via `REQUIRES_NEW` + Catch + Neu-Lesen)\n- `UserServiceCreationRaceIntegrationTest` (neu): 4 parallele echte Threads gegen echtes Postgres/Liquibase, Muster `UserServicePersonalSpaceIntegrationTest`\n- `UserServiceTest`: umgestellt von `@InjectMocks`/`MockitoExtension` auf manuelle Konstruktion (analog `SpaceServiceTest`), da der Konstruktor jetzt einen `PlatformTransactionManager` erwartet; zwei neue Fälle für Race-Gewinn-Lesen und unabhängige Constraint-Verletzungen\n\n## Weitere Fundstellen desselben Musters (nicht behoben, zur Information)\n\n- `DirectorySyncStatusRecorder.record` (`io.opaa.group.sync`): `statusRepository.findByOrganizationId(...).orElseGet(() -> new DirectorySyncStatus(organizationId))` gegen die Unique-Constraint `uk_directory_sync_status_organization`, ohne Behandlung einer `DataIntegrityViolationException`. Vermutlich geringes Risiko, da Sync-Läufe pro Organisation nicht typischerweise parallel angestoßen werden — aber dasselbe Check-then-Create-Muster wie hier.\n\n## Zugehörige Issues\n\nCloses #293\n\n## Art der Änderung\n\n- [x] Bugfix\n- [ ] Neues Feature\n- [ ] Dokumentation\n- [ ] Refactoring\n- [ ] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal\n- [x] Bei Bugfix: Reproduktionsnachweis erbracht — der Test schlägt auf dem fehlerhaften Stand fehl und besteht mit dem Fix (rote Fehlermeldung oben eingefügt)\n- [ ] Dokumentation aktualisiert (falls zutreffend) — nicht erforderlich\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet) — bereits unterzeichnet\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Code, Developer-Rolle)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
        "closes": [
          293
        ],
        "additions": 282,
        "deletions": 50,
        "changed_files": 3
      },
      {
        "number": 298,
        "title": "test(backend): FK-abhängige Space-Tests auf echtes Liquibase-Schema umstellen",
        "url": "https://github.com/criew/opaa/pull/298",
        "author": "criew",
        "labels": [
          "bug",
          "backend",
          "size:M"
        ],
        "body": "## Zusammenfassung\n\nStellt die Fremdschlüssel-abhängigen Space-Integrationstests (`SpaceServiceIntegrationTest`, `SpaceRepositoryTest`) von Hibernate-generiertem Schema (`spring.liquibase.enabled=false`, `ddl-auto=create-drop`) auf das echte, versionierte Liquibase-Schema um (`spring.liquibase.enabled=true`, `ddl-auto=none`), nach dem in `UserServicePersonalSpaceIntegrationTest` (#287) etablierten Muster. `Space.ownerId`, `SpaceMembership.userId` und alle `organizationId`-Felder sind schlichte `UUID`-Spalten ohne `@ManyToOne` — Hibernate legt dafür keine Fremdschlüssel an, Liquibase schon (`fk_spaces_owner`, `fk_space_memberships_user`, `fk_spaces_organization`, `fk_space_memberships_organization`). Jeder FK-abhängige Fall war in diesen Suiten bisher ungetestet und genau das war die Wurzel der Regressionen in PR #254 und #280.\n\n**Update nach Review:** Beide Blocker und alle vier Nits aus dem Review sind behoben, siehe Kommentar unten für die Details je Befund.\n\n### Entscheidung: welche Suiten umgestellt wurden und welche nicht\n\n| Suite | Umgestellt? | Begründung |\n|---|---|---|\n| `SpaceServiceIntegrationTest` | Ja | Erzeugt Spaces/Memberships mit echten Owner-/Member-/Organisations-Bezügen über `SpaceService` — genau der FK-abhängige Fall |\n| `SpaceRepositoryTest` | Ja | Gleicher Grund, auf Repository-Ebene |\n| `UserServicePersonalSpaceIntegrationTest` (#287) | War bereits umgestellt | Als Muster übernommen; Container-Definition jetzt über die gemeinsame `TestcontainersConfiguration` statt einer eigenen Kopie |\n| `SpaceServiceTest` | Bewusst **nicht** umgestellt | Reine Unit-Logik gegen gemockte Repositories (`@Mock`), keine reale Datenbankverbindung, kein Fremdschlüsselbezug — hier bringt Liquibase keinen Mehrwert, nur Laufzeit |\n| `QueryIntegrationTest`, `DocumentIndexingIntegrationTest`, `ProviderConfigurationTest`, `MixedProviderConfigurationTest`, `OpenAiIntegrationTest` | **Bereits auf dem Liquibase-Schema, kein Handlungsbedarf** | Keine dieser Klassen setzt `spring.liquibase.enabled=false` — sie nutzen die Anwendungsdefaults (`application.yml`: `ddl-auto: none`, `liquibase.enabled: true`) und liefen von Anfang an gegen das volle Liquibase-Schema. Es gab hier nichts umzustellen. |\n| `Migration008RenameWorkspaceToSpaceTest`, `Migration010SpaceUniquenessTest` | Unverändert (liefen bereits gegen echtes Liquibase per Definition) | Wenden Changelogs direkt per JDBC an, kein Hibernate im Spiel |\n\n`TestcontainersConfiguration` (`io.opaa`) ist jetzt `public` statt package-private und wird von allen drei betroffenen Klassen per `@Import` geteilt, statt den Container-Bean ein drittes Mal zu duplizieren (Hinweis aus dem Review von PR #287).\n\n### Nachweis der Wirksamkeit (Abnahmekriterium)\n\n**Ergänzt nach Review:** Der ursprüngliche Nachweis (Revert des #280-Fixes) reproduzierte #287, nicht #288 — `UserServicePersonalSpaceIntegrationTest` lief schon vor diesem PR gegen das Liquibase-Schema und wird hier nicht umgestellt. Der eigentliche Nachweis für die tatsächlich umgestellten Klassen steht jetzt in `SpaceRepositoryTest` selbst, als zwei permanente Regressionswächter:\n\n- `savingASpaceWithANonExistentOwnerFailsInsteadOfSilentlyPersisting()`\n- `savingASpaceWithANonExistentOrganizationFailsInsteadOfSilentlyPersisting()`\n\nBeide rufen `spaceRepository.saveAndFlush(space)` auf — Produktivcode (`SpaceRepository` wird von `SpaceService` und jedem zukünftigen Aufrufer verwendet) — mit einer `ownerId` bzw. `organizationId`, die nicht in der Datenbank existiert.\n\n**Rot** (`SpaceRepositoryTest` testweise mit den alten Properties `spring.liquibase.enabled=false`, `ddl-auto=create-drop`, nur die beiden neuen Tests):\n```\nSpaceRepositoryTest > savingASpaceWithANonExistentOwnerFailsInsteadOfSilentlyPersisting() FAILED\n    java.lang.AssertionError: Expecting code to raise a throwable.\n\nSpaceRepositoryTest > savingASpaceWithANonExistentOrganizationFailsInsteadOfSilentlyPersisting() FAILED\n    java.lang.AssertionError: Expecting code to raise a throwable.\n\n2 tests completed, 2 failed\n```\nDer Insert eines Space mit nicht existierendem Owner/Organisation gelang unter dem alten Schema lautlos — exakt die Lücke, die #288 schließt.\n\n**Grün** (mit dem in diesem PR eingeführten Liquibase-Schema, den Properties aus `main`):\n```\nBUILD SUCCESSFUL in 27s\n```\nBeide Tests werfen jetzt `DataIntegrityViolationException` mit `fk_spaces_owner` bzw. `fk_spaces_organization` in der Meldung.\n\nDer ursprüngliche #280-Nachweis über `UserServicePersonalSpaceIntegrationTest` bleibt als Beleg dafür im PR, dass das etablierte Muster (#287) grundsätzlich wirkt — er ist nur nicht der Wirksamkeitsnachweis *dieses* PRs.\n\nZusätzlich beim Umstellen selbst ein reales Cross-Test-Problem gefunden und behoben: Der erste Entwurf von `SpaceServiceIntegrationTest#cleanUp()` rief `organizationRepository.deleteAll()` auf und löschte damit versehentlich die von Liquibase einmalig geseedete Standard-Organisation (`Organization.DEFAULT_ID`), von der `UserServicePersonalSpaceIntegrationTest` abhängt, sobald beide Klassen denselben Spring-Kontext teilen. Der volle Testlauf (`./gradlew test`) deckte das sofort auf (3 Fehlschläge mit `fk_users_organization`-Verletzung); behoben, indem jeder Test nur seine eigenen, frisch angelegten Organisationen anlegt statt alle zu löschen.\n\n### Aufräumen der selbst angelegten Organisationen (Nit 4)\n\n`SpaceServiceIntegrationTest` und `SpaceRepositoryTest` löschen ihre je Testmethode angelegten Organisationen jetzt in `@AfterEach` per ID (nach den davon abhängigen Users/Spaces/Memberships) — `Organization.DEFAULT_ID` bleibt unangetastet. Vorher sammelten sich pro vollem Lauf ~50 Restzeilen in der geteilten Datenbank an; das ist behoben.\n\n### Teardown-Muster für Migrationstests vereinheitlicht (Nit 5)\n\n`backend/src/test/java/io/opaa/migration/package-info.java` (neu) macht `connection.setAutoCommit(true)` nach jedem `Liquibase.update(...)` **unbedingt** verbindlich für dieses Package (nicht nur „falls danach noch etwas gebraucht wird\") und dokumentiert die Begründung — das im Review von #283 empfohlene Muster, weil es die Ursache (Liquibase lässt `autoCommit=false` zurück) behebt statt nur das Symptom in `tearDown()` zu kaschieren. `Migration008RenameWorkspaceToSpaceTest` ist als dokumentierte Altlast (vor #283 entstanden) benannt, nicht als zweite gültige Alternative. Adressiert an #201, #202 und #238.\n\n### Laufzeit\n\n- Beide konvertierten Klassen isoliert: **27–32s** (neu, gemeinsamer Kontext) vs. **62s** (alt, zwei separate `@DynamicPropertySource`-Container ohne `@ServiceConnection`-Caching) — die Umstellung ist hier sogar schneller, weil der Kontext jetzt mit `UserServicePersonalSpaceIntegrationTest` geteilt wird.\n- Voller Backend-Build (`./gradlew build`, alle 231+2 Tests): **~2m 30s**, wiederholt stabil bei 2m–2m15s (231–233 bestanden, 1 übersprungen ohne Docker).\n- **Korrektur (Nit 6):** `OpaaApplicationTests` teilt den Kontext entgegen der ursprünglichen PR-Beschreibung *nicht* — ihr fehlen `@ActiveProfiles`/`@TestPropertySource`, sie hat damit einen eigenen Cache-Key und startet einen eigenen Container. Der geteilte Kontext umfasst `SpaceServiceIntegrationTest`, `SpaceRepositoryTest` und `UserServicePersonalSpaceIntegrationTest`. Am Laufzeit-Fazit ändert das nichts.\n- Eine isolierte Baseline-Messung auf dem alten Stand (`git stash`) lieferte in dieser gemeinsam genutzten Umgebung (mehrere parallele Agent-Worktrees, gleicher Docker-Daemon) stark schwankende Werte (9m45s mit 14 infrastrukturbedingten Fehlschlägen durch Verbindungsabbrüche) — kein belastbarer Vergleichswert unter Last.\n- Bewertung: Kein spürbarer Laufzeit-Nachteil. Ein über mehrere Suiten geteilter Testcontainer lohnt sich nicht zusätzlich — er existiert durch `@ServiceConnection` + `@Import(TestcontainersConfiguration.class)` bereits: Spring cached identische Kontexte automatisch.\n\n## Was #201, #202 und #238 wissen müssen\n\n- **Muster für FK-abhängige Service-/Repository-Tests:** `@SpringBootTest(webEnvironment = RANDOM_PORT)` + `@Import(TestcontainersConfiguration.class)` + `@ActiveProfiles({\"local\", \"basic\"})` + `@TestPropertySource(properties = \"OPAA_AUTH_BASIC_SECRET=...\")` + `@Testcontainers(disabledWithoutDocker = true)`. Kein `@TestPropertySource` mehr für Liquibase/ddl-auto nötig — das sind bereits die Anwendungsdefaults (`application.yml`). `WebEnvironment.NONE` funktioniert **nicht**, weil `BasicSecurityConfig` einen Web-Anwendungskontext braucht (`HttpSecurity`-Bean fehlt sonst).\n- **Jede Organisation/jeder Nutzer muss real persistiert sein**, bevor er als `organizationId`/`ownerId`/`userId` verwendet wird — sonst schlägt der Insert mit der jeweiligen `fk_*`-Verletzung fehl. Bare `UUID.randomUUID()` als Platzhalter für Owner/Member/Organisation funktioniert unter diesem Schema nicht mehr.\n- **`organizationRepository.deleteAll()` in `@BeforeEach` niemals verwenden**, solange der Kontext geteilt wird — das löscht die von Liquibase geseedete `Organization.DEFAULT_ID` und reißt jeden Test um, der sich (wie `UserService.findOrCreateUser`) implizit auf ihre Existenz verlässt. Stattdessen pro Test eigene, frische Organisationen mit zufälliger ID anlegen und in `@AfterEach` gezielt per ID wieder entfernen.\n- **Teardown-Muster für Migrationstests:** `connection.setAutoCommit(true)` **unbedingt** nach jedem `Liquibase.update(...)`, dokumentiert in `io.opaa.migration.package-info`. `Migration008RenameWorkspaceToSpaceTest` folgt dem nicht — das ist eine dokumentierte Altlast, kein Muster zum Nachahmen.\n- **Wirksamkeitsnachweis muss zur eigenen Suite gehören:** Ein Nachweis über eine bereits vorher FK-fähige Klasse belegt nicht die Wirksamkeit der eigenen Umstellung — siehe die Korrektur oben.\n\n## Zugehörige Issues\n\nCloses #288\n\n## Art der Änderung\n\n- [ ] Bugfix\n- [ ] Neues Feature\n- [ ] Dokumentation\n- [x] Refactoring\n- [ ] CI/Build\n\n## Checkliste\n\n- [x] Tests bestehen lokal\n- [x] Bei Bugfix: Reproduktionsnachweis erbracht — siehe Abschnitt „Nachweis der Wirksamkeit\" oben (kein Bugfix im engeren Sinn, aber derselbe Nachweis erbracht)\n- [x] Dokumentation aktualisiert (falls zutreffend) — Javadoc in den umgestellten Testklassen und neues `io.opaa.migration.package-info`\n- [x] Keine Secrets oder Anmeldeinformationen committet\n- [x] Commit-Nachrichten folgen Conventional Commits\n- [ ] CLA unterzeichnet (Erstbeitragende: Unterzeichnungskommentar unten posten, oder wurde bereits unterzeichnet)\n\n## Annahmen\n\n- Frontend ist von dieser Änderung nicht betroffen; die Frontend-Schritte der Pre-Push-Checkliste (`npm run format/lint/test/build`) wurden deshalb nicht ausgeführt (kein `node_modules` in diesem frischen Worktree, keine Frontend-Datei geändert).\n- `SpaceServiceTest` (reine Unit-Tests mit gemockten Repositories) bewusst nicht umgestellt — dort existiert keine reale Datenbankverbindung und damit kein Fremdschlüsselbezug.\n\n## KI-Agenten-Offenlegung\n\n- [ ] Dieser PR wurde von einem Menschen verfasst\n- [x] Dieser PR wurde von einem KI-Agenten verfasst (angeben: Claude Opus 5 / Claude Code, Developer-Rolle)\n- [ ] Dieser PR wurde von Mensch + KI-Agent gemeinsam verfasst",
        "closes": [
          288
        ],
        "additions": 229,
        "deletions": 82,
        "changed_files": 6
      },
      {
        "number": 296,
        "title": "docs(agents): Branch-Regel klarstellen und Reproduktionsnachweis verbindlich machen",
        "url": "https://github.com/criew/opaa/pull/296",
        "author": "criew",
        "labels": [
          "documentation",
          "size:S"
        ],
        "body": "## Zusammenfassung\n\nZwei Klarstellungen an den Agenten- und Workflow-Regeln, beide ausgelöst durch Befunde aus den Reviews zu Stufe A von #198.\n\n### 1. Branch-Regel gilt ausnahmslos\n\nBeim Hotfix in PR #287 wurde der Branch `fix/280_personal-space-transaction` verwendet. Die Regel in `AGENTS.md` war eindeutig, sagte aber nicht ausdrücklich, dass sie auch für Fehlerbehebungen gilt.\n\nEntscheidung des Maintainers: **Alle** Branches beginnen mit `feature/`. Kein `fix/`-, `hotfix/`- oder `docs/`-Präfix. Die Art der Änderung wird über den Conventional-Commit-Typ ausgedrückt. In `CONTRIBUTING.md` stand mit `feature/15_fix-null-pointer` bereits ein passendes Beispiel — die Absicht war immer so gemeint, nur nicht ausgesprochen.\n\n### 2. Reproduktionsnachweis bei Fehlerbehebungen\n\n**Dreimal in Folge war der Produktivcode fehlerhaft und der zugehörige Test trotzdem grün:**\n\n| PR | Fehler | Warum der Test ihn nicht fing |\n|---|---|---|\n| #254 | Migration brach auf nicht-leerem Bestand ab | Changelog wurde in den Tests nie ausgeführt |\n| #280 | `REQUIRES_NEW` brach jede Erstanmeldung | Testschema kannte den Fremdschlüssel nicht |\n| #283 | Cache-Invalidierung vor dem Commit | Test prüft eine Bedingung, die vor und nach dem Fix gilt |\n\nGefunden hat es jedes Mal erst der Code Reviewer — indem er den Fix zurückdrehte und schaute, ob der Test dann fehlschlägt. `AGENTS.md` verlangte bisher den reproduzierenden Test, aber nicht den Beleg, dass er den Fehler auch fängt.\n\nNeu ist ein Abschnitt „Reproduktionsnachweis\" mit den vier bisher beobachteten Ursachen für wirkungslose Tests, eine Regel in `.claude/rules/workflow.md` und ein Checklistenpunkt im PR-Template.\n\n## Zugehörige Issues\n\nCloses #295\n\n## Art der Änderung\n\n- [x] Dokumentation\n\n## Checkliste\n\n- [x] `AGENTS.md`, `.claude/rules/workflow.md`, `CONTRIBUTING.md` und das PR-Template tragen konsistente Aussagen\n- [x] Reine Dokumentationsänderung, Pre-Push-Checkliste entfällt\n\n## KI-Agenten-Offenlegung\n\nErstellt von Claude Opus 5 als Orchestrator, auf Weisung des Maintainers. Auslöser waren Befunde aus den Reviews zu PR #254, #280, #283 und #287.",
        "closes": [
          295
        ],
        "additions": 22,
        "deletions": 2,
        "changed_files": 4
      }
    ]
  }
}